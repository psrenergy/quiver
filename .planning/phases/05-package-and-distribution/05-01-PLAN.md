---
phase: 05-package-and-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bindings/js/package.json
  - bindings/js/bunfig.toml
  - bindings/js/test/setup.ts
  - bindings/js/biome.json
  - bindings/js/README.md
  - bindings/js/src/index.ts
  - bindings/js/src/database.ts
  - bindings/js/src/loader.ts
  - bindings/js/src/errors.ts
  - bindings/js/src/ffi-helpers.ts
  - bindings/js/src/create.ts
  - bindings/js/src/read.ts
  - bindings/js/src/query.ts
  - bindings/js/src/transaction.ts
  - bindings/js/src/types.ts
  - bindings/js/test/database.test.ts
  - bindings/js/test/create.test.ts
  - bindings/js/test/read.test.ts
  - bindings/js/test/query.test.ts
  - bindings/js/test/transaction.test.ts
  - scripts/test-all.bat
autonomous: true
requirements:
  - PKG-01
  - PKG-02
  - PKG-03
  - PKG-04
must_haves:
  truths:
    - "package.json has complete metadata (description, license, keywords, engines, files, types, exports)"
    - "Running 'bun test' from bindings/js/ succeeds without the .bat wrapper (setup.ts preloads PATH)"
    - "Running 'bun run typecheck' passes (tsc --noEmit)"
    - "Running 'bun run lint' passes (biome check)"
    - "test-all.bat includes JS tests in its test run"
    - "README.md documents installation, DLL setup, quick start, and API methods"
  artifacts:
    - path: "bindings/js/package.json"
      provides: "Complete npm package metadata"
      contains: "engines"
    - path: "bindings/js/bunfig.toml"
      provides: "Test preload configuration for standalone bun test"
      contains: "preload"
    - path: "bindings/js/test/setup.ts"
      provides: "DLL PATH setup for standalone test execution"
      contains: "process.env.PATH"
    - path: "bindings/js/biome.json"
      provides: "Biome lint and format configuration"
      contains: "biomejs.dev"
    - path: "bindings/js/README.md"
      provides: "Usage documentation"
      min_lines: 30
    - path: "scripts/test-all.bat"
      provides: "Updated test runner including JS tests"
      contains: "JavaScript"
  key_links:
    - from: "bindings/js/bunfig.toml"
      to: "bindings/js/test/setup.ts"
      via: "preload config"
      pattern: "preload.*setup\\.ts"
    - from: "bindings/js/test/setup.ts"
      to: "bindings/js/src/loader.ts"
      via: "PATH modification before dlopen"
      pattern: "process\\.env\\.PATH"
    - from: "bindings/js/package.json"
      to: "bindings/js/src/index.ts"
      via: "exports and types fields"
      pattern: "src/index\\.ts"
    - from: "scripts/test-all.bat"
      to: "bindings/js/test/test.bat"
      via: "call to JS test runner"
      pattern: "bindings.*js.*test"
---

<objective>
Package the JS/TS binding as a well-structured npm package with complete metadata, TypeScript types, Biome lint/format tooling, standalone test support via bunfig.toml preload, usage documentation, and integration into the project-wide test runner.

Purpose: Make the binding installable, verifiable, and maintainable -- the final step before the v0.5 milestone is complete.
Output: Complete npm package configuration, Biome setup with all violations fixed, README, standalone bun test support, test-all.bat integration.
</objective>

<execution_context>
@C:/Users/rsampaio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rsampaio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-package-and-distribution/05-CONTEXT.md
@.planning/phases/05-package-and-distribution/05-RESEARCH.md

@bindings/js/package.json
@bindings/js/tsconfig.json
@bindings/js/src/index.ts
@bindings/js/test/test.bat
@scripts/test-all.bat

<interfaces>
<!-- Key types and contracts. Existing binding structure. -->

From bindings/js/src/index.ts:
```typescript
import "./create";
import "./read";
import "./query";
import "./transaction";

export { QuiverError } from "./errors";
export { Database } from "./database";
export type { ScalarValue, ArrayValue, Value, ElementData, QueryParam } from "./types";
```

From bindings/js/package.json (CURRENT state -- will be modified):
```json
{
  "name": "quiverdb",
  "version": "0.5.0",
  "type": "module",
  "main": "src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "test": "bun test" }
}
```

From bindings/js/tsconfig.json:
```json
{
  "compilerOptions": {
    "strict": true, "target": "ESNext", "module": "ESNext",
    "moduleResolution": "bundler", "esModuleInterop": true,
    "skipLibCheck": true, "noEmit": true, "types": ["bun-types"]
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
```

From bindings/js/test/test.bat:
```batch
@echo off
pushd %~dp0..
set PATH=%~dp0..\..\..\build\bin;%PATH%
bun test test/ %*
popd
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Package configuration, test preload, Biome setup and lint fix</name>
  <files>
    bindings/js/package.json,
    bindings/js/bunfig.toml,
    bindings/js/test/setup.ts,
    bindings/js/biome.json,
    bindings/js/src/index.ts,
    bindings/js/src/database.ts,
    bindings/js/src/loader.ts,
    bindings/js/src/errors.ts,
    bindings/js/src/ffi-helpers.ts,
    bindings/js/src/create.ts,
    bindings/js/src/read.ts,
    bindings/js/src/query.ts,
    bindings/js/src/transaction.ts,
    bindings/js/src/types.ts,
    bindings/js/test/database.test.ts,
    bindings/js/test/create.test.ts,
    bindings/js/test/read.test.ts,
    bindings/js/test/query.test.ts,
    bindings/js/test/transaction.test.ts
  </files>
  <action>
    **Step 1: Update package.json** with complete npm metadata per user decision:
    ```json
    {
      "name": "quiverdb",
      "version": "0.5.0",
      "description": "Bun-native SQLite wrapper binding for Quiver via bun:ffi",
      "type": "module",
      "main": "src/index.ts",
      "types": "src/index.ts",
      "exports": {
        ".": "./src/index.ts"
      },
      "files": [
        "src/",
        "package.json",
        "tsconfig.json",
        "README.md"
      ],
      "engines": {
        "bun": ">=1.0.0"
      },
      "license": "MIT",
      "keywords": ["sqlite", "database", "ffi", "bun", "quiver"],
      "scripts": {
        "test": "bun test",
        "typecheck": "tsc --noEmit",
        "lint": "biome check",
        "format": "biome format --write"
      },
      "devDependencies": {
        "@biomejs/biome": "^2.4.6"
      }
    }
    ```
    Note: Do NOT add `repository`, `homepage`, `bugs` fields -- this project is not on a public host. Omit rather than use placeholders.

    **Step 2: Create bunfig.toml** for standalone `bun test` support:
    ```toml
    [test]
    preload = ["./test/setup.ts"]
    ```

    **Step 3: Create test/setup.ts** that prepends build/bin/ to PATH before any test imports:
    ```typescript
    import { join, resolve } from "node:path";

    const buildBinDir = resolve(join(import.meta.dir, "..", "..", "..", "build", "bin"));
    const currentPath = process.env.PATH ?? "";

    if (!currentPath.includes(buildBinDir)) {
      process.env.PATH = `${buildBinDir};${currentPath}`;
    }
    ```
    Uses `;` as PATH separator (Windows). Uses `import.meta.dir` (Bun-specific). Resolves path from test/setup.ts -> bindings/js/ -> ../../build/bin/.

    **Step 4: Install Biome** by running `cd bindings/js && bun add -d @biomejs/biome`.

    **Step 5: Create biome.json** with strict/recommended rules per research:
    ```json
    {
      "$schema": "https://biomejs.dev/schemas/2.0.0/schema.json",
      "formatter": {
        "enabled": true,
        "indentStyle": "space",
        "indentWidth": 2,
        "lineWidth": 100
      },
      "linter": {
        "enabled": true,
        "rules": {
          "recommended": true
        }
      },
      "javascript": {
        "formatter": {
          "quoteStyle": "double",
          "semicolons": "always"
        }
      },
      "files": {
        "ignore": ["node_modules/"]
      }
    }
    ```
    If Biome v2 requires a different schema URL or structure, adjust accordingly based on what `bun add` installs.

    **Step 6: Run Biome fix on all code.** Execute `cd bindings/js && bunx --bun @biomejs/biome check --write .` to auto-fix all fixable violations in src/ and test/ files. Review any unsafe fixes. The user decision explicitly says "Fix any existing violations as part of this phase."

    **Step 7: Verify** typecheck and lint pass:
    - `cd bindings/js && bun run typecheck` must succeed
    - `cd bindings/js && bun run lint` must succeed
    - `cd bindings/js && bun test` must succeed (standalone, using bunfig.toml preload)
    - `bindings/js/test/test.bat` must also succeed (existing .bat wrapper still works)
  </action>
  <verify>
    <automated>cd bindings/js && bun test && bun run typecheck && bun run lint</automated>
  </verify>
  <done>
    package.json has all required metadata fields (description, license, keywords, engines, files, types, exports, scripts).
    bunfig.toml exists with test preload pointing to test/setup.ts.
    test/setup.ts prepends build/bin/ to PATH on Windows.
    Biome installed as devDependency, biome.json configured.
    All existing code passes lint and typecheck.
    All 61 tests pass via both `bun test` (standalone) and test.bat (wrapper).
  </done>
</task>

<task type="auto">
  <name>Task 2: README and test-all.bat integration</name>
  <files>
    bindings/js/README.md,
    scripts/test-all.bat
  </files>
  <action>
    **Step 1: Create bindings/js/README.md** with usage-focused documentation per user decision. Structure:

    - **Header**: Package name and one-line description
    - **Requirements**: Bun >= 1.0.0, libquiver.dll and libquiver_c.dll in PATH
    - **Installation**: `bun add quiverdb` (note: native DLLs must be in PATH separately)
    - **DLL Setup**: Explain that consumers need compiled Quiver libraries in system PATH. Point to Quiver build instructions.
    - **Quick Start**: Complete working example showing:
      ```typescript
      import { Database } from "quiverdb";

      const db = Database.fromSchema("my.db", "schema.sql");
      db.createElement("Items", { label: "Item 1", value: 42 });
      const values = db.readScalarIntegers("Items", "value");
      console.log(values); // [42]
      db.close();
      ```
    - **API Methods**: Organized list of all public methods grouped by category:
      - Lifecycle: `Database.fromSchema()`, `Database.fromMigrations()`, `close()`
      - Create/Delete: `createElement()`, `deleteElement()`
      - Read (bulk): `readScalarIntegers()`, `readScalarFloats()`, `readScalarStrings()`
      - Read (by ID): `readScalarIntegerById()`, `readScalarFloatById()`, `readScalarStringById()`
      - Read (IDs): `readElementIds()`
      - Query: `queryString()`, `queryInteger()`, `queryFloat()`
      - Transaction: `beginTransaction()`, `commit()`, `rollback()`, `inTransaction()`
    - **Types**: List exported types: `ScalarValue`, `ArrayValue`, `Value`, `ElementData`, `QueryParam`, `QuiverError`
    - **Development**: Commands for test, typecheck, lint, format
    - **License**: MIT

    Keep it concise and practical. No badges, no extensive prose.

    **Step 2: Update scripts/test-all.bat** to include JavaScript tests. The current script runs 5 test suites (C++, C API, Julia, Dart, Python) with step numbers [1/5] through [5/5]. Changes needed:

    1. Add `SET JS_RESULT=SKIP` to the initialization block (after `SET DART_RESULT=SKIP`)
    2. Add `SET PYTHON_RESULT=SKIP` (if not already present -- verify current state)
    3. Add a new step for JavaScript tests between Dart (Step 4) and Python (Step 5). Renumber Python to Step 6.
    4. Update all step counters from `/5]` to `/6]`
    5. The JS test step calls `"%ROOT_DIR%\bindings\js\test\test.bat"` using the same pattern as Julia/Dart/Python
    6. Add `echo   JS tests:     %JS_RESULT%` to the summary block
    7. Add `echo   Python tests: %PYTHON_RESULT%` to the summary block (verify it is present)

    The new JS test block (Step 5 of 6):
    ```batch
    REM ============================================================
    REM Step 5: Run JavaScript Tests
    REM ============================================================
    echo [5/6] Running JavaScript tests...

    call "%ROOT_DIR%\bindings\js\test\test.bat"
    if errorlevel 1 (
        SET JS_RESULT=FAIL
        SET FAILED=1
    ) else (
        SET JS_RESULT=PASS
    )
    echo.
    ```
  </action>
  <verify>
    <automated>cd bindings/js && bun test && bun run typecheck && bun run lint</automated>
  </verify>
  <done>
    README.md exists with installation, DLL setup, quick start example, API methods list, types, and development commands.
    test-all.bat includes JavaScript test step (6 test suites total).
    All tests still pass after changes.
  </done>
</task>

</tasks>

<verification>
Phase-level verification after all tasks complete:

1. `cd bindings/js && bun test` -- all 61 tests pass standalone (via bunfig.toml preload)
2. `bindings/js/test/test.bat` -- all tests pass via .bat wrapper
3. `cd bindings/js && bun run typecheck` -- TypeScript type checking passes
4. `cd bindings/js && bun run lint` -- Biome linting passes
5. `cd bindings/js && bun run format` -- Biome formatting clean (no changes)
6. Verify package.json has: description, license, keywords, engines, files, types, exports, scripts (typecheck, lint, format)
7. Verify README.md is present and has quickstart, API list, DLL setup instructions
8. Verify test-all.bat has 6 test steps including JavaScript
</verification>

<success_criteria>
- package.json contains all required metadata fields per user decisions
- bunfig.toml enables standalone `bun test` without the .bat wrapper
- test/setup.ts correctly prepends build/bin/ to PATH for DLL loading
- Biome is installed and configured; all code passes lint and format checks
- tsc --noEmit passes (TypeScript type checking)
- README.md provides installation, DLL setup, quick start, API reference, and dev commands
- test-all.bat includes JavaScript tests as a 6th test suite
- All 61 existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-package-and-distribution/05-01-SUMMARY.md`
</output>
