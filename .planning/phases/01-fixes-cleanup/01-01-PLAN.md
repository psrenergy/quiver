---
phase: 01-fixes-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - include/quiver/c/database.h
  - src/c/database_transaction.cpp
  - tests/test_c_api_database_transaction.cpp
  - bindings/julia/src/c_api.jl
  - bindings/julia/src/database_transaction.jl
  - bindings/dart/lib/src/ffi/bindings.dart
  - bindings/dart/lib/src/database_transaction.dart
  - bindings/python/src/quiverdb/_declarations.py
  - bindings/python/src/quiverdb/_c_api.py
  - bindings/python/src/quiverdb/database.py
  - bindings/js/src/transaction.ts
  - src/blob/dimension.cpp
autonomous: true
requirements: [CAPI-01, CLEAN-01]
must_haves:
  truths:
    - "quiver_database_in_transaction uses int* out-param, consistent with all other boolean out-params in the C API"
    - "All existing bindings (Julia, Dart, Python, JS) pass their in_transaction tests with the new signature"
    - "JS binding inTransaction() uses Int32Array(1), not Uint8Array(1)"
    - "src/blob/dimension.cpp no longer exists in the repository"
  artifacts:
    - path: "include/quiver/c/database.h"
      provides: "C API header with int* out_active signature"
      contains: "int\\* out_active"
    - path: "src/c/database_transaction.cpp"
      provides: "C API implementation with int* out_active"
      contains: "int\\* out_active"
    - path: "bindings/js/src/transaction.ts"
      provides: "JS binding using Int32Array for in_transaction"
      contains: "Int32Array"
  key_links:
    - from: "include/quiver/c/database.h"
      to: "src/c/database_transaction.cpp"
      via: "function signature match"
      pattern: "int\\* out_active"
    - from: "src/c/database_transaction.cpp"
      to: "bindings/js/src/transaction.ts"
      via: "FFI call with correct buffer type"
      pattern: "Int32Array\\(1\\)"
    - from: "src/c/database_transaction.cpp"
      to: "bindings/julia/src/c_api.jl"
      via: "ccall with Ptr{Cint}"
      pattern: "Ptr\\{Cint\\}"
    - from: "src/c/database_transaction.cpp"
      to: "bindings/dart/lib/src/ffi/bindings.dart"
      via: "ffi binding with Int32 pointer"
      pattern: "ffi\\.Pointer<ffi\\.Int32>"
---

<objective>
Fix the C API bool inconsistency in quiver_database_in_transaction (bool* -> int*) and delete the empty dead code file src/blob/dimension.cpp.

Purpose: Establish consistent C API conventions before building out JS binding parity. All boolean out-params must use int* to avoid ABI issues across FFI boundaries.
Output: Consistent C API signature, updated bindings (Julia, Dart, Python, JS), removed dead code.
</objective>

<execution_context>
@C:/Users/rsampaio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rsampaio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current C API signature that needs changing (include/quiver/c/database.h line 52): -->
```c
QUIVER_C_API quiver_error_t quiver_database_in_transaction(quiver_database_t* db, bool* out_active);
```

<!-- Target signature (consistent with all other boolean out-params like is_healthy): -->
```c
QUIVER_C_API quiver_error_t quiver_database_in_transaction(quiver_database_t* db, int* out_active);
```

<!-- Reference: the consistent pattern used everywhere else (include/quiver/c/database.h line 45): -->
```c
QUIVER_C_API quiver_error_t quiver_database_is_healthy(quiver_database_t* db, int* out_healthy);
QUIVER_C_API quiver_error_t quiver_database_has_time_series_files(quiver_database_t* db, const char* collection, int* out_result);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change in_transaction from bool* to int* across C API and all bindings</name>
  <files>
    include/quiver/c/database.h,
    src/c/database_transaction.cpp,
    tests/test_c_api_database_transaction.cpp,
    bindings/julia/src/c_api.jl,
    bindings/julia/src/database_transaction.jl,
    bindings/dart/lib/src/ffi/bindings.dart,
    bindings/dart/lib/src/database_transaction.dart,
    bindings/python/src/quiverdb/_declarations.py,
    bindings/python/src/quiverdb/_c_api.py,
    bindings/python/src/quiverdb/database.py,
    bindings/js/src/transaction.ts
  </files>
  <action>
    Change `quiver_database_in_transaction` from `bool* out_active` to `int* out_active` in the C API header and implementation, then propagate to all bindings.

    **C API header** (`include/quiver/c/database.h` line 52):
    - Change `bool* out_active` to `int* out_active`

    **C API implementation** (`src/c/database_transaction.cpp` line 42):
    - Change function signature from `bool* out_active` to `int* out_active`
    - Change the assignment: `*out_active = db->db.in_transaction();` stays the same (implicit bool-to-int conversion is fine, but for explicitness use `*out_active = db->db.in_transaction() ? 1 : 0;`)

    **C API test** (`tests/test_c_api_database_transaction.cpp` around line 149):
    - Change `bool active = true;` to `int active = 1;`
    - Change `EXPECT_FALSE(active)` to `EXPECT_EQ(active, 0)` and `EXPECT_TRUE(active)` to `EXPECT_NE(active, 0)`

    **Julia generated binding** (`bindings/julia/src/c_api.jl` lines 132-133):
    - Change `out_active::Ptr{Bool}` to `out_active::Ptr{Cint}` in the ccall signature

    **Julia wrapper** (`bindings/julia/src/database_transaction.jl` line 17):
    - Change `out_active = Ref{Bool}(false)` to `out_active = Ref{Cint}(0)`
    - Change `return out_active[]` to `return out_active[] != 0` (to still return Julia Bool)

    **Dart generated binding** (`bindings/dart/lib/src/ffi/bindings.dart` lines 253-268):
    - Change `ffi.Pointer<ffi.Bool> out_active` to `ffi.Pointer<ffi.Int32> out_active` in the function signature
    - Change the NativeFunction type from `ffi.Pointer<ffi.Bool>` to `ffi.Pointer<ffi.Int32>` in the lookup
    - Change the asFunction type from `ffi.Pointer<ffi.Bool>` to `ffi.Pointer<ffi.Int32>`

    **Dart wrapper** (`bindings/dart/lib/src/database_transaction.dart` line 28):
    - Change `final outActive = arena<Bool>();` to `final outActive = arena<Int32>();`
    - Change `return outActive.value;` to `return outActive.value != 0;`
    - Add `import 'dart:ffi' as ffi;` if Int32 is not already imported (check existing imports)

    **Python declarations** (`bindings/python/src/quiverdb/_declarations.py` line 110):
    - Change `_Bool* out_active` to `int* out_active`

    **Python C API** (`bindings/python/src/quiverdb/_c_api.py` line 246):
    - Change `_Bool* out_active` to `int* out_active`

    **Python wrapper** (`bindings/python/src/quiverdb/database.py` line 187):
    - Change `out = ffi.new("_Bool*")` to `out = ffi.new("int*")`
    - Change `return bool(out[0])` to `return out[0] != 0` (both work, but be explicit)

    **JS binding** (`bindings/js/src/transaction.ts` line 32):
    - Change `const outActive = new Uint8Array(1);` to `const outActive = new Int32Array(1);`
    - The `outActive[0] !== 0` check stays the same (works for both types)
  </action>
  <verify>
    <automated>cmake --build build --config Debug 2>&1 | tail -5 && ./build/bin/quiver_c_tests.exe --gtest_filter="*Transaction*" 2>&1 | tail -10</automated>
  </verify>
  <done>
    - quiver_database_in_transaction signature uses int* in header and implementation
    - C API tests compile and pass with int variable
    - Julia ccall uses Ptr{Cint}, wrapper returns Bool via != 0
    - Dart binding uses ffi.Pointer of ffi.Int32, wrapper returns bool via != 0
    - Python cdef uses int*, wrapper uses ffi.new("int*")
    - JS uses Int32Array(1) instead of Uint8Array(1)
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete empty dead code file src/blob/dimension.cpp</name>
  <files>src/blob/dimension.cpp</files>
  <action>
    Delete the file `src/blob/dimension.cpp`. It contains only empty namespace declarations and an include -- no actual code. It is not referenced in CMakeLists.txt (confirmed: grep for "dimension.cpp" in CMakeLists.txt returns no matches).

    Verify after deletion:
    - The file `include/quiver/blob/dimension.h` (the header) must NOT be deleted -- it is a legitimate header used by other files
    - The build still succeeds since the .cpp was never compiled
  </action>
  <verify>
    <automated>test ! -f src/blob/dimension.cpp && cmake --build build --config Debug 2>&1 | tail -3</automated>
  </verify>
  <done>
    - src/blob/dimension.cpp no longer exists
    - include/quiver/blob/dimension.h still exists (header is real code)
    - Build succeeds without the dead file
  </done>
</task>

</tasks>

<verification>
Run the full test suite to confirm no regressions:

```bash
# C++ core tests (in_transaction is tested here too)
./build/bin/quiver_tests.exe --gtest_filter="*Transaction*"

# C API tests
./build/bin/quiver_c_tests.exe --gtest_filter="*Transaction*"

# JS binding tests
bindings/js/test/test.bat

# Julia binding tests
bindings/julia/test/test.bat

# Dart binding tests
bindings/dart/test/test.bat

# Python binding tests
bindings/python/test/test.bat
```

All transaction tests across all bindings must pass. The C++ core tests should be unaffected (C++ Database::in_transaction returns bool -- only the C API wrapper changes).
</verification>

<success_criteria>
1. `quiver_database_in_transaction` uses `int* out_active` in both header and implementation
2. All C API tests pass (specifically Transaction suite)
3. JS binding `inTransaction()` uses `Int32Array(1)` and JS transaction tests pass
4. Julia, Dart, Python bindings updated and their transaction tests pass
5. `src/blob/dimension.cpp` is deleted
6. Full build succeeds in Debug mode
</success_criteria>

<output>
After completion, create `.planning/phases/01-fixes-cleanup/01-01-SUMMARY.md`
</output>
