---
phase: 01-dart-metadata-types
task: 0
total_tasks: 0
status: planning
last_updated: 2026-03-01T18:35:58.366Z
---

<current_state>
Phase 1 directory created. Roadmap, requirements, and state files are committed. We were about to spawn the gsd-planner agent to create PLAN.md files for this phase. No research needed — the codebase was thoroughly analyzed in the previous session.
</current_state>

<completed_work>

- Milestone v0.5 initialized with PROJECT.md, STATE.md, MILESTONES.md
- REQUIREMENTS.md created with 8 DART requirements + 5 JUL requirements
- ROADMAP.md created with 2 phases
- Phase 1 directory created: .planning/phases/01-dart-metadata-types/
- All planning docs committed (2 commits on rs/more12)
</completed_work>

<remaining_work>

- Create PLAN.md file(s) for Phase 1 via gsd-planner (was about to spawn)
- Verify plans via gsd-plan-checker
- Execute Phase 1: implement Dart ScalarMetadata and GroupMetadata classes
- Execute Phase 2: Julia cleanup (dead code + helper_maps removal)
</remaining_work>

<decisions_made>

- Dart will get proper ScalarMetadata and GroupMetadata classes in a new metadata.dart file, matching Julia's struct pattern and Python's frozen dataclass pattern
- The inline record types repeated ~15 times across database.dart, database_metadata.dart, and database_read.dart will all be replaced
- Julia's quiver_database_sqlite_error (exceptions.jl:5) is dead code — delete it
- Julia's helper_maps.jl (scalar_relation_map, set_relation_map) breaks homogeneity — remove from module, delete file and tests
- No research needed — all code was read and analyzed thoroughly
</decisions_made>

<blockers>
- None
</blockers>

<context>
The key Dart files to modify are:
1. NEW: bindings/dart/lib/src/metadata.dart — define ScalarMetadata and GroupMetadata classes
2. bindings/dart/lib/src/database.dart — replace _parseScalarMetadata/_parseGroupMetadata return types with the new classes
3. bindings/dart/lib/src/database_metadata.dart — replace all inline record types in method signatures
4. bindings/dart/lib/src/database_read.dart — replace inline record types in readScalarsById, readVectorsById, readSetsById and the group reader methods
5. bindings/dart/lib/quiverdb.dart — add export for metadata.dart

The ScalarMetadata class should have fields: name (String), dataType (int), notNull (bool), primaryKey (bool), defaultValue (String?), isForeignKey (bool), referencesCollection (String?), referencesColumn (String?)

The GroupMetadata class should have fields: groupName (String), dimensionColumn (String), valueColumns (List<ScalarMetadata>)

Phase requirement IDs: DART-01, DART-02, DART-03, DART-04, DART-05, DART-06, DART-07, DART-08
</context>

<next_action>
Resume with: /gsd:plan-phase 1 --skip-research
This will spawn the planner to create PLAN.md files. Research is not needed — all codebase analysis is captured in this handoff.
</next_action>
