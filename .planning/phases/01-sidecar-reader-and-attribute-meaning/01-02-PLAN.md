---
phase: 01-sidecar-reader-and-attribute-meaning
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - tests/test_database_ui_metadata.cpp
  - src/ui_config.cpp
  - src/database_describe.cpp
  - CHANGELOG.md
  - src/CLAUDE.md
  - tests/CLAUDE.md
autonomous: true
requirements: [READ-01, READ-02, READ-04, RENDER-03, SAFE-01, SAFE-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A migrations path with a trailing separator, and a relative migrations path, resolve to the same `ui/` directory as the absolute separator-free form — never `<migrations>/ui`, never a `ui/` under the process CWD (READ-01, success criterion 5)."
    - "`main.toml`, `enum.toml`, a `themes/` subdirectory and a non-`.toml` file are never mistaken for a collection file, and no filename is ever translated into a table name (READ-02)."
    - "A scalar the sidecar does not describe renders the line it renders today in all three corpus cases: its collection has no ui file, its own `[[attribute]]` entry is absent, and a ui entry names a column the schema does not have (RENDER-03, success criterion 3)."
    - "`from_migrations` against a tree with no `ui/`, an empty `ui/`, a zero-byte `enum.toml`, or an unparseable `.toml` still opens the database, logs a warning for the malformed case only, and all three reports come back byte-identical to a run of the same tree with the sidecar deleted (SAFE-01, SAFE-02, success criterion 4)."
    - "One malformed `ui/*.toml` costs only that collection its metadata; every other collection's clauses still render (D-09)."
    - "An attribute carrying `hide = true` still renders its clauses — `describe` describes the schema, not the UI (D-20)."
    - "Fixtures build `migrations/` and `ui/` in a per-test temp dir; nothing is committed under `tests/schemas/ui/` (success criterion 6, D-12)."
    - "The existing `tests/test_database_lifecycle.cpp` describe assertions at `:396-436`, `:438-460`, `:463-485` and `:487-500` pass **unmodified** — a diff touching them is a failure, not a fix (success criterion 6)."
    - "`CHANGELOG.md`'s `## [0.10.7]` heading carries its release date `2026-09-17` and its compare link ends `v0.10.6...v0.10.7`; a new `## [0.10.8] — unreleased` section sits above it with a `v0.10.7...HEAD` compare link at the top of the newest-first link block (D-15)."
    - statement: "The malformed-sidecar warning reaches the logger rather than being swallowed; confirmed by the absence of a thrown exception plus the presence of the degraded (sidecar-free) report, since the C++ suite has no log-capture harness to assert the warning text directly."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT edit, re-anchor or relax any assertion in `tests/test_database_lifecycle.cpp` to make this phase pass. The un-anchored `Vectors:` / `Sets:` / `Time Series:` substring searches there are a known latent defect that this phase deliberately does not fix; a phase whose success criterion is that they pass unmodified cannot also be the phase that modifies them."
      category: transparency
      verification: judgment
    - statement: "MUST NOT commit any fixture file under `tests/schemas/ui/`. Such a directory becomes a live sibling of `tests/schemas/migrations` for every `from_migrations` call across six suites plus the recursive-copy Lua migrations test, silently changing tests this phase never touched."
      category: safety
      verification: judgment
  artifacts:
    - path: "tests/test_database_ui_metadata.cpp"
      provides: "Path-resolution, shape-selection, undescribed, no-sidecar and malformed-sidecar coverage"
      contains: "PathResolution"
    - path: "CHANGELOG.md"
      provides: "Dated 0.10.7 section, corrected compare link, new 0.10.8 unreleased section with this milestone's entry"
      contains: "## [0.10.8]"
    - path: "src/CLAUDE.md"
      provides: "Documentation of the ui/ sidecar reader, its degrade-never-throw posture and its confinement"
    - path: "tests/CLAUDE.md"
      provides: "Documentation of the new suite and its temp-dir-only fixture rule"
  key_links:
    - from: "tests/test_database_ui_metadata.cpp"
      to: "src/ui_config.cpp"
      via: "public-API-only exercise of the loader through Database::from_migrations + describe_collection"
      pattern: "from_migrations"
---

<objective>
Prove the degrade-never-throw contract and the three undescribed cases, close the path-resolution and
shape-selection edges, and land the phase's paperwork: the `CHANGELOG.md` reconciliation D-15
specifies and the CLAUDE.md updates the root self-updating rule requires.

Purpose: SAFE-01 and SAFE-02 are the requirements that make this feature safe to ship against real
PSR studies, and they are exactly the ones a happy-path implementation leaves untested.
Output: the remaining test coverage, a reconciled changelog, and updated `src/CLAUDE.md` and
`tests/CLAUDE.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-sidecar-reader-and-attribute-meaning/01-CONTEXT.md
@.planning/phases/01-sidecar-reader-and-attribute-meaning/01-RESEARCH.md
@.planning/phases/01-sidecar-reader-and-attribute-meaning/01-VALIDATION.md
@.planning/phases/01-sidecar-reader-and-attribute-meaning/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1-02-01: Path-resolution, shape-selection, undescribed and degradation coverage</name>
  <reversibility rating="reversible">Test-only additions plus any localized fix they expose; each is a single-function change behind the interfaces plan 01 already fixed.</reversibility>
  <files>tests/test_database_ui_metadata.cpp, src/ui_config.cpp, src/database_describe.cpp</files>
  <read_first>
    - tests/test_database_ui_metadata.cpp (the fixture and every test plan 00 and plan 01 wrote)
    - src/ui_config.cpp (the resolution, shape gate, inner/outer catch structure under test)
    - src/database_describe.cpp (`write_ui_clauses` and the scalar loop)
    - tests/test_database_lifecycle.cpp (lines 383-500 — the four assertions that must pass unmodified; read them, do not edit them)
    - tests/test_c_api_database_lifecycle.cpp (lines 225-245 — existing trailing-separator migrations paths, the real-world case READ-01 exists for)
    - .planning/phases/01-sidecar-reader-and-attribute-meaning/01-VALIDATION.md (the per-task verification map whose gtest filters these test names must satisfy)
  </read_first>
  <behavior>
    - A migrations path passed with a trailing separator finds the same `ui/` and renders the same clauses as the same path without one.
    - A relative migrations path (the process CWD set to the temp root, the path given as `migrations`) finds the sibling `ui/`, not a `ui/` under the CWD, and renders the same clauses.
    - A `ui/` holding `main.toml` (flat keys, no top-level `id`), a theme-shaped file, a `themes/` subdirectory, and a `notes.txt` contributes nothing, and none of them throws.
    - A collection with no ui file at all renders every scalar exactly as a no-sidecar run does.
    - An attribute with no `[[attribute]]` entry in its collection's file renders exactly as a no-sidecar run does, while its siblings in the same file render their clauses.
    - A ui entry naming a column the schema does not have changes nothing and warns nothing.
    - An attribute carrying `hide = true` still renders its clauses.
    - A tree with an empty `ui/` directory, a tree with a zero-byte `enum.toml`, and a tree with a syntactically invalid `.toml` each open successfully and produce three reports byte-identical to the same tree with `ui/` removed.
    - With two collection files where one is unparseable, the other collection still renders its clauses.
  </behavior>
  <action>
Add the remaining coverage to `tests/test_database_ui_metadata.cpp`, using plan 00's fixture. Every
test drives the loader through the public API only — `Database::from_migrations` plus `describe`,
`describe_collection` and `summarize_collection` — exactly as `tests/test_binary_metadata.cpp` does,
because the parser is linked PRIVATE on `quiver` and is unreachable from the test target. Do not add
a parser include to the test file.

Name the tests so the validation map's existing gtest filters select them:

  - `UiConfigTest.PathResolutionTrailingSeparator` — build the tree once, open it with
    `migrations_dir()` and again with `migrations_dir() + "/"`, assert both `describe_collection`
    outputs are equal and both contain the label clause.
  - `UiConfigTest.PathResolutionRelativeMigrationsPath` — save `fs::current_path()`, set it to the
    temp root, open with the bare relative string `migrations`, assert the same output, and restore
    the CWD in a scope guard so a failing assertion cannot leak the change into the next test.
  - `UiConfigTest.PathResolutionNeverReadsUiUnderMigrations` — additionally plant a decoy
    `migrations/ui/hydro_plant.toml` whose label differs, and assert the rendered label is the
    sibling's, never the decoy's. This is the exact failure raw `parent_path()` produces.
  - `UiConfigTest.ShapeSelectionIgnoresNonCollectionFiles` — write `main.toml` with flat
    `model = "..."` and `collections = [...]` keys and no top-level `id`, a theme-shaped file with an
    `id` but no `attribute` array, a `notes.txt`, and a `themes/` subdirectory holding a file that
    would otherwise qualify; assert the report is identical to the no-sidecar run and that
    `from_migrations` did not throw.
  - `UiConfigTest.ShapeSelectionUsesFileIdNotFilename` — name the file `dc_line.toml` while its
    top-level `id` is the schema's actual collection name; assert the clauses render, proving no
    filename-to-table-name translation is involved.
  - `DatabaseUiMetadataTest.UndescribedCollectionRendersUnchanged`,
    `DatabaseUiMetadataTest.UndescribedAttributeRendersUnchanged`,
    `DatabaseUiMetadataTest.UndescribedDanglingUiColumnRendersUnchanged` — the three corpus cases of
    success criterion 3. Each compares the affected line against the same line from a second
    `from_migrations` run on a copy of the tree with `ui/` removed, so "renders exactly as today" is
    asserted against the binary's own output rather than a hand-typed expectation.
  - `DatabaseUiMetadataTest.HiddenAttributeStillRenders` — an `[[attribute]]` entry carrying
    `hide = true` alongside its label; assert the label clause is present (D-20: `describe` describes
    the schema, not the UI; 360 of 736 corpus attributes carry the flag).
  - `DatabaseUiMetadataTest.MalformedUiReportsIdenticalToNoUiDir` — parameterize over four broken
    trees: an empty `ui/` directory, a zero-byte `enum.toml`, a `.toml` with invalid syntax, and a
    collection file whose `attribute` value is a string rather than an array. For each: assert
    `from_migrations` does not throw, then assert all three reports equal the reports from the same
    tree with `ui/` removed. Byte equality of the three reports is the criterion-4 assertion.
  - `UiConfigTest.MalformedOneFileKeepsOtherCollections` — two collection files, one unparseable;
    assert the good collection's clauses still render. This is what D-09's inner per-file catch buys,
    and it is the only test that distinguishes the nested posture from a single outer catch.

Build the "same tree with the sidecar removed" comparison by copying the whole temp tree to a second
directory and `fs::remove_all`-ing its `ui/`, then opening a fresh database file in it — never by
mutating the tree under an already-open handle.

If any of these fails, fix `src/ui_config.cpp` or `src/database_describe.cpp` rather than relaxing
the assertion. Do not touch `tests/test_database_lifecycle.cpp` under any circumstance: success
criterion 6 requires those four assertions to pass unmodified, and their un-anchored substring
searches are a recorded latent defect this phase deliberately leaves alone.
  </action>
  <acceptance_criteria>
    - `./build/bin/quiver_tests.exe --gtest_filter=*UiConfig*PathResolution*` reports 0 failures and at least 3 tests.
    - `./build/bin/quiver_tests.exe --gtest_filter=*UiConfig*ShapeSelection*` reports 0 failures and at least 2 tests.
    - `./build/bin/quiver_tests.exe --gtest_filter=*DatabaseUiMetadata*Undescribed*` reports 0 failures and at least 3 tests.
    - `./build/bin/quiver_tests.exe --gtest_filter=*DatabaseUiMetadata*Malformed*:*UiConfig*Malformed*` reports 0 failures and at least 2 tests.
    - `./build/bin/quiver_tests.exe --gtest_filter=*DatabaseUiMetadata*NoUiDir*` still passes (plan 00's SAFE-01 baseline is unbroken).
    - `./build/bin/quiver_tests.exe --gtest_filter=*DescribeVectorsHeaderPrintedOnce*:*DescribeSetsHeaderPrintedOnce*:*DescribeTimeSeriesWithDimensionColumn*:*DescribeNoCategoryHeaderWhenEmpty*` reports 4 tests, 0 failures, and `git diff --stat tests/test_database_lifecycle.cpp` is empty.
    - `test ! -d tests/schemas/ui` succeeds, and `git status --porcelain tests/schemas/` is empty.
    - The test file contains no parser include: `! grep -qiE '#include.*toml' tests/test_database_ui_metadata.cpp` succeeds.
    - `./build/bin/quiver_tests.exe` and `./build/bin/quiver_c_tests.exe` both report 0 failures.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build --config Debug && ./build/bin/quiver_tests.exe --gtest_filter=*UiConfig*:*DatabaseUiMetadata*</automated>
  </verify>
  <done>Trailing-separator, relative and decoy-`ui/` path cases all resolve to the sibling directory; non-collection files are never selected; all three undescribed cases and all four malformed-sidecar cases render byte-identically to a sidecar-free run of the same tree; one broken file costs only its own collection; and `tests/test_database_lifecycle.cpp` is unmodified.</done>
</task>

<task type="auto">
  <name>Task 1-02-02: Reconcile CHANGELOG.md and update the nearest CLAUDE.md files</name>
  <reversibility rating="reversible">Documentation edits; undoing them is a revert of a text diff with no code or contract implication.</reversibility>
  <files>CHANGELOG.md, src/CLAUDE.md, tests/CLAUDE.md</files>
  <read_first>
    - CHANGELOG.md (lines 1-30 for the current head and the `## [0.10.7] — unreleased` heading; line 111 for the `## [0.10.6] — 2026-09-11` dated-heading format; lines 530-545 for the newest-first compare-link block)
    - CLAUDE.md (root — the Changelog rule, the Versioning section, the Self-Updating rule)
    - src/CLAUDE.md (where an internal `src/`-local component is documented; find how `csv_read` is covered and match that depth)
    - tests/CLAUDE.md (the suite breakdown this new suite must be added to)
    - .planning/phases/01-sidecar-reader-and-attribute-meaning/01-CONTEXT.md (D-15's three exact edits)
  </read_first>
  <action>
**CHANGELOG.md — three edits, per D-15.** The file is missing a section, not misnumbered: tag
`v0.10.7` exists at `7bd1f16`, which is the very commit that wrote the entries under that heading,
and no commit has touched the file since. Renaming the heading would relabel already-released work.
So:

  1. Change `## [0.10.7] — unreleased` to `## [0.10.7] — 2026-09-17`. Use the em dash with a space
     either side, matching the `## [0.10.6] — 2026-09-11` heading. `v0.10.7` is a lightweight tag, so
     its date is its commit date.
  2. Change the `[0.10.7]` link target's tail from `v0.10.6...HEAD` to `v0.10.6...v0.10.7`, matching
     the released-link form `[0.10.6]` already uses.
  3. Insert a new `## [0.10.8] — unreleased` section above the 0.10.7 section, and a
     `[0.10.8]: https://github.com/psrenergy/quiver/compare/v0.10.7...HEAD` line at the top of the
     newest-first link block.

Then write this milestone's entry under the new `## [0.10.8] — unreleased` heading, in an `### Added`
subsection, covering what a caller observes: `describe` and `describe_collection` on a database
opened with `from_migrations` now append an attribute's English label, its enum code-to-label list,
and (in `describe_collection` only) its tooltip, read from the `ui/` TOML sidecar that sits beside
the migrations directory. State the clause grammar with one worked line so a reader can recognize the
output; state that a database with no `ui/`, or with a broken one, reads exactly as before and never
fails to open; state that the feature reaches every binding and Lua for free because the reports are
already plain strings; and state what is deliberately not included — no C API symbol, no structured
getter, no validation of the sidecar against the schema, English only. This is not a **BREAKING**
entry: nothing a caller does changes, and the version bump to 0.10.8 has already happened in
`CMakeLists.txt`.

**src/CLAUDE.md.** Add a section for the UI sidecar reader at the same depth `csv_read` is covered.
Record: the component is `src/ui_config.{h,cpp}` with no public header, no C API symbol and no FFI
binding, and the TOML parser is confined to the `.cpp` because it is linked PRIVATE on `quiver`; the
load happens once in `from_migrations` after `migrate_up`, deliberately **not** hooked onto
`load_schema_metadata`, because `migrate_up` early-returns before reaching it on the
already-up-to-date open path every re-open of an existing study takes; the path is
`weakly_canonical(migrations_path).parent_path() / "ui"` and raw `parent_path()` was compiled and
proven wrong in two ways; collection files self-select by shape rather than by filename; `enum.toml`
has no wrapper key, so its top-level keys are themselves the vocabulary names and the parser
discovers them by iterating the table; the whole load is a nested try/catch that warns and degrades
to an empty map and never throws, explicitly not `binary/binary_metadata.cpp`'s posture; `squash` is
spelled with an explicit ASCII range test because the standard lowercase function is undefined
behavior on a negative `char` and 344 corpus strings are non-ASCII, with the consequence that it
drops non-ASCII bytes and therefore biases toward printing a label — deliberate, not to be
"corrected"; and `normalize_ui_text` maps every C0 control byte and `0x7F` to a space, a superset of
the newline collapse, so a sidecar string cannot emit an ANSI escape into a terminal rendering the
report. Also record that `summarize_collection`'s enum annotation is Phase 2 and that this phase does
not touch it.

**tests/CLAUDE.md.** Add `test_database_ui_metadata.cpp` to the suite breakdown, noting it is the one
suite that drives `from_migrations` describe output, that its two gtest fixture names
(`UiConfigTest`, `DatabaseUiMetadataTest`) exist so the loader-facing and render-facing halves can be
filtered separately, and that its fixtures are per-test temp directories only — nothing may be
committed under `tests/schemas/ui/`, because such a directory would become a live sibling of
`tests/schemas/migrations` for every `from_migrations` call across six suites plus the
recursive-copy Lua migrations test.

Do not touch the root `CLAUDE.md`: this phase changes no cross-cutting rule, adds no public API
method, and alters no cross-layer naming convention, so its tables stay as they are.
  </action>
  <acceptance_criteria>
    - `grep -c '^## \[0.10.8\] — unreleased' CHANGELOG.md` is 1, and it appears before the `## [0.10.7]` heading (`awk '/^## \[0.10.8\]/{a=NR} /^## \[0.10.7\]/{b=NR} END{exit !(a && b && a < b)}' CHANGELOG.md` succeeds).
    - `grep -c '^## \[0.10.7\] — 2026-09-17' CHANGELOG.md` is 1, and `grep -c '^## \[0.10.7\] — unreleased' CHANGELOG.md` is 0.
    - `grep -c 'compare/v0.10.6\.\.\.v0.10.7' CHANGELOG.md` is 1, and `grep -c 'compare/v0.10.6\.\.\.HEAD' CHANGELOG.md` is 0.
    - `grep -c 'compare/v0.10.7\.\.\.HEAD' CHANGELOG.md` is 1, and that line is the first entry of the link block (`grep -n '^\[0\.10\.' CHANGELOG.md | head -1` names `[0.10.8]`).
    - The 0.10.8 section is non-empty: at least one `### Added` subsection with prose describing the sidecar feature.
    - `grep -qi 'ui_config' src/CLAUDE.md` succeeds and `grep -qi 'test_database_ui_metadata' tests/CLAUDE.md` succeeds.
    - `git diff --stat CLAUDE.md` is empty (the root file is deliberately untouched).
    - `python -c` is unavailable in this environment; run the version check as `uv run python scripts/assert_version.py` and confirm it reports agreement at 0.10.8 across all five manifests.
  </acceptance_criteria>
  <verify>
    <automated>grep -c '^## \[0.10.8\] — unreleased' CHANGELOG.md &amp;&amp; grep -c '^## \[0.10.7\] — 2026-09-17' CHANGELOG.md &amp;&amp; grep -c 'compare/v0.10.7\.\.\.HEAD' CHANGELOG.md &amp;&amp; grep -qi 'ui_config' src/CLAUDE.md &amp;&amp; grep -qi 'test_database_ui_metadata' tests/CLAUDE.md &amp;&amp; uv run python scripts/assert_version.py</automated>
  </verify>
  <done>`CHANGELOG.md` carries a dated 0.10.7 section with a released compare link, a new 0.10.8 unreleased section holding this milestone's entry, and a `v0.10.7...HEAD` link at the top of the link block; `src/CLAUDE.md` and `tests/CLAUDE.md` document the sidecar reader and its suite; the root `CLAUDE.md` is untouched.</done>
</task>

</tasks>

## Artifacts this phase produces (contributed by this plan)

| Artifact | Kind | Path |
|---|---|---|
| `PathResolution*`, `ShapeSelection*`, `Malformed*` tests | gtest cases in `UiConfigTest` | `tests/test_database_ui_metadata.cpp` |
| `Undescribed*`, `HiddenAttributeStillRenders`, `MalformedUiReportsIdenticalToNoUiDir` | gtest cases in `DatabaseUiMetadataTest` | `tests/test_database_ui_metadata.cpp` |
| `## [0.10.8] — unreleased` section + entry; dated `## [0.10.7] — 2026-09-17`; corrected `[0.10.7]` and new `[0.10.8]` compare links | changelog edits | `CHANGELOG.md` |
| UI-sidecar reader section | docs | `src/CLAUDE.md` |
| `test_database_ui_metadata.cpp` suite entry + temp-dir-only fixture rule | docs | `tests/CLAUDE.md` |

The full phase-wide artifact table, including every symbol plans 00 and 01 create, lives in
`01-01-PLAN.md` → "Artifacts this phase produces".

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| malformed `ui/*.toml` → `from_migrations` | The degrade-never-throw contract is the availability boundary this plan's tests exercise directly. |
| test process → process-wide current working directory | `PathResolutionRelativeMigrationsPath` mutates global process state that every other test in the binary shares. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Denial of Service | `load_ui_config` degrade-never-throw contract | high | mitigate | `DatabaseUiMetadataTest.MalformedUiReportsIdenticalToNoUiDir` parameterizes over empty directory, zero-byte `enum.toml`, invalid syntax, and a wrong-typed `attribute` key, asserting no throw plus byte-identical reports; `UiConfigTest.MalformedOneFileKeepsOtherCollections` proves the inner per-file catch confines the damage. This is the executable proof of T-01-01's mitigation, which plan 01 only implements. |
| T-01-02 | Tampering | `ui_dir` resolution | high | mitigate | `PathResolutionNeverReadsUiUnderMigrations` plants a decoy `migrations/ui/` and asserts the sibling wins — the exact misresolution raw `parent_path()` produces; `PathResolutionRelativeMigrationsPath` asserts the CWD is never the base. |
| T-01-07 | Tampering | process-wide `fs::current_path` mutation inside a gtest binary | medium | mitigate | The relative-path test saves and restores the CWD through a scope guard whose destructor runs on the assertion-failure path too, so a red test cannot leave the working directory changed for every subsequent test in `quiver_tests`. |
| T-01-08 | Tampering | a committed `tests/schemas/ui/` becoming a live sidecar for six other suites | high | mitigate | Every fixture is written into a per-test temp directory and removed in both `SetUp` and `TearDown`; the acceptance criteria assert `test ! -d tests/schemas/ui` and an empty `git status --porcelain tests/schemas/`, and the rule is restated as a prohibition in this plan's `must_haves`. |
| T-01-SC | Tampering | npm/pip/cargo installs | — | N/A | No packages are installed by this plan. `uv run python scripts/assert_version.py` executes an in-repo script with no new dependency. |

All `high`-severity rows are dispositioned `mitigate` (ASVS L1, block on `high`).
</threat_model>

<verification>
1. `cmake --build build --config Debug` — clean build.
2. `./build/bin/quiver_tests.exe --gtest_filter=*UiConfig*:*DatabaseUiMetadata*` — every phase test, 0 failures.
3. `./build/bin/quiver_tests.exe` — full suite green.
4. `./build/bin/quiver_c_tests.exe` — full C API suite green.
5. `git diff --stat tests/test_database_lifecycle.cpp` — empty (success criterion 6).
6. `test ! -d tests/schemas/ui` and `git status --porcelain tests/schemas/` empty.
7. `uv run python scripts/assert_version.py` — all five manifests agree at 0.10.8.
8. `scripts/format.bat` — clang-format clean on the touched sources.
</verification>

<success_criteria>
All four malformed-sidecar cases and all three undescribed cases render byte-identically to a
sidecar-free run of the same tree; trailing-separator, relative and decoy-`ui/` paths all resolve to
the sibling directory; one broken file costs only its own collection; a hidden attribute still
renders; nothing is committed under `tests/schemas/ui/`; the four pre-existing describe assertions
pass unmodified; and `CHANGELOG.md`, `src/CLAUDE.md` and `tests/CLAUDE.md` record the change.
</success_criteria>

<output>
Create `.planning/phases/01-sidecar-reader-and-attribute-meaning/01-02-SUMMARY.md` when done.
</output>