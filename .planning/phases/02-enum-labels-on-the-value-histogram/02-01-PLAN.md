---
phase: 02-enum-labels-on-the-value-histogram
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/database_describe.cpp
  - tests/test_database_ui_metadata.cpp
  - CHANGELOG.md
  - CLAUDE.md
  - src/CLAUDE.md
autonomous: true
requirements: [RENDER-02]

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D2-01/D2-02: summarize_collection on a from_migrations database renders each observed histogram code as `code SP \"Label\": count` — exactly one U+0020 between the code and the opening quote, nothing between the closing quote and the `:`. Worked target: `values {0 \"Per Unit\": 2, 1: 1}` (RENDER-02 SC-1)."
    - "D2-05: an observed code the vocabulary does not cover renders bare (`1: 1`), and a collection with no sidecar, an empty vocabulary or a null UiAttribute* renders the whole clause byte-identically to today (RENDER-02 SC-2). This is the `empty` probe edge, explicit."
    - "D2-07: a vocabulary code with zero observed rows never appears in `values {}` — summarize reports the table, not the vocabulary."
    - "D2-04: entry order is the existing `ORDER BY <col>` ascending code; no sort, no comparator and no ordering container is added. Codes are unique per entry so no tie exists. This is the `ordering` probe edge, explicit."
    - "D2-03/D2-08: label text passes through normalize_ui_text then quote_ui_text verbatim — C0 bytes and 0x7F to space, UTF-8 byte-for-byte untranscoded, only `\\` and `\"` escaped. `squash` is NOT applied here: D-04's restates-the-name suppression has no attribute name at entry level. This is the `encoding` probe edge, explicit."
    - "D2-12: the `impl_->ui_metadata.find(collection, scalar.name)` lookup sits inside the bounded-cardinality branch, immediately before the `out << \"; values {\"` emission — never at the top of the per-scalar loop, so a collection of TEXT/REAL/PK scalars pays zero two-level map lookups."
    - "D2-11/D2-13: summarize_collection's scalar lines gain no `; label` and no `; tooltip` clause, and kMaxDistributionCardinality stays 64 (a >64-distinct-code column still renders no clause at all — recorded known ceiling, describe_collection's `; enum {...}` is the fallback)."
    - "D2-09/D2-10 are recorded as honesty records, not as evidence: SC-3's four test_database_lifecycle.cpp assertions are from_schema(\":memory:\") + describe() and cannot be reached by a summarize-only diff; and quote_ui_text gives a parse-level guarantee, not substring immunity — a corpus label spelled `Sets: 3` renders verbatim inside its quotes, unchanged from Phase 1's accepted D-02 posture."
    - "D2-14: CHANGELOG.md's 0.10.8 unreleased entry no longer claims summarize omits this metadata, the root CLAUDE.md summarize_collection bullet names the annotation, and src/CLAUDE.md is current (Self-Updating rule) including D2-06 recorded as project decision D-09 and the two known limits (the from_migrations-only load, the >64-code ceiling)."
    - statement: "D2-06 (project decision D-09): an enum label that normalizes to empty drops the ANNOTATION and keeps the ENTRY — the deliberate divergence from D-06, which drops the whole entry in the `enum {}` clause. Guaranteed by the `if (!text.empty())` guard around the annotation only; no authored assertion, since CONTEXT.md's <specifics> fixes this phase's test at one EXPECT_TRUE plus one EXPECT_FALSE."
      verification: backstop
    - statement: "D2-08 adjacency: a label whose text is the code's own digits renders as `1 \"1\": 5` — quoted, parseable, never suppressed for redundancy. No authored assertion; the quoting is what keeps it unambiguous."
      verification: backstop
  artifacts:
    - "src/database_describe.cpp — the `; values {` emission loop (currently lines 270-277) carrying the per-code annotation"
    - "tests/test_database_ui_metadata.cpp — TEST_F(DatabaseUiMetadataTest, SummarizeHistogramAnnotatesCodesWithEnumLabels), the file's first test that calls create_element"
    - "CHANGELOG.md, CLAUDE.md, src/CLAUDE.md — D2-14 documentation edits"
  key_links:
    - "impl_->ui_metadata (src/database_impl.h:80, populated only by from_migrations at src/database.cpp:258) -> UiMetadata::find(collection, attribute) -> UiAttribute::enum_labels (std::map<int64_t, std::string>) keyed by the same int64_t query_int_rows yields (src/database_impl.h:27) — exact key type, no conversion"
    - "normalize_ui_text -> quote_ui_text, both already in src/database_describe.cpp's anonymous namespace; the annotation is the only new caller pair"
  prohibitions:
    - "expect_reports_match (tests/test_database_ui_metadata.cpp:174-179) and its four existing callers stay unmodified; no valid-sidecar caller is added to it."
    - "tests/test_database_lifecycle.cpp stays byte-for-byte unmodified."
    - "The renderer adds no substring blocklist, no stripping of `,` `:` `{` `}` from labels, and no second escaping rule — normalize_ui_text and quote_ui_text are reused verbatim."
    - "No placeholder annotation is emitted for an uncovered code or an empty-normalizing label — `0 \"?\": 7`, `0 \"<unknown>\": 7` and `0 \"\": 7` are all forbidden, and neither case drops the entry."
    - "No vocabulary code with zero observed rows appears in `values {}`."
    - "No `; label` and no `; tooltip` clause is added to summarize_collection's scalar lines."
    - "No new source file, no new header, no signature change, no CMake edit, no C API symbol and no binding change; kMaxDistributionCardinality stays 64."
    - "The ui_metadata load is not hooked onto load_schema_metadata or require_schema to close the Database::open gap — migrate_up early-returns before it on the open-an-existing-study path."
---

<objective>
Annotate `summarize_collection`'s integer value distribution with each observed code's English enum
label, from the `UiMetadata` map Phase 1 already builds in `from_migrations` — RENDER-02, the
milestone's headline output.

The entire production change is one emission loop in `src/database_describe.cpp` (D2-12 fixes where
the lookup goes). No new file, no new header, no signature change, no CMake edit, no C API change,
no binding change.

Purpose: an agent inspecting a PSR study reads `values {0 "User Defined Forecast": 7, 1 "Model": 5}`
instead of `values {0: 7, 1: 5}`.
Output: the annotated emission loop, one gtest case pinning D2-01/D2-02/D2-05/D2-07, and D2-14's
three documentation edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-enum-labels-on-the-value-histogram/02-CONTEXT.md
@.planning/phases/02-enum-labels-on-the-value-histogram/02-VALIDATION.md
@CLAUDE.md
@src/CLAUDE.md
@tests/CLAUDE.md
</context>

## Artifacts this phase produces

Almost everything here is *modified*, not created. Named explicitly so verification has targets:

| Artifact | Kind | Where |
|----------|------|-------|
| The `; values {` emission loop | modified block | `src/database_describe.cpp`, inside `Database::summarize_collection`, currently lines 270-277 |
| `DatabaseUiMetadataTest.SummarizeHistogramAnnotatesCodesWithEnumLabels` | new test | `tests/test_database_ui_metadata.cpp` (appended; the file's first `create_element` caller) |
| `0.10.8 — unreleased` entry | modified prose | `CHANGELOG.md` (the sentence at line 26 that scopes summarize out) |
| Core API `summarize_collection` bullet | modified prose | root `CLAUDE.md` line 576 |
| `ui_metadata.h`/`ui_metadata.cpp` section | modified prose | `src/CLAUDE.md` (the sentence at line 177 that scopes summarize out), plus decision **D-09** and the two known limits |

Nothing is created. No new symbol, no new file, no new gtest suite name — the existing
`DatabaseUiMetadataTest` fixture hosts the test.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 2-01-01: Annotate the histogram entry with its enum label</name>

  <read_first>
    - `src/database_describe.cpp` — lines 55-115 for `normalize_ui_text` / `quote_ui_text` (the two
      anonymous-namespace helpers this task reuses), lines 116-164 for `write_ui_clauses` (the
      grammar and the D-06 empty-label precedent this task deliberately diverges from), and lines
      237-302 for `Database::summarize_collection` and its `; values {` loop.
    - `tests/test_database_ui_metadata.cpp` — lines 1-181 for `UiTempTreeFixture`,
      `reservoir_schema()`, `write_ui_file`, `open_tree()` and `expect_reports_match`; lines 487-511
      (`UiMetadataTest.EnumGappedCodesRenderVerbatim`) for the exact `enum.toml` + collection-file
      fixture idiom this test copies.
    - `src/ui_metadata.h` — `UiAttribute::enum_labels` and `UiMetadata::find`.
    - `src/database_impl.h` lines 22-45 (`query_int_rows` row type) and line 80 (`ui_metadata`).
    - `tests/test_database_describe.cpp` lines 60-88 (`SummarizeScalarsAndGroups`) for the
      `create_element` + `summarize_collection` assertion idiom.
  </read_first>

  <files>tests/test_database_ui_metadata.cpp, src/database_describe.cpp</files>

  <behavior>
    New `TEST_F(DatabaseUiMetadataTest, SummarizeHistogramAnnotatesCodesWithEnumLabels)`, appended
    to `tests/test_database_ui_metadata.cpp`. Exactly the coverage CONTEXT.md `<specifics>` fixes —
    one `EXPECT_TRUE`, one `EXPECT_FALSE`, no more:

    - Fixture: `write_migration(1, reservoir_schema(), "DROP TABLE HydroPlant; DROP TABLE Configuration;")`.
      `initial_volume_type` is the non-primary-key INTEGER column (`NOT NULL`, so every created
      element must set it).
    - `enum.toml` declares `[[initial_volume_type]]` twice: `id = 0`, `label.en = "Per Unit"` and
      `id = 2`, `label.en = "Volume"`. Code `1` is deliberately absent from the vocabulary; code `2`
      is deliberately never observed in the table.
    - `hydro_plant.toml`: top-level `id = "HydroPlant"`, one `[[attribute]]` with
      `id = "initial_volume_type"` and `enum = "initial_volume_type"`.
    - Three `create_element("HydroPlant", ...)` calls with distinct labels and
      `initial_volume_type` set to `0`, `0`, `1` (`static_cast<int64_t>`).
    - `EXPECT_TRUE` that `db.summarize_collection("HydroPlant")` contains the substring
      `values {0 "Per Unit": 2, 1: 1}` — this one string pins D2-01 (label rides the key),
      D2-02 (one space in, nothing before the colon), D2-03 (quoted via `quote_ui_text`),
      D2-04 (ascending code order) and D2-05 (code 1 bare) at once.
    - `EXPECT_FALSE` that the same report contains the substring `"Volume"` (with its ASCII double
      quotes) — code 2's annotation is the only thing that could ever emit it, so this pins D2-07
      with no ambiguity against the lowercase `initial_volume_type` column name.
    - Both assertions stream the report via `<< report` on failure, matching the file's existing
      diagnostic style.

    RED first: build and run the new test against the unmodified renderer; it must fail on the
    `EXPECT_TRUE` (today's output is `values {0: 2, 1: 1}`). Then make it green with the emission
    change below.
  </behavior>

  <action>
    Raw-string-literal trap, hit twice in Phase 1: `R"(...)"` terminates at the first `)"`. The
    `hm³`/`(MW)`-style fixtures need a custom delimiter (`R"TOML(...)TOML"`). The two assertion
    strings in this test contain no `)`, so a plain `R"(...)"` is fine for them — verify before
    assuming.

    Production change, in `Database::summarize_collection` (`src/database_describe.cpp`), inside the
    `if (distinct[0][0] > 0 && distinct[0][0] <= kMaxDistributionCardinality)` branch only:

    1. Per D2-12, take `const auto* meta = impl_->ui_metadata.find(collection, scalar.name);`
       immediately before `out << "; values {"` — inside the cardinality branch, NOT at the top of
       the per-scalar loop. A collection of 200 TEXT scalars must pay zero two-level map lookups;
       the read sits next to its use.
    2. In the existing `for (size_t i = 0; i < rows.size(); ++i)` body, keep `out << rows[i][0];`
       and the `out << ": " << rows[i][1];` that follows it, and insert the annotation between
       them. When `meta` is non-null, look `rows[i][0]` up in `meta->enum_labels` — `rows[i][0]` is
       `int64_t` and the map key is `int64_t`, so the lookup is exact with no conversion or cast.
       On a hit, run the value through `normalize_ui_text` and, per D2-06, emit
       `' '` followed by `quote_ui_text(text)` ONLY when the normalized text is non-empty.
    3. D2-06 is a deliberate divergence from D-06 and must carry a comment at the injection site
       recording it: there the entry IS the vocabulary so an empty-normalizing label drops the whole
       entry; here the entry is an observed row count, so an empty-normalizing label drops the
       annotation and keeps the entry. Recorded as project decision D-09.
    4. Nothing else changes. No `write_ui_clauses` call (D2-11 — it would fire for TEXT, REAL and
       primary-key scalars and turn the statistics report into a second copy of
       `describe_collection`). No sort (D2-04 — `ORDER BY <col>` is already ascending code). No
       `squash` (D2-08 — there is no attribute name at entry level). No change to
       `kMaxDistributionCardinality` (D2-13). No new helper: the annotation has one call site, so
       extracting one would be dead abstraction.
    5. Do NOT touch `expect_reports_match` (`tests/test_database_ui_metadata.cpp:174-179`) or any of
       its four callers, and do NOT touch `tests/test_database_lifecycle.cpp`. Per D2-09 those four
       lifecycle assertions are `from_schema(":memory:")` + `describe()` and cannot be reached by
       this diff at all — they are a regression anchor, never evidence for this phase.
    6. Run `clang-format -i` on both touched files before committing; Phase 1 needed a follow-up
       style commit for skipping this.

    Reuse note for the security posture: `quote_ui_text` escapes `\` and `"` only. That is a
    parse-level guarantee, not substring immunity (D2-10), and it is unchanged here — see
    `<threat_model>`.
  </action>

  <verify>
    <automated>cmake --build build --config Debug --target quiver_tests &amp;&amp; build/bin/quiver_tests.exe --gtest_filter='*DatabaseUiMetadata*:*UiMetadata*'</automated>
    <automated>build/bin/quiver_tests.exe --gtest_filter='*DatabaseUiMetadata*Histogram*'</automated>
    <automated>build/bin/quiver_tests.exe --gtest_filter='*DescribeVectorsHeaderPrintedOnce*:*DescribeSetsHeaderPrintedOnce*:*DescribeTimeSeriesWithDimensionColumn*:*DescribeNoCategoryHeaderWhenEmpty*'</automated>
    <automated>build/bin/quiver_tests.exe</automated>
    <automated>build/bin/quiver_c_tests.exe</automated>
  </verify>

  <acceptance_criteria>
    - `build/bin/quiver_tests.exe --gtest_filter='*DatabaseUiMetadata*Histogram*'` reports `1 test
      from 1 test suite ran` and `[  PASSED  ] 1 test` (VALIDATION rows 2-01-01, 2-01-02, 2-01-03).
    - `build/bin/quiver_tests.exe --gtest_filter='*DatabaseUiMetadata*Malformed*:*NoUiDir*'` passes
      (VALIDATION row 2-01-04, the SAFE-02 regression anchor).
    - `build/bin/quiver_tests.exe --gtest_filter='*DescribeVectorsHeaderPrintedOnce*:*DescribeSetsHeaderPrintedOnce*:*DescribeTimeSeriesWithDimensionColumn*:*DescribeNoCategoryHeaderWhenEmpty*'`
      passes (VALIDATION row 2-01-05 — anchor, not evidence, per D2-09).
    - `build/bin/quiver_tests.exe` reports at least 1271 passing and 0 failing (Phase 1 baseline
      1270 plus this one test); `build/bin/quiver_c_tests.exe` reports 557 passing, 0 failing.
    - `git diff --stat -- tests/test_database_lifecycle.cpp` prints nothing.
    - `git diff -U0 -- tests/test_database_ui_metadata.cpp | grep -c '^-[^-]'` returns `0` — the
      test file is appended to only, so `expect_reports_match` and its callers are untouched.
    - `git status --porcelain` lists no file outside `src/database_describe.cpp` and
      `tests/test_database_ui_metadata.cpp`; in particular no new `src/*.h`, no `src/CMakeLists.txt`
      and nothing under `tests/schemas/ui/`.
    - `grep -c 'kMaxDistributionCardinality = 64' src/database_describe.cpp` returns `1` (D2-13).
    - `grep -n 'write_ui_clauses' src/database_describe.cpp` shows the definition and its one
      pre-existing call inside `write_collection_section` — no call inside `summarize_collection`
      (D2-11).
    - `clang-format --dry-run -Werror src/database_describe.cpp tests/test_database_ui_metadata.cpp`
      reports no violations.
  </acceptance_criteria>

  <reversibility rating="costly">
    D2-01's `code SP "Label": count` grammar. Undoing it rewrites this plan's expected-string
    assertion and any downstream corpus tooling that learned the shape; it breaks no published
    contract, since the reports are opaque `std::string` through the C API. Same rating Phase 1
    recorded for D-01's clause grammar. Not a checkpoint: the diff is one loop and `git revert`
    restores the byte-identical prior output.
  </reversibility>

  <done>
    `summarize_collection` on a `from_migrations` database renders
    `values {0 "Per Unit": 2, 1: 1}`; the uncovered code stays bare, the unobserved vocabulary code
    never appears, and every pre-existing test in both suites still passes.
  </done>
</task>

<task type="auto">
  <name>Task 2-01-02: D2-14 documentation edits and the D-09 decision record</name>

  <read_first>
    - `src/database_describe.cpp` — the emission block task 1 just changed, so the prose describes
      what actually shipped rather than what was planned.
    - `CHANGELOG.md` lines 1-30 — the `0.10.8 — unreleased` entry and its closing sentence at line
      26, which scopes `summarize_collection()` out of this milestone's feature.
    - `CLAUDE.md` line 576 — the Core API bullet listing the three text reports.
    - `src/CLAUDE.md` lines 118-180 — the `ui_metadata.h`/`ui_metadata.cpp` section and its closing
      sentence at line 177, which likewise scopes summarize out.
    - `.planning/phases/02-enum-labels-on-the-value-histogram/02-CONTEXT.md` `<deferred>` — the exact
      wording of the two known limits to record.
  </read_first>

  <files>CHANGELOG.md, CLAUDE.md, src/CLAUDE.md</files>

  <action>
    Three prose edits, per D2-14 and the project's Self-Updating rule. No code.

    1. `CHANGELOG.md`, inside the existing `## [0.10.8] — unreleased` → `### Added` entry: replace
       the closing clause at line 26 that scopes summarize out with the truth this phase makes —
       `summarize_collection()`'s integer value distribution now annotates each observed code with
       that code's enum label, `values {0 "Per Unit": 2, 1: 1}`. Keep the rest of the entry's
       "deliberately not included" list intact (no C API symbol, no structured getter, no sidecar
       validation, English only, `from_schema` unaffected) and extend it with the two known limits:
       a code the vocabulary does not cover stays bare, and a column with more than 64 distinct
       codes still renders no distribution clause at all. Do not open a new version section; do not
       touch the `[0.10.7]` section or any compare link. Match the file's em-dash and wrapping
       conventions.
    2. Root `CLAUDE.md` line 576, Core API bullet: after
       `summarize_collection(c)`'s existing parenthetical, state that a low-cardinality INTEGER
       distribution annotates each observed code with its `ui/` sidecar enum label when the database
       was opened with `from_migrations`. One clause, not a paragraph — this bullet is a surface
       index, not documentation.
    3. `src/CLAUDE.md`, in the `ui_metadata.h`/`ui_metadata.cpp` section: replace the closing
       sentence at line 177 that defers summarize to a later phase, and in its place record three
       things.
       - **D-09 (D2-06):** in `summarize_collection`'s histogram an enum label that normalizes to
         empty drops the *annotation* and keeps the *entry* — the deliberate divergence from D-06,
         which drops the whole entry in `describe`'s `enum {}` clause. There the entry is
         vocabulary; here it is an observed row count, and dropping it destroys data. Written as a
         numbered decision so a later reader does not read it as an inconsistency.
       - **Known limit 1:** `ui_metadata` is populated only by `from_migrations`, so
         `Database::open("study.db").summarize_collection(...)` still shows bare codes. This must
         not be "fixed" by hooking the load onto `load_schema_metadata` / `require_schema` —
         `migrate_up` early-returns before it on the open-an-existing-study path, which is the
         documented reason the load lives in `from_migrations`.
       - **Known limit 2:** `kMaxDistributionCardinality` (64) suppresses the whole `; values {}`
         clause above 64 distinct codes, so the richest enum column renders no labels;
         `describe_collection`'s `; enum {...}` is the fallback. Also note that 64 labelled entries
         is a ~2 KB single line — fine for an LLM reader, rough for a terminal — and that no
         truncation is proposed, because truncation needs its own ellipsis convention.
       Also record D2-10's honest position in one sentence: `quote_ui_text` gives a parse-level
       guarantee, not substring immunity, and summarize additionally emits
       `  Vectors:` / `  Sets:` / `  Time Series:` headers, so a naive header-count test written
       against summarize on a `from_migrations` database would be breakable. Do not propose a
       blocklist as the remedy.

    `tests/CLAUDE.md` needs no edit: its `test_database_ui_metadata.cpp` suite entry already exists
    and this task adds no new file or fixture rule. Do not touch `CMakeLists.txt` or any of the five
    version manifests — this phase ships inside the already-open `0.10.8`.
  </action>

  <verify>
    <automated>grep -c "does not yet render this metadata" CHANGELOG.md; grep -c "does not render any of this yet" src/CLAUDE.md; grep -c 'values {0 "Per Unit": 2, 1: 1}' CHANGELOG.md; grep -n "summarize_collection" CLAUDE.md | head -3</automated>
    <automated>uv run python scripts/assert_version.py</automated>
    <automated>build/bin/quiver_tests.exe --gtest_filter='*DatabaseUiMetadata*:*UiMetadata*'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "does not yet render this metadata" CHANGELOG.md` returns `0` (VALIDATION row
      2-01-06).
    - `grep -c "does not render any of this yet" src/CLAUDE.md` returns `0`.
    - `grep -c 'values {0 "Per Unit": 2, 1: 1}' CHANGELOG.md` returns `1` — the entry shows the
      rendered form, not just a claim.
    - `grep -c '^## \[0.10.8\] — unreleased' CHANGELOG.md` returns `1` and
      `grep -c '^## \[0.10.7\] — 2026-09-17' CHANGELOG.md` returns `1` — no new version section was
      opened and Phase 1's D-15 reconciliation is intact.
    - `sed -n '576p' CLAUDE.md | grep -c 'summarize_collection'` returns `1` and that same line
      mentions the enum-label annotation — the bullet was edited in place, not duplicated.
    - `grep -c 'D-09' src/CLAUDE.md` returns at least `1`, and `grep -c 'from_migrations' src/CLAUDE.md`
      shows the known-limit sentence present (the `Database::open` gap is named).
    - `uv run python scripts/assert_version.py` passes — all five manifests still agree at `0.10.8`.
    - `git diff --stat` lists exactly `CHANGELOG.md`, `CLAUDE.md`, `src/CLAUDE.md` for this task's
      commit — no code file, no `tests/CLAUDE.md`, no `CMakeLists.txt`.
    - `build/bin/quiver_tests.exe --gtest_filter='*DatabaseUiMetadata*:*UiMetadata*'` still passes
      (prose-only task; proves nothing was edited by accident).
  </acceptance_criteria>

  <done>
    No document in the repo still claims `summarize_collection` omits this metadata; D2-06 is
    recorded as project decision D-09 in `src/CLAUDE.md` alongside the two known limits and D2-10's
    honest position.
  </done>
</task>

</tasks>

<assumption_delta_decision>
**Scan:** `gsd_run query assumption-delta scan 2 --json` → `detected: true`, one signal.

**Signal:** kind `pluralization`, term `second`, snippet
`…introduces "Vectors:", "Sets:", "Time Series:" or a second "values {", so the four brittle
assertions in te…` — ROADMAP.md Phase 2 success criterion 3.

**Decision: `no-change`.**

Rationale: the token is prose, not a cardinality transition. "a second `values {`" names an
*injection hazard* — the thing the criterion asserts must never happen — not a design that grows
from one histogram clause to many. The emission loop writes exactly one `; values {` per scalar
line before and after this phase, and the annotation adds no clause and no repetition. Same shape
as Phase 1's false positive on the word "also"; no plan structure changes.
</assumption_delta_decision>

<threat_model>
Resolved config: `security_asvs_level: 1`, `security_block_on: high`.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ui/*.toml` sidecar → renderer | Untrusted text: the sidecar is corpus data authored outside this repo, on the filesystem beside a study's migrations. Its `label.en` strings are the only attacker-influenced bytes this phase newly copies into a report. |
| report string → consumer | The returned `std::string` is read by an LLM agent, and printed to a terminal by `quiver_cli` and every binding's REPL. |

No package-manager install occurs in this phase (no npm/pip/cargo step, no new dependency, no CMake
change), so no `T-02-SC` supply-chain row applies and no legitimacy checkpoint is required.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | `normalize_ui_text` call in the new annotation, `src/database_describe.cpp` | high | mitigate | Every emitted label goes through `normalize_ui_text` **before** `quote_ui_text`, mapping every byte below `0x20` and `0x7F` to a space. This is what stops a sidecar label from emitting an ANSI escape (ESC `0x1B`) into a terminal rendering the report, or a `\n` that forges an additional report line. Reusing the existing helper — not a new one — is the mitigation (D2-03); skipping it is the vulnerability. Pinned indirectly by the Phase 1 tests the task's full-suite run re-executes. |
| T-02-02 | Spoofing | `quote_ui_text` output inside `values {}` | medium | accept | `quote_ui_text` escapes only `\` and `"`, so a corpus label spelled `Sets: 3` or `values {see manual}` renders verbatim *inside* its quotes. That is a **parse**-level guarantee, not substring immunity — Phase 1's accepted D-02 posture, unchanged, and the entire reason the quotes exist. Accepted, not mitigated (D2-03 and D2-10 both reject a substring blocklist: a second escaping rule can drift from D-02). |
| T-02-03 | Spoofing | `  Vectors:` / `  Sets:` / `  Time Series:` headers in `summarize_collection` | medium | accept | What Phase 2 genuinely widens: `summarize_collection` emits those headers too, so a *future* naive header-count test written against summarize on a `from_migrations` database would be breakable by a label containing that text. Recorded as a known ceiling per D2-10. No existing test is exposed — the four SC-3 lifecycle assertions are `from_schema(":memory:")` + `describe()` and unreachable from this diff (D2-09). Remedy if it ever bites: assert on the report's structure (line prefix + indentation), not on a substring count. |
| T-02-04 | Denial of Service | `kMaxDistributionCardinality`, line length | low | accept | 64 labelled entries is a ~2 KB single line. The 64-code cap (D2-13) already bounds it, and no truncation is added because truncation needs its own ellipsis convention. Recorded as a known limit in `src/CLAUDE.md` by task 2. |
| T-02-05 | Information Disclosure | `impl_->ui_metadata.find` lookup | low | accept | The annotation exposes only sidecar text the same database already renders in `describe` / `describe_collection` (Phase 1). No new data source, no new file read, no new code path reaching the filesystem. |

No `high` threat is left undispositioned: T-02-01 is mitigated inside this phase's diff. Everything
accepted is `medium` or below and carries a recorded rationale.
</threat_model>

<verification>
Run in this order after both tasks land:

1. `cmake --build build --config Debug` — clean build, no new warnings in `src/database_describe.cpp`.
2. `build/bin/quiver_tests.exe` — at least 1271 passing, 0 failing (Phase 1 baseline 1270 + 1).
3. `build/bin/quiver_c_tests.exe` — 557 passing, 0 failing (unaffected; no C API change).
4. `git diff --stat master` — exactly five files: `src/database_describe.cpp`,
   `tests/test_database_ui_metadata.cpp`, `CHANGELOG.md`, `CLAUDE.md`, `src/CLAUDE.md`. No new file
   anywhere, nothing under `tests/schemas/ui/`, no `tests/test_database_lifecycle.cpp`.
5. `clang-format --dry-run -Werror src/database_describe.cpp tests/test_database_ui_metadata.cpp` —
   no violations.
6. `uv run python scripts/assert_version.py` — all five manifests agree at `0.10.8`.

**Manual-only (VALIDATION.md):** rendering against a real PSR study corpus. Open a real study with
`from_migrations`, call `summarize_collection`, confirm histogram entries carry their labels —
noting that the recorded `HasCommitment` inversion renders verbatim by design (accepted risk in
STATE.md, `validate_ui_config()` is out of scope).
</verification>

<success_criteria>
- RENDER-02 SC-1: an enum-bound INTEGER scalar's histogram renders each code with its label beside
  the code, from Phase 1's map (D2-01, D2-02).
- RENDER-02 SC-2: an uncovered code and a column with no vocabulary both keep today's bare entry —
  the annotation is per-code, not per-column (D2-05).
- RENDER-02 SC-3: the four `tests/test_database_lifecycle.cpp` describe assertions pass unmodified
  (satisfied literally; per D2-09 they are an anchor, not evidence).
- D2-07: no zero-observation vocabulary code is printed.
- D2-14: no document still scopes `summarize_collection` out of this milestone's feature; D2-06 is
  recorded as D-09 with the two known limits.
- Both suites green; five files touched; no new file.
</success_criteria>

<output>
Create `.planning/phases/02-enum-labels-on-the-value-histogram/02-01-SUMMARY.md` when done.

Record in `key-decisions`: **D-09** (D2-06 — annotation dropped, entry kept; the deliberate
divergence from D-06), the `Database::open` gap (`ui_metadata` is populated only by
`from_migrations`, and must not be fixed by hooking `load_schema_metadata` / `require_schema`), and
the >64-distinct-code ceiling. Record D2-09 and D2-10 as honesty records, not as evidence.
</output>
