---
phase: 06-test-migration-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bindings/js/test/database.test.ts
  - bindings/js/test/csv.test.ts
  - bindings/js/test/metadata.test.ts
  - bindings/js/test/time-series.test.ts
  - bindings/js/test/composites.test.ts
  - bindings/js/test/lua-runner.test.ts
  - bindings/js/deno.json
autonomous: true
requirements: [TEST-01, TEST-02]

must_haves:
  truths:
    - "All 6 test files use Deno.test instead of vitest describe/test"
    - "All 6 test files use @std/assert assertions instead of vitest expect"
    - "database.test.ts uses Deno.makeTempDirSync/Deno.removeSync instead of node:fs mkdtempSync/rmSync"
    - "csv.test.ts uses Deno.readTextFileSync instead of node:fs readFileSync"
    - "No node: protocol imports remain in any test file"
    - "Full test suite (all 12 files) passes with deno test"
  artifacts:
    - path: "bindings/js/test/database.test.ts"
      provides: "Database lifecycle + error handling + library loading tests under Deno.test"
      contains: "Deno.test"
    - path: "bindings/js/test/csv.test.ts"
      provides: "CSV export/import/options tests under Deno.test"
      contains: "Deno.test"
    - path: "bindings/js/test/metadata.test.ts"
      provides: "Metadata get/list tests under Deno.test"
      contains: "Deno.test"
    - path: "bindings/js/test/time-series.test.ts"
      provides: "Time series read/update/files tests under Deno.test"
      contains: "Deno.test"
    - path: "bindings/js/test/composites.test.ts"
      provides: "Composite read helpers tests under Deno.test"
      contains: "Deno.test"
    - path: "bindings/js/test/lua-runner.test.ts"
      provides: "LuaRunner tests under Deno.test"
      contains: "Deno.test"
  key_links:
    - from: "bindings/js/test/*.test.ts"
      to: "jsr:@std/assert"
      via: "import assertions"
      pattern: "from.*jsr:@std/assert"
    - from: "bindings/js/test/database.test.ts"
      to: "Deno namespace"
      via: "Deno.makeTempDirSync, Deno.removeSync"
      pattern: "Deno\\.(makeTempDirSync|removeSync)"
    - from: "bindings/js/test/csv.test.ts"
      to: "Deno namespace"
      via: "Deno.readTextFileSync"
      pattern: "Deno\\.readTextFileSync"
---

<objective>
Migrate remaining 6 test files (including 2 with filesystem operations) from vitest to Deno.test, then validate the full 12-file suite passes.

Purpose: database.test.ts and csv.test.ts use node:fs/node:os APIs that must be replaced with Deno equivalents (Deno.makeTempDirSync, Deno.readTextFileSync, etc). The other 4 files follow the same mechanical pattern as Plan 01. Final task validates the entire suite.

Output: All 12 test files fully migrated, full test suite passing under deno test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@bindings/js/deno.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate database.test.ts and csv.test.ts (filesystem-heavy) to Deno.test</name>
  <files>bindings/js/test/database.test.ts, bindings/js/test/csv.test.ts</files>
  <read_first>bindings/js/test/database.test.ts, bindings/js/test/csv.test.ts</read_first>
  <action>
These 2 files require extra work beyond the standard vitest-to-Deno.test conversion because they use node:fs and node:os APIs.

**database.test.ts specific changes:**

1. Remove vitest import: `import { afterEach, describe, expect, test } from "vitest";`
2. Add assertions: `import { assert, assertEquals, assertInstanceOf, assertNotEquals, assertThrows } from "jsr:@std/assert";`
3. Replace dirname pattern:
   Remove:
   ```
   import { dirname } from "node:path";
   import { fileURLToPath } from "node:url";
   const __dirname = dirname(fileURLToPath(import.meta.url));
   ```
   Replace with: `const __dirname = import.meta.dirname!;`

4. Replace node:fs and node:os imports:
   Remove:
   ```
   import { existsSync, mkdtempSync, rmSync } from "node:fs";
   import { tmpdir } from "node:os";
   ```
   These Deno replacements are used inline (no import needed):
   - `existsSync(path)` -> Use a try/catch with `Deno.statSync(path)` returning true/false, OR use a small helper: `function existsSync(path: string): boolean { try { Deno.statSync(path); return true; } catch { return false; } }`
   - `mkdtempSync(join(tmpdir(), "quiver_js_test_"))` -> `Deno.makeTempDirSync({ prefix: "quiver_js_test_" })`
   - `rmSync(dir, { recursive: true })` -> `Deno.removeSync(dir, { recursive: true })`

5. Replace `join` import: `import { join } from "node:path";` -> `import { join } from "jsr:@std/path";`

6. Fix src imports: `"../src/index"` -> `"../src/index.ts"`, `"../src/loader"` -> `"../src/loader.ts"`

7. Convert describe/test/afterEach structure to Deno.test/t.step:
   The `afterEach` cleanup pattern with `tempDirs` array should become cleanup in a `finally` block or using `Deno.test` with a wrapper. Since the existing code already uses `try/finally` for db.close(), the simplest approach is:
   - Remove `afterEach` entirely
   - The `makeTempDir()` function still tracks dirs in an array
   - After each `Deno.test` top-level block, add cleanup in a finally or at the end of the test function
   
   Alternatively, since each test creates its own tempDir and uses try/finally, convert the `describe("Database lifecycle", ...)` block into `Deno.test("Database lifecycle", async (t) => { ... })` with `t.step(...)` for each nested test. Put the cleanup in the Deno.test's own finally block.

   For `describe("Database lifecycle", ...)`:
   ```typescript
   Deno.test("Database lifecycle", async (t) => {
     const tempDirs: string[] = [];

     function makeTempDir(): string {
       const dir = Deno.makeTempDirSync({ prefix: "quiver_js_test_" });
       tempDirs.push(dir);
       return dir;
     }

     // ... all t.step() calls ...

     // Cleanup after all steps
     for (const dir of tempDirs) {
       try {
         Deno.removeSync(dir, { recursive: true });
       } catch {
         // Ignore cleanup errors
       }
     }
   });
   ```

8. Convert assertions:
   - `expect(db).toBeDefined()` -> `assertNotEquals(db, undefined)`
   - `expect(db).toBeInstanceOf(Database)` -> `assertInstanceOf(db, Database)`
   - `expect(existsSync(dbPath)).toBe(true)` -> `assertEquals(existsSync(dbPath), true)` (using the helper)
   - `expect(() => ...).toThrow(QuiverError)` -> `assertThrows(() => ..., QuiverError)`
   - `expect(err).toBeInstanceOf(QuiverError)` -> `assertInstanceOf(err, QuiverError)`
   - `expect(err.message).toBe("Database is closed")` -> `assertEquals(err.message, "Database is closed")`
   - `expect(err).toBeInstanceOf(Error)` -> `assertInstanceOf(err, Error)`
   - `expect(err.name).toBe("QuiverError")` -> `assertEquals(err.name, "QuiverError")`
   - `expect(version).toBeTruthy()` -> `assert(version)`
   - `expect(typeof version).toBe("string")` -> `assertEquals(typeof version, "string")`
   - `expect(version.length).toBeGreaterThan(0)` -> `assertGreater(version.length, 0)` -- add `assertGreater` to import

**csv.test.ts specific changes:**

1. Remove vitest import: `import { describe, expect, test } from "vitest";`
2. Add assertions: `import { assert, assertEquals, assertFalse, assertThrows } from "jsr:@std/assert";` -- add `assertStringIncludes` if needed for `.toContain()` on strings

3. Replace dirname pattern (same as above).

4. Replace node:fs and node:os imports:
   Remove:
   ```
   import { existsSync, readFileSync, unlinkSync } from "node:fs";
   import { tmpdir } from "node:os";
   ```
   Replacements:
   - `existsSync(path)` -> same helper as database.test.ts, OR inline `try { Deno.statSync(p); return true; } catch { return false; }`
   - `readFileSync(csvPath, "utf-8")` -> `Deno.readTextFileSync(csvPath)`
   - `unlinkSync(p)` -> `Deno.removeSync(p)`
   - `tmpdir()` -> Not needed. The `tempCsv()` helper uses `join(tmpdir(), ...)`. Replace with `Deno.makeTempFileSync({ prefix: "quiver_js_csv_test_", suffix: `.csv` })` -- but actually the original creates a path string without creating the file. So replace `tempCsv` with a function that uses Deno APIs to get the temp directory:
     ```typescript
     function tempCsv(name: string): string {
       const dir = Deno.env.get("TMPDIR") || Deno.env.get("TMP") || Deno.env.get("TEMP") || "/tmp";
       return join(dir, `quiver_js_csv_test_${name}_${Date.now()}.csv`);
     }
     ```
     Or simpler: just use the Deno temp dir approach. Since csv.test.ts needs a path but doesn't need the file to pre-exist, use:
     ```typescript
     function tempCsv(name: string): string {
       return join(Deno.makeTempDirSync(), `${name}.csv`);
     }
     ```

   - The `cleanup()` function: replace `existsSync` check + `unlinkSync` with try/catch `Deno.removeSync`:
     ```typescript
     function cleanup(...paths: string[]): void {
       for (const p of paths) {
         try { Deno.removeSync(p); } catch { /* Ignore */ }
       }
     }
     ```

5. Replace `join` import: `"node:path"` -> `"jsr:@std/path"`
6. Fix src imports: `"../src/index"` -> `"../src/index.ts"`

7. Convert describe/test to Deno.test/t.step.

8. Convert assertions:
   - `expect(existsSync(csvPath)).toBe(true)` -> `assertEquals(existsSync(csvPath), true)` or use the helper
   - `expect(content).toContain("some string")` -> `assertStringIncludes(content, "some string")` -- import `assertStringIncludes` from `jsr:@std/assert`
   - `expect(content).not.toContain("some string")` -> `assertFalse(content.includes("some string"))` -- import `assertFalse`
   - `expect(content).toBe("exact string")` -> `assertEquals(content, "exact string")`
   - `expect(labels.sort()).toEqual([...])` -> `assertEquals(labels.sort(), [...])`
   - `expect(() => ...).toThrow(QuiverError)` -> `assertThrows(() => ..., QuiverError)`
  </action>
  <verify>
    <automated>cd C:/Development/DatabaseTmp/quiver4/bindings/js && deno test --allow-ffi --allow-read --allow-write --allow-env test/database.test.ts test/csv.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "from.*vitest" bindings/js/test/database.test.ts returns 0
    - grep -c "from.*vitest" bindings/js/test/csv.test.ts returns 0
    - grep -c "node:fs" bindings/js/test/database.test.ts returns 0
    - grep -c "node:os" bindings/js/test/database.test.ts returns 0
    - grep -c "node:fs" bindings/js/test/csv.test.ts returns 0
    - grep -c "node:os" bindings/js/test/csv.test.ts returns 0
    - grep -c "Deno.test" bindings/js/test/database.test.ts returns at least 1
    - grep -c "Deno.test" bindings/js/test/csv.test.ts returns at least 1
    - grep "Deno.makeTempDirSync\|Deno.removeSync" bindings/js/test/database.test.ts shows Deno filesystem APIs
    - grep "Deno.readTextFileSync" bindings/js/test/csv.test.ts shows Deno file read API
    - deno test --allow-ffi --allow-read --allow-write --allow-env test/database.test.ts exits 0
    - deno test --allow-ffi --allow-read --allow-write --allow-env test/csv.test.ts exits 0
  </acceptance_criteria>
  <done>database.test.ts and csv.test.ts migrated from vitest to Deno.test. All node: imports replaced with Deno APIs. Both files pass under deno test.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate metadata.test.ts, time-series.test.ts, composites.test.ts, lua-runner.test.ts to Deno.test</name>
  <files>bindings/js/test/metadata.test.ts, bindings/js/test/time-series.test.ts, bindings/js/test/composites.test.ts, bindings/js/test/lua-runner.test.ts</files>
  <read_first>bindings/js/test/metadata.test.ts, bindings/js/test/time-series.test.ts, bindings/js/test/composites.test.ts, bindings/js/test/lua-runner.test.ts</read_first>
  <action>
Apply the standard vitest-to-Deno.test transformation to all 4 files. These files have no filesystem I/O beyond schema path construction.

For each file:
1. Remove vitest import
2. Add `jsr:@std/assert` import with only needed assertions
3. Replace `dirname`/`fileURLToPath` with `const __dirname = import.meta.dirname!;`
4. Replace `join` from `"node:path"` with `"jsr:@std/path"`
5. Fix `../src/index` -> `../src/index.ts`
6. Convert `describe/test` to `Deno.test/t.step`
7. Convert all assertion calls

**metadata.test.ts specific:**
- Imports `GroupMetadata, ScalarMetadata` types -- keep these, fix extension
- Uses `toBe`, `toBeNull`, `toThrow`, `toBeDefined`, `toBeGreaterThanOrEqual`
- `expect(meta.name).toBe("some_integer")` -> `assertEquals(meta.name, "some_integer")`
- `expect(typeof meta.dataType).toBe("number")` -> `assertEquals(typeof meta.dataType, "number")`
- `expect(meta.notNull).toBe(false)` -> `assertEquals(meta.notNull, false)`
- `expect(meta.defaultValue).toBeNull()` -> `assertEquals(meta.defaultValue, null)`
- `expect(meta.valueColumns.length).toBeGreaterThanOrEqual(1)` -> `assert(meta.valueColumns.length >= 1)` (use `assert` from @std/assert)
- `expect(someInt).toBeDefined()` -> `assertNotEquals(someInt, undefined)`
- `expect(() => ...).toThrow()` -> `assertThrows(() => ...)` (no specific error type)
- Imports needed: `assert, assertEquals, assertNotEquals, assertThrows`

**time-series.test.ts specific:**
- Uses `toEqual`, `toBe`, `toBeNull`
- `expect(result.date_time).toEqual([...])` -> `assertEquals(result.date_time, [...])`
- `expect(result).toEqual({})` -> `assertEquals(result, {})`
- `expect(db.hasTimeSeriesFiles("Collection")).toBe(true)` -> `assertEquals(db.hasTimeSeriesFiles("Collection"), true)`
- `expect(columns).toEqual(["data_file", "metadata_file"])` -> `assertEquals(columns, ["data_file", "metadata_file"])`
- `expect(result.data_file).toBeNull()` -> `assertEquals(result.data_file, null)`
- Imports needed: `assertEquals`

**composites.test.ts specific:**
- Uses `toBe`, `toBeCloseTo`, `toBeNull`, `toEqual`
- `expect(scalars.float_attribute).toBeCloseTo(3.14)` -> `assertAlmostEquals(scalars.float_attribute, 3.14)` -- import `assertAlmostEquals` from `jsr:@std/assert`
- `expect(scalars.integer_attribute).toBe(42)` -> `assertEquals(scalars.integer_attribute, 42)`
- `expect(scalars.float_attribute).toBeNull()` -> `assertEquals(scalars.float_attribute, null)`
- `expect(vectors.amount).toEqual([10, 20, 30])` -> `assertEquals(vectors.amount, [10, 20, 30])`
- Imports needed: `assertAlmostEquals, assertEquals`

**lua-runner.test.ts specific:**
- Uses `toContain`, `toHaveLength`, `toThrow`, `not.toThrow`
- `expect(labels).toContain("FromLua")` -> `assert(labels.includes("FromLua"))` (array .includes)
- `expect(() => runner.run("if then")).toThrow(QuiverError)` -> `assertThrows(() => runner.run("if then"), QuiverError)`
- `expect(labels).toHaveLength(2)` -> `assertEquals(labels.length, 2)`
- `expect(() => runner.run("")).not.toThrow()` -> just call `runner.run("")` (no assertion needed; if it throws, the test fails)
- `expect(() => runner.close()).not.toThrow()` -> just call `runner.close()`
- Imports needed: `assert, assertEquals, assertThrows`
  </action>
  <verify>
    <automated>cd C:/Development/DatabaseTmp/quiver4/bindings/js && deno test --allow-ffi --allow-read --allow-write --allow-env test/metadata.test.ts test/time-series.test.ts test/composites.test.ts test/lua-runner.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "from.*vitest" bindings/js/test/metadata.test.ts bindings/js/test/time-series.test.ts bindings/js/test/composites.test.ts bindings/js/test/lua-runner.test.ts returns 0 for each
    - grep -c "Deno.test" bindings/js/test/metadata.test.ts returns at least 1
    - grep -c "Deno.test" bindings/js/test/time-series.test.ts returns at least 1
    - grep -c "Deno.test" bindings/js/test/composites.test.ts returns at least 1
    - grep -c "Deno.test" bindings/js/test/lua-runner.test.ts returns at least 1
    - grep -c "assertAlmostEquals" bindings/js/test/composites.test.ts returns at least 1
    - grep -c "node:path" bindings/js/test/metadata.test.ts returns 0
    - deno test --allow-ffi --allow-read --allow-write --allow-env test/metadata.test.ts exits 0
    - deno test --allow-ffi --allow-read --allow-write --allow-env test/time-series.test.ts exits 0
    - deno test --allow-ffi --allow-read --allow-write --allow-env test/composites.test.ts exits 0
    - deno test --allow-ffi --allow-read --allow-write --allow-env test/lua-runner.test.ts exits 0
  </acceptance_criteria>
  <done>All 4 files converted from vitest to Deno.test with @std/assert. No vitest or node: imports remain. deno test passes for all 4 files.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite validation and deno.json imports map update</name>
  <files>bindings/js/deno.json</files>
  <read_first>bindings/js/deno.json, bindings/js/test/create.test.ts</read_first>
  <action>
Run the full test suite across all 12 files to validate the complete migration.

1. Run: `cd bindings/js && deno test --allow-ffi --allow-read --allow-write --allow-env test/`
2. If any test fails, diagnose and fix.
3. Verify zero vitest imports remain across all test files: `grep -r "vitest" bindings/js/test/`
4. Verify zero node: imports remain across all test files: `grep -r "node:" bindings/js/test/`

If deno.json needs an imports map entry for `@std/assert` to lock the version, add it:
```json
{
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.0"
  }
}
```
This is only needed if bare `jsr:@std/assert` specifiers cause version resolution issues. If direct `jsr:@std/assert` imports work fine (they should -- Deno resolves jsr: specifiers natively), skip the imports map.

After the full suite passes, regenerate the lock file:
```bash
cd bindings/js && deno cache --lock=deno.lock --lock-write test/create.test.ts
```
Or run the test suite once which will update deno.lock automatically.

Verify the lock file now includes @std/assert entries.
  </action>
  <verify>
    <automated>cd C:/Development/DatabaseTmp/quiver4/bindings/js && deno test --allow-ffi --allow-read --allow-write --allow-env test/ 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - deno test --allow-ffi --allow-read --allow-write --allow-env test/ exits 0 with all 12 test files passing
    - grep -r "vitest" bindings/js/test/ returns no matches
    - grep -r "node:" bindings/js/test/ returns no matches
    - bindings/js/deno.lock contains "@std/assert" entry
    - Output of deno test shows "ok" for every test step across all 12 files
  </acceptance_criteria>
  <done>Full test suite (all 12 files) passes under Deno.test. No vitest or node: imports remain in any test file. deno.lock updated with @std/assert dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code -> Native FFI | Tests call C API via Deno.dlopen, same trust boundary as production code |
| Test code -> Filesystem | database.test.ts and csv.test.ts create/read temp files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | jsr:@std/assert import | accept | @std/assert is Deno's official standard library, integrity verified by deno.lock hash |
| T-06-02 | Information Disclosure | test temp files | accept | Tests use system temp directory, clean up in finally blocks, no secrets written |
| T-06-03 | Elevation of Privilege | Deno permissions | mitigate | Tests request only minimum permissions: --allow-ffi (required), --allow-read (schema files), --allow-write (temp files), --allow-env (temp dir) |
</threat_model>

<verification>
Run the complete test suite:
```bash
cd bindings/js && deno test --allow-ffi --allow-read --allow-write --allow-env test/
```

Verify no vitest remnants:
```bash
grep -rl "vitest" bindings/js/test/
grep -rl "node:" bindings/js/test/
```
Both should return empty.

Verify all files use Deno.test:
```bash
grep -l "Deno.test" bindings/js/test/*.test.ts | wc -l
```
Should return 12.
</verification>

<success_criteria>
- All 12 test files migrated from vitest to Deno.test
- Zero vitest imports remain in any test file
- Zero node: protocol imports remain in any test file
- database.test.ts uses Deno.makeTempDirSync/Deno.removeSync
- csv.test.ts uses Deno.readTextFileSync/Deno.removeSync
- All src imports use .ts extension
- Full suite passes: `deno test --allow-ffi --allow-read --allow-write --allow-env test/` exits 0
- deno.lock updated with @std/assert
</success_criteria>

<output>
After completion, create `.planning/phases/06-test-migration-validation/06-02-SUMMARY.md`
</output>
