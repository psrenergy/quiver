---
phase: 03-the-agent-reads-instead-of-transcribing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bindings/js/src/lua-api.ts
autonomous: true
requirements: [DOC-02, DOC-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The Standard library bullet in LUA_DB_API_REFERENCE no longer ends on 'there is no io' — it continues into: a data file on disk is read with db:read_csv / db:read_csv_stream, and file contents are never transcribed into the script (D-30, DOC-02)."
    - "The '## CSV file reading' section carries exactly one worked example, covering both real file shapes: BOM/CRLF + junk row above + units row below the header + apostrophe thousands separators + DD/MM/YYYY, and quoted fields containing commas + English month names (D-31, DOC-03)."
    - "The example spells out the tonumber/gsub parenthesis trap in a comment: gsub returns two values, the unparenthesised form silently returns nil."
    - "The example's Lua, copied verbatim out of the reference, runs against the committed fixtures and produces the values the two regression tests assert."
    - "The read-don't-transcribe instruction appears once, at the Standard library bullet only — not repeated at the head of the CSV section (D-30)."
  artifacts:
    - bindings/js/src/lua-api.ts
    - build/example-check/example.lua (throwaway harness, not committed)
  key_links:
    - "example Lua column indices <-> tests/fixtures/ma_energia_residencial.csv and ma_gd_data.csv actual columns"
    - "lua-api.ts <-> bindings/js/test/lua-api-sync.test.ts (the /Loaded standard libraries: ([^.]*)\\./ regex and the db: literal-token convention)"
    - "lua-api.ts template literal <-> TypeScript parse (backticks and ${ must stay escaped)"
---

<objective>
Close the gap the whole milestone exists for: `LUA_DB_API_REFERENCE` currently tells a model it has
no filesystem and stops there, so the model concludes it must paste file data into its script. Add
the counterpoint where that statement lives (DOC-02) and a worked example over the two real dirty
files (DOC-03) — then actually run the example before it ships.

Purpose: the reference is system-prompt payload interpolated into every `claw` session. A wrong
example teaches the wrong thing on every session forever, which is worse than no example.
Output: `bindings/js/src/lua-api.ts`, edited in two places and proven to run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-the-agent-reads-instead-of-transcribing/03-CONTEXT.md

@bindings/js/src/lua-api.ts
@tests/test_lua_runner_read_csv.cpp
</context>

<scope_guard>
This is a documentation phase. No production code changes, and no changes to the CSV reader's
behaviour or option surface — phases 1 and 2 closed both. If a doc edit appears to require a code
change, the doc is describing something untrue: fix the doc and flag it in the SUMMARY.

The `## CSV file reading` section (~line 616) is **already current**. It documents both entry
points, the `{ header = , rows = }` shape, string-only cells, no row padding, both option keys,
`header_row`'s 1-based rule and its `0` = no-header spelling, and the error catalogue. Do not
rewrite it. The only addition to that section is the worked example.
</scope_guard>

<tasks>

<task type="tracer">
  <name>Task 1: The read-don't-transcribe instruction plus the Energia half of the example, run end to end</name>
  <files>bindings/js/src/lua-api.ts, build/example-check/example.lua (throwaway)</files>
  <read_first>
Read `bindings/js/src/lua-api.ts` fully once — the Standard library bullet around line 93 and the
`## CSV file reading` section around line 616 are the two edit sites. Read
`tests/test_lua_runner_read_csv.cpp` lines 547-593 (`EnergiaRegressionJunkRowAboveUnitsRowBelowHeader`):
that test contains Lua that is known to work against the committed fixture, and it is what this
example is lifted from. Do not compose fresh Lua.
  </read_first>
  <action>
Two edits, in order.

**Edit 1 — DOC-02, per D-30.** Extend the **Standard library** bullet under `## Critical rules`.
It currently ends by listing what is absent (`os`, `io`, `debug`, `package`/`require`,
`dofile`/`loadfile`) and then notes `//` integer division. Append one or two sentences to that same
bullet making the counterpoint explicit: no `io` does **not** mean data on disk is out of reach — a
CSV file is read with `db:read_csv` / `db:read_csv_stream` (pointing at the `## CSV file reading`
section), and the contents of a data file must never be copied, pasted, or re-typed into the script
as literals. State it as an instruction to the model, not as a capability note. Per D-30 this is the
single placement: do NOT repeat it at the head of the CSV section, which already says "the only way
to get file data into a script, since `io` is deliberately absent".

Constraint the sync test imposes: `bindings/js/test/lua-api-sync.test.ts` extracts the stdlib list
with `/Loaded standard libraries: ([^.]*)\./` over a backtick-stripped, whitespace-collapsed view of
the doc. The first `.` after `Loaded standard libraries:` must therefore still close the library
list exactly as today. Append after that sentence; do not restructure it.

**Edit 2 — the Energia half of DOC-03.** Add a worked example at the end of the
`## CSV file reading` section (after the `db:read_csv_stream` block, before the section's `---`),
introduced by one sentence saying it is over two real files that used to be transcribed by hand.
Write the Energia half now; task 2 adds the GD half.

Lift the Energia transformation verbatim from the named regression test: read with
`{ header_row = 2 }` because line 1 is a block-title junk row; note in a comment that `rows[1]` is
the units row that sits *below* the header, so real data starts at `rows[2]`; split the DD/MM/YYYY
cell in column 5 with `row[5]:match("(%d%d)/(%d%d)/(%d%d%d%d)")`; strip the apostrophe thousands
separators in column 6 with `row[6]:gsub("['%s]", "")`.

The **parenthesis trap comment is the single highest-value line in this phase** — the failure is
silent — so give it two or three comment lines of its own, naming both forms: `gsub` returns *two*
values (the string and a replacement count), so the bare `tonumber(row[6]:gsub(...))` hands the
count to `tonumber` as its **base** argument and returns `nil` with no error; the extra parentheses
in `tonumber((row[6]:gsub(...)))` truncate the call to one value. Write the correct form in the
example and the broken form only inside the comment.

This is a TypeScript template literal: every backtick in the prose stays escaped as `\``, and a
literal `${` would open an interpolation. The existing text shows the convention throughout — the
example is a fenced ```lua block inside the literal, exactly like the blocks already in the file.

**Then prove it runs.** Create `build/example-check/` (gitignored, throwaway), copy
`tests/fixtures/ma_energia_residencial.csv` into it byte-for-byte, and write
`build/example-check/example.lua` containing the example's Lua **copied verbatim out of the
reference** (unescape the `\`` back to `` ` `` if any appear inside the block) plus appended
`assert`s for the values `EnergiaRegressionJunkRowAboveUnitsRowBelowHeader` asserts: the first data
row is `2005-01` / `93943`, and the row at file position 224 is `2023-07` / `386433`. Adjust the
index expressions to whatever accumulator shape the example uses — assert the same *values*, not
the test's exact subscripts. If a value disagrees, the example is wrong: fix the example, not the
assert.
  </action>
  <verify>
    <automated>rm -f build/example-check/check.sqlite &amp;&amp; ./build/bin/quiver_cli.exe --schema tests/schemas/valid/basic.sql build/example-check/check.sqlite build/example-check/example.lua</automated>
  </verify>
  <done>
The Standard library bullet carries the read-don't-transcribe instruction and the stdlib sentence
still parses under the sync test's regex. The CSV section carries the Energia half of the example
including the parenthesis-trap comment. The example's Lua, lifted verbatim into
`build/example-check/example.lua`, runs to completion through `quiver_cli` against the committed
fixture with every assert passing (exit 0).
  </done>
</task>

<task type="auto">
  <name>Task 2: The GD half of the example, then the budget and gate sweep</name>
  <files>bindings/js/src/lua-api.ts, build/example-check/example.lua (throwaway)</files>
  <read_first>
`tests/test_lua_runner_read_csv.cpp` lines 596-630 (`GdRegressionQuotedCommaAndEnglishMonthNames`)
— the second known-good transformation.
  </read_first>
  <action>
**Add the GD half** to the same example block, continuing from task 1's Energia half. Lift it from
the named regression test: this file's header is line 1, so no `header_row` is needed (say so in a
comment — it shows the default); a `MONTHS` name-to-number table is script-side work because `os` is
unloaded and there is no date library to lean on; the date cell is a quoted field containing a comma
(`"May 1, 2014"`), so note in a comment that the row still has exactly 2 fields — mishandled quoting
would have split the date and shifted the value column; parse it with
`row[1]:match("(%a+) (%d+), (%d+)")` and format the key with `string.format("%d-%02d", ...)`; the
value in column 2 is a plain decimal needing no cleanup, which is the point of showing both files.

**Hold the budget (D-31).** The finished example is the FULL form the user explicitly chose — do not
trim it back toward a compact one, that is a new decision, not an implementation detail. Equally, do
not let the fenced ```lua block exceed ~35 lines. It earns its place by being a complete worked
shape over two real files, not by touring the option surface, so it adds no `read_csv_stream` demo,
no `separator` demo, and no error-handling tour.

**Then sweep the gates:**
1. Extend `build/example-check/example.lua` with the GD half (again copied verbatim out of the
   reference) and its asserts from the regression test — first row `2014-05` / `33`, row 71
   `2021-07` / `51818.33` — after copying `tests/fixtures/ma_gd_data.csv` into
   `build/example-check/`. Re-run the whole script: both halves must pass in one run.
2. `bun test bindings/js/test/lua-api-sync.test.ts` — a TypeScript parse error in the template
   literal, a dropped `db:` literal token, or a disturbed stdlib sentence all fail here.
3. Confirm by eye that the instruction from task 1 appears once and was not also duplicated into the
   CSV section (D-30), and that the example block is within budget.

Nothing in CI re-runs the example. That fact is documented in plan 03-02; do not add a test harness
for it here — `build/example-check/` is throwaway and is not committed.
  </action>
  <verify>
    <automated>rm -f build/example-check/check.sqlite &amp;&amp; ./build/bin/quiver_cli.exe --schema tests/schemas/valid/basic.sql build/example-check/check.sqlite build/example-check/example.lua &amp;&amp; bun test bindings/js/test/lua-api-sync.test.ts</automated>
  </verify>
  <done>
One worked example in the `## CSV file reading` section covers both real file shapes and is at most
~35 lines of Lua. Copied verbatim into the throwaway harness it runs both halves in a single
`quiver_cli` invocation with every assert passing, and `lua-api-sync.test.ts` passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `LUA_DB_API_REFERENCE` -> downstream LLM system prompt | Prose written here is interpolated verbatim into every `claw` session and imitated by the model. |
| example Lua -> `db:read_csv` sandbox | Any path form shown in the example is the form a model will copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | the worked example in `lua-api.ts` | high | mitigate | The example is lifted from two passing regression tests and executed against the committed fixtures through `quiver_cli` before it ships (task 1 and task 2 verify), rather than eyeballed. |
| T-03-02 | Information disclosure | example path arguments | low | mitigate | The example uses bare relative filenames only, which `resolve_sandboxed_path` resolves against the database directory; no absolute path and no `..` segment appears, so the pattern a model copies cannot escape the sandbox. |
| T-03-03 | Denial of service | template literal syntax | medium | mitigate | Unescaped backticks or a literal `${` break the TypeScript parse for every consumer; `bun test bindings/js/test/lua-api-sync.test.ts` imports the module and fails on a parse error (task 2 verify). |

No package-manager installs occur in this plan; no package-legitimacy gate applies.
</threat_model>

<verification>
- `./build/bin/quiver_cli.exe --schema tests/schemas/valid/basic.sql build/example-check/check.sqlite build/example-check/example.lua` — exit 0, both halves' asserts pass.
- `bun test bindings/js/test/lua-api-sync.test.ts` — passes.
- Manual: the read-don't-transcribe instruction sits at the Standard library bullet and nowhere else;
  the example block is at most ~35 lines.
</verification>

<success_criteria>
- DOC-02: the reference tells the model to read data files from disk, stated at the place it is
  currently told it has no filesystem access.
- DOC-03: the reference carries a worked example over both realistically dirty files, including the
  `tonumber`/`gsub` parenthesis trap that silently returns `nil`.
- The example is executable truth, not plausible Lua: it has been run.
</success_criteria>

<output>
Create `.planning/phases/03-the-agent-reads-instead-of-transcribing/03-01-SUMMARY.md` when done.
Record in it: the final line count of the example block, the fixture values asserted, and anything
the reference claimed that turned out to be untrue.
</output>
