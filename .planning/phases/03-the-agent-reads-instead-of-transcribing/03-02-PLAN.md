---
phase: 03-the-agent-reads-instead-of-transcribing
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - CLAUDE.md
  - bindings/js/CLAUDE.md
  - CHANGELOG.md
autonomous: true
requirements: [DOC-04, TEST-05]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The CLAUDE.md nearest the phase-3 change (bindings/js/CLAUDE.md) records that the CSV worked example is executable truth verified by hand against tests/fixtures/, and that nothing in CI re-runs it (DOC-04)."
    - "Root CLAUDE.md no longer claims 'separator is the only option today' for db:read_csv — header_row shipped in phase 2 (DOC-04)."
    - "The cross-layer tables make the Lua-only status of db:read_csv / db:read_csv_stream visible where a reader scans for a method's counterparts, not only in the design-decision prose (DOC-04)."
    - "CHANGELOG.md's unreleased section carries one entry about the agent-reference change — not a restatement of the reader's features, which phase 2 already logged (DOC-04)."
    - "The full C++ and C API suites pass in a Release build configured with -DQUIVER_BUILD_TESTS=ON, and the build tree is removed afterwards (TEST-05, D-32)."
  artifacts:
    - CLAUDE.md
    - bindings/js/CLAUDE.md
    - CHANGELOG.md
  key_links:
    - "root CLAUDE.md db:read_csv design-decision bullet <-> the header_row option shipped in phase 2"
    - "Release configure flags <-> QUIVER_BUILD_TESTS (the `release` preset sets it OFF and would test nothing)"
---

<objective>
The house paperwork for the reference change (DOC-04) and the one Release run that gates the
release (TEST-05).

Purpose: phase 2 already documented the reader, `header_row`, `tests/fixtures/` and the
release-preset trap. This plan **extends** those entries where phase 3 changed something and fixes
one claim phase 2 left stale — it does not restate them.
Output: three documentation files updated, and a green Release run over the finished tree.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-the-agent-reads-instead-of-transcribing/03-CONTEXT.md
@.planning/phases/03-the-agent-reads-instead-of-transcribing/03-01-SUMMARY.md

@CLAUDE.md
@bindings/js/CLAUDE.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The nearest CLAUDE.md files, the stale option claim, and the changelog entry</name>
  <files>bindings/js/CLAUDE.md, CLAUDE.md, CHANGELOG.md</files>
  <read_first>
`bindings/js/CLAUDE.md` "Rules and gotchas", first bullet (the `LUA_DB_API_REFERENCE` bullet).
Root `CLAUDE.md`: the `db:read_csv` design-decision bullet near line 224, and the two cross-layer
tables under "Representative Cross-Layer Examples". `CHANGELOG.md`'s unreleased section.
  </read_first>
  <action>
**`bindings/js/CLAUDE.md` — the CLAUDE.md nearest the edited file, so this is the primary DOC-04
target.** Its `LUA_DB_API_REFERENCE` bullet already explains what `test/lua-api-sync.test.ts` can and
cannot check. Extend it with what phase 3 added to the uncheckable half: the reference now carries a
worked CSV example whose Lua is real, lifted from the `test_lua_runner_read_csv.cpp` regression tests
and executed against `tests/fixtures/ma_energia_residencial.csv` and `ma_gd_data.csv` before it
shipped — and **nothing in CI re-runs it**, so an edit to that example has to be re-run by hand the
same way (a throwaway file-backed database plus the fixtures, driven through `quiver_cli`). One
bullet or two sentences; this is the fact a future editor needs and cannot infer.

**Root `CLAUDE.md` — two edits.**

1. *A stale claim, not a new entry.* The `db:read_csv` design-decision bullet ends "`separator` is
   the only option today, a single-character string defaulting to `,`". That stopped being true in
   phase 2: `header_row` shipped alongside it. Correct the sentence to name both options, keeping
   `header_row`'s 1-based default of `1` and its `0` = no-header meaning. Do not expand the bullet
   further — the full option semantics live in the agent reference.
2. *Make the Lua-only status visible where people look.* The design-decision bullet already says
   these two names have no counterpart in any other layer, but the "Representative Cross-Layer
   Examples" table is the index a reader scans for a method's siblings, and it has no row at all.
   Add one row — category "CSV file read", `N/A` in the C++, C API, Julia and Dart columns, and
   `db:read_csv()` / `db:read_csv_stream()` in the Lua column — so the absence reads as deliberate
   rather than as a gap someone should fill. Do not add a row to the binary subsystem table.

**`src/CLAUDE.md` and `tests/CLAUDE.md` — check, do not edit.** Phase 3 changes nothing under `src/`
or `tests/`; both files already document `csv_read.{h,cpp}`, `make_format`, `resolve_sandboxed_path`,
`tests/fixtures/` and the release-preset trap. Confirm neither carries a claim this phase falsified,
and if one does, fix that claim and say so in the SUMMARY. Otherwise leave them untouched — a
duplicate entry costs a future reader more than it pays.

**`CHANGELOG.md` — one entry, under `### Changed` in the existing unreleased section.** It is about
the **agent reference**, not the reader: the shipped `LUA_DB_API_REFERENCE` used to state only that
the Lua sandbox has no `io`, which led models to transcribe data files into their scripts as
literals; it now points at `db:read_csv` / `db:read_csv_stream` at that exact statement and carries a
worked example over two real, dirty files including the `tonumber`/`gsub` parenthesis trap. Do not
restate `db:read_csv`'s or `header_row`'s features — phase 2 already logged both under `### Added`
in the same section.

**Two version facts to check and report, not to act on.** Run `uv run python
scripts/assert_version.py` (plain `python`/`python3` are not on PATH here) to confirm all five
manifests still agree; this milestone is purely additive so no bump is warranted. Separately: the
unreleased section is headed `## [0.10.4] — unreleased`, but the manifests are at `0.10.6` and tags
`v0.10.4`, `v0.10.5`, `v0.10.6` all exist — so that heading is stale and spans three tagged
releases. Renaming it is a release-ritual decision the root `CLAUDE.md` explicitly calls unsettled,
so **do not rename it and do not bump anything**. Add the entry under the existing heading and flag
the mismatch prominently in the SUMMARY for the user to decide.
  </action>
  <verify>
    <automated>uv run python scripts/assert_version.py &amp;&amp; grep -c "header_row" CLAUDE.md &amp;&amp; grep -c "read_csv" bindings/js/CLAUDE.md &amp;&amp; git diff --stat CLAUDE.md bindings/js/CLAUDE.md CHANGELOG.md</automated>
  </verify>
  <done>
`bindings/js/CLAUDE.md` records that the worked example is hand-verified against the fixtures and
unguarded by CI. Root `CLAUDE.md` names both `read_csv` options and carries a cross-layer row making
the Lua-only status visible. `CHANGELOG.md`'s unreleased section has one new entry about the
reference change and no restatement of phase 2's. All five manifests agree at one version, and the
stale unreleased heading is flagged rather than changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: The Release gate over the finished tree</name>
  <files>(no files modified — verification only)</files>
  <action>
TEST-05 is already marked `[x]` in `REQUIREMENTS.md`: phase 2's plan 02-04 ran it (291/291
`LuaRunner*` in Debug and Release) and phase 2's verifier re-ran it in a fresh tree (293/293). Per
D-32 this run is the **final release gate over the finished tree**, not a rediscovery — which is why
it sits last, after the documentation changes have landed.

Configure explicitly. Never use the plain `release` preset: it sets `QUIVER_BUILD_TESTS=OFF`, so it
would build no test binaries and report success while testing nothing. The flags are
`-DCMAKE_BUILD_TYPE=Release -DQUIVER_BUILD_TESTS=ON -DQUIVER_BUILD_C_API=ON` into a `build-release`
tree, and the full suites are `quiver_tests.exe` and `quiver_c_tests.exe` — the whole suite, not a
`LuaRunner*` filter, because success criterion 3 is about the whole suite in Release. Allow a long
first build: `Dependencies.cmake` fetches and compiles sqlite3, lua, sol2, tomlplusplus, spdlog,
rapidcsv, argparse and googletest from scratch.

A failure here is information, not an obstacle to route around: this phase changed only prose, so a
Release-only failure is either a pre-existing defect or a `SOL_SAFE_GETTER`-off marshalling bug that
Debug hides — exactly what this gate exists to surface. Stop and report it with the failing test
names; do not relax a test and do not "fix" it inside a documentation phase.

`build-release/` is not gitignored, so the run removes it on success (the verify command's last
step). On failure the tree stays in place for diagnosis — remove it by hand once the failure is
recorded.
  </action>
  <verify>
    <automated>./build/bin/quiver_tests.exe &amp;&amp; cmake -S . -B build-release -G Ninja -DCMAKE_BUILD_TYPE=Release -DQUIVER_BUILD_TESTS=ON -DQUIVER_BUILD_C_API=ON &amp;&amp; cmake --build build-release --config Release &amp;&amp; ./build-release/bin/quiver_tests.exe &amp;&amp; ./build-release/bin/quiver_c_tests.exe &amp;&amp; rm -rf build-release</automated>
  </verify>
  <done>
Every test in `quiver_tests` and `quiver_c_tests` passes in a Release build that was configured with
tests ON, the Debug suite still passes, and the `build-release` tree is gone.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> future maintainer | A stale claim in `CLAUDE.md` is acted on as if true. |
| Release build configuration -> the release gate | The wrong configure flags produce a green run that tested nothing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Spoofing | the `release` CMake preset | high | mitigate | The gate configures explicitly with `-DQUIVER_BUILD_TESTS=ON`; the preset sets it OFF and would report success having built no test binary. Task 2 names the flags and runs both test executables by path. |
| T-03-05 | Tampering | root `CLAUDE.md` option claim | medium | mitigate | The stale "`separator` is the only option today" sentence is corrected to name `header_row`, which shipped in phase 2. |
| T-03-06 | Repudiation | `CHANGELOG.md` unreleased heading | low | accept | The `[0.10.4] — unreleased` heading disagrees with the `0.10.6` manifests and three existing tags. Resolving it is a release-ritual decision the root `CLAUDE.md` calls unsettled; this plan flags it for the user rather than deciding it. |

No package-manager installs occur in this plan; no package-legitimacy gate applies.
</threat_model>

<verification>
- `uv run python scripts/assert_version.py` — all five manifests agree; no bump performed.
- `./build/bin/quiver_tests.exe` — the Debug suite still passes.
- `cmake -S . -B build-release -G Ninja -DCMAKE_BUILD_TYPE=Release -DQUIVER_BUILD_TESTS=ON -DQUIVER_BUILD_C_API=ON` then `cmake --build build-release --config Release` then both test executables — all pass.
- `build-release/` removed.
- `git diff` shows changes confined to `CLAUDE.md`, `bindings/js/CLAUDE.md` and `CHANGELOG.md`.
</verification>

<success_criteria>
- DOC-04: the `CLAUDE.md` nearest each change describes the new surface, and `CHANGELOG.md` carries
  an entry under the current unreleased version.
- TEST-05: the full test suite passes in a Release build, not only Debug.
- No production code changed anywhere in the phase.
</success_criteria>

<output>
Create `.planning/phases/03-the-agent-reads-instead-of-transcribing/03-02-SUMMARY.md` when done.
Record in it: the Release test counts for both executables, and — prominently, for the user to
decide — the `CHANGELOG.md` unreleased-heading mismatch (`[0.10.4]` against `0.10.6` manifests and
existing `v0.10.4`/`v0.10.5`/`v0.10.6` tags).
</output>