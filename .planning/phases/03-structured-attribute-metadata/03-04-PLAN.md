---
phase: 03-structured-attribute-metadata
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - bindings/dart/lib/src/ffi/bindings.dart
  - bindings/dart/lib/src/ffi/library_loader.dart
  - bindings/dart/lib/src/metadata.dart
  - bindings/dart/lib/src/database_metadata.dart
  - bindings/dart/lib/quiverdb.dart
  - bindings/dart/test/ui_metadata_test.dart
  - bindings/dart/test/struct_sizes_test.dart
autonomous: true
requirements: [META-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A Dart caller reads a scalar attribute's UI record and both vocabulary surfaces (ROADMAP criterion 1 and 2, Dart)."
    - "An unknown column and an unknown vocabulary name each surface the Pattern 2 message unchanged through Dart's exception (ROADMAP criterion 2)."
    - "Dart's load-time struct-size gate checks five structs, and deleting its call site provably turns a test red."
    - "bindings.dart was hand-edited, not regenerated — ffigen was not run."
  artifacts:
    - bindings/dart/test/ui_metadata_test.dart
  key_links:
    - "A full ffigen regen flips enums and breaks Hub — bindings.dart is hand-edited by standing milestone constraint (02-CONTEXT D-16)."
    - "The suite must clear .dart_tool/hooks_runner/ and .dart_tool/lib/ or it silently runs the previous native layout against the new Dart constants (02-CONTEXT D-15); test.bat already does this."
    - "In Phase 2 a sibling test that called the gate function directly repopulated the evidence record and masked the wiring-evidence test — ordering the evidence test first in its file is what un-masks it."
---

<objective>
Bind the three UI-metadata getters in Dart and add `quiver_ui_metadata_t` as the fifth struct in
Dart's load-time size gate — by hand-editing `bindings.dart`, never by regenerating it.

Purpose: Dart is the binding with no generator safety net and the one whose Phase 2 gate test was
silently masked by test ordering. Both hazards get an explicit countermeasure here.

Output: `UiMetadata` / `UiEnumEntry`, `getAttributeUiMetadata`, `listUiVocabularies`,
`getUiVocabulary`, the fifth gate entry, and `bindings/dart/test/ui_metadata_test.dart`.
</objective>

<user_story>
**As a** Hub user browsing a PSR study database,
**I want to** read an attribute's display label, unit and enum vocabulary as typed Dart values,
**so that** the UI shows what a code means instead of the code.
</user_story>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-structured-attribute-metadata/03-CONTEXT.md
@.planning/phases/03-structured-attribute-metadata/03-PATTERNS.md
@.planning/phases/03-structured-attribute-metadata/03-02-SUMMARY.md
@.planning/phases/02-config-path-locale-and-struct-size-safety/02-CONTEXT.md
@bindings/dart/CLAUDE.md
</context>

<corrections_carried>
**Correction 2 (Lua is not gated by the C-struct freeze).** Plan 03-06 runs in this same wave, not
behind it — `LuaRunner` calls the C++ getters directly and never decodes `quiver_ui_metadata_t`.

**Correction 3 (no new C header).** The C surface lives inside the existing
`include/quiver/c/database.h`, so `bindings/dart/pubspec.yaml`'s ffigen block — which lists the same
five headers **twice**, under `headers.entry-points` and `headers.include-directives` — needs no
edit and must stay unchanged. 03-CONTEXT D-43 budgeted only one generator-config line for a new
header and missed both of Dart's lists; declaring inside the existing header removes the question.
</corrections_carried>

<tasks>

<task type="auto">
  <name>Task 1: Hand-edit bindings.dart with the frozen struct and the six new symbols</name>
  <files>bindings/dart/lib/src/ffi/bindings.dart</files>
  <read_first>
    - bindings/dart/lib/src/ffi/bindings.dart (the existing `quiver_scalar_metadata_t` `final class ... extends Struct` declaration, its `@Int32()` / `external Pointer<Char>` field annotations, and the lookup/asFunction pair for `quiver_database_get_scalar_metadata` and `quiver_scalar_metadata_sizeof`)
    - include/quiver/c/database.h (the frozen `quiver_ui_metadata_t` block and the six function signatures, as left by 03-01 and 03-02)
    - bindings/dart/CLAUDE.md (hand-edit discipline; the Arena marshalling idiom)
    - .planning/phases/02-config-path-locale-and-struct-size-safety/02-CONTEXT.md D-15 and D-16
  </read_first>
  <action>
Hand-edit `bindings/dart/lib/src/ffi/bindings.dart`. Do not run ffigen and do not run
`bindings/dart/generator/generator.bat` — a full regen flips enum representations and breaks Hub,
which is a standing milestone constraint (02-CONTEXT D-16), not a preference.

Add `quiver_ui_metadata_t` as a `final class ... extends Struct` beside `quiver_scalar_metadata_t`,
declaring the nine fields in the frozen order — `label`, `tooltip`, `unit`, `format`, `icon`,
`vocabulary` as `external Pointer<Char>`; `display_order` as `@Int64() external int`; `configured`
and `hidden` as `@Int32() external int`. Dart computes the layout from the declaration order, so the
order is load-bearing: any reordering silently shifts every offset. Add a comment beside the class
naming the frozen offsets 0/8/16/24/32/40/48/56/60 and stating that `sizeOf<quiver_ui_metadata_t>()`
must equal 64.

Add the six lookup/`asFunction` pairs, copying the shape of the existing
`quiver_database_get_scalar_metadata` and `quiver_scalar_metadata_sizeof` entries:
`quiver_ui_metadata_sizeof`, `quiver_database_get_attribute_ui_metadata`,
`quiver_database_free_ui_metadata`, `quiver_database_list_ui_vocabularies`,
`quiver_database_get_ui_vocabulary`, `quiver_database_free_ui_vocabulary`. Match the native
signatures exactly, including `Size` for `size_t` out-parameters and `Pointer<Int64>` /
`Pointer<Pointer<Char>>` for the vocabulary arrays.

Change nothing about `quiver_scalar_metadata_t` or `quiver_group_metadata_t` (D-16, ROADMAP
criterion 4).
  </action>
  <verify>
    <automated>cd bindings/dart && dart analyze</automated>
  </verify>
  <acceptance_criteria>
    - `dart analyze` in `bindings/dart` reports no new issues.
    - `bindings.dart` declares `quiver_ui_metadata_t` with exactly nine fields in the order label, tooltip, unit, format, icon, vocabulary, display_order, configured, hidden.
    - `bindings.dart` declares all six new functions.
    - `git diff bindings/dart/lib/src/ffi/bindings.dart` shows no change to `quiver_scalar_metadata_t` or `quiver_group_metadata_t`.
    - `git diff --stat bindings/dart/pubspec.yaml` is empty — ffigen was not run and its config needed no change.
  </acceptance_criteria>
  <done>Dart's FFI layer carries the frozen struct and all six symbols, hand-edited.</done>
</task>

<task type="auto">
  <name>Task 2: The fifth gate entry, with the wiring-evidence test ordered first and the mutation actually run</name>
  <files>bindings/dart/lib/src/ffi/library_loader.dart, bindings/dart/test/struct_sizes_test.dart</files>
  <read_first>
    - bindings/dart/lib/src/ffi/library_loader.dart lines 60-115 (the four `checkStructSize` blocks, `callNativeSizeof`, `_checkedStructs`, `_structSizesChecked` memoization, and the call site that runs the gate)
    - bindings/dart/test/struct_sizes_test.dart lines 1-60 (the current test order — note which test runs first and which one calls the gate function directly)
    - .planning/phases/02-config-path-locale-and-struct-size-safety/02-CONTEXT.md D-15
    - bindings/dart/test/test.bat (the `.dart_tool/hooks_runner/` and `.dart_tool/lib/` clearing lines — CRLF file, do not let a unix tool rewrite it)
  </read_first>
  <action>
Append the fifth `checkStructSize` block for `quiver_ui_metadata_t` to `library_loader.dart`'s gate,
**after** the `quiver_csv_options_t` block and with no reordering of the existing four (D-40),
followed by the matching `_checkedStructs.add('quiver_ui_metadata_t')`. Use
`sizeOf<quiver_ui_metadata_t>()` as the expected value and
`callNativeSizeof('quiver_ui_metadata_sizeof', b.quiver_ui_metadata_sizeof)` as the native value,
exactly matching the four blocks above it. Leave the `_structSizesChecked` memoization flag alone —
it is what keeps the five native calls to once per isolate.

In `bindings/dart/test/struct_sizes_test.dart`:
- extend the happy-path test with the new struct's 64-byte assertion, keeping 24 / 56 / 32 / 56 for
  the four existing ones;
- extend the `_checkedStructs` wiring-evidence assertion to the five-element list, with
  `'quiver_ui_metadata_t'` last, and rename it so its title states five;
- **ensure the wiring-evidence test is the first test declared in the file.** In Phase 2 a sibling
  test that called the gate function directly repopulated the evidence record before the evidence
  test ran, so the evidence test passed even when the real call site was unwired. If the file's
  current order has already been corrected, leave it; if any test declared above the evidence test
  calls the gate function directly, move the evidence test above it and leave a comment naming the
  masking failure so the order is not casually reshuffled later.

Then **run the mutation check, do not merely write a test that would catch it.** Temporarily comment
out the `_assertStructSizes(...)` call site in `library_loader.dart`, run
`bindings/dart/test/test.bat`, and confirm the wiring-evidence test fails. Restore the call site and
confirm the suite is green again. Report both results in the plan summary — an evidence test whose
mutation was never run is exactly the shape of the defect this countermeasure exists to prevent.
  </action>
  <verify>
    <automated>bindings/dart/test/test.bat</automated>
  </verify>
  <acceptance_criteria>
    - `library_loader.dart`'s gate contains exactly five `checkStructSize` calls, with `quiver_ui_metadata_t` last and the first four unmoved.
    - `struct_sizes_test.dart` asserts `_checkedStructs` equals a 5-element list and asserts `sizeOf<quiver_ui_metadata_t>() == 64`.
    - The wiring-evidence test is the first test declared in `struct_sizes_test.dart`, and no test declared above it calls the gate function directly.
    - **The mutation check was RUN, not only written:** the summary records that commenting out the `_assertStructSizes` call site made the wiring-evidence test fail, and that restoring it made the suite green. A summary that only states the test exists does not satisfy this criterion.
    - `bindings/dart/test/test.bat` passes in full, having cleared `.dart_tool/hooks_runner/` and `.dart_tool/lib/` first.
  </acceptance_criteria>
  <done>Dart's gate checks five structs, and its wiring has been proven observable by an executed mutation rather than by inspection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Dart's three getters and their tests</name>
  <files>bindings/dart/lib/src/metadata.dart, bindings/dart/lib/src/database_metadata.dart, bindings/dart/lib/quiverdb.dart, bindings/dart/test/ui_metadata_test.dart</files>
  <read_first>
    - bindings/dart/lib/src/metadata.dart (the `ScalarMetadata` class and its `.fromNative(...)` constructor)
    - bindings/dart/lib/src/database_metadata.dart lines 1-40 (`getScalarMetadata` — `Arena` + `arena<T>()` + `check(...)` + `.fromNative(ref)` + free + `finally { arena.releaseAll() }`)
    - bindings/dart/lib/src/database_read.dart lines 1340-1385 (`readTimeSeriesFiles` — the paired out-pointer + manual zip + combined free shape)
    - bindings/dart/lib/quiverdb.dart (the export list)
    - bindings/dart/lib/src/database.dart line 235 (`hasUiConfig` — the established camelCase spelling for the `ui` segment)
    - bindings/dart/test/metadata_test.dart and bindings/dart/test/database_ui_options_test.dart (test shape, and how a `tests/schemas/ui/` fixture is opened from Dart)
    - bindings/dart/CLAUDE.md
  </read_first>
  <behavior>
    - `db.getAttributeUiMetadata('Storage', 'has_commitment')` returns `configured == true`, `label == 'Has Commitment'`, `vocabulary == 'bool'`, `displayOrder == -1`.
    - `db.getAttributeUiMetadata('Storage', 'max_generation')` returns `unit == 'MW'`, and `db.getAttributeUiMetadata('Storage', 'internal_code')` returns `hidden == true` with `label == 'Internal Code'`. These two are the offset-distinguishing reads: `enum_basic` declares no `tooltip`, `format` or `icon`, so on the `has_commitment` record alone those three slots and `unit` are all `''` and a transposition among offsets 8/16/24/32 reads identically — as does one between `configured`@56 and `hidden`@60. Dart derives its layout from declaration order alone, so this is the only runtime check on that order.
    - `db.getAttributeUiMetadata('Storage', 'notes')` returns `configured == true` with `label == ''`.
    - A real-but-unconfigured column returns `configured == false` and throws nothing.
    - An absent column throws carrying exactly `Scalar attribute not found: 'no_such_column' in collection 'Storage'`.
    - `db.listUiVocabularies()` returns a `List<String>` holding the fixture's declared names. `enum_basic` declares one, so this is a shape assertion; the ordering guarantee is proven once, in 03-01 Task 3's three-name scratch sidecar.
    - `db.getUiVocabulary('bool')` returns two `UiEnumEntry` values `(0, 'Disabled')` and `(1, 'Enabled')` in that order.
    - `db.getUiVocabulary('no_such_vocabulary')` throws carrying exactly `Vocabulary not found: 'no_such_vocabulary'`.
  </behavior>
  <action>
Add two classes to `bindings/dart/lib/src/metadata.dart`, beside `ScalarMetadata`: `UiEnumEntry`
with `final int code` and `final String label`; and `UiMetadata` with `final bool configured`,
`final String label`, `tooltip`, `unit`, `format`, `icon`, `vocabulary`, `final bool hidden`,
`final int displayOrder`. Give `UiMetadata` a `.fromNative(quiver_ui_metadata_t native)` constructor
mirroring `ScalarMetadata.fromNative`. Every text field is a non-nullable `String`, never `String?` —
D-13 makes empty string the spelling of absence on this boundary and the C converter allocates even
an empty field with `quiver::string::new_c_str`, so no pointer is ever null. That is a deliberate
departure from
`ScalarMetadata.defaultValue`'s nullable handling, where a null pointer genuinely means "no
default"; note the reason in a comment.

Add the three methods to the `DatabaseMetadata` extension in
`bindings/dart/lib/src/database_metadata.dart`, named `getAttributeUiMetadata`,
`listUiVocabularies` and `getUiVocabulary` — `Ui` cased as in the existing `hasUiConfig`. Each
begins with `_ensureNotClosed()`, allocates through a local `Arena`, and releases in a `finally`,
matching `getScalarMetadata`. `getUiVocabulary` copies `readTimeSeriesFiles`'s paired-pointer shape:
one `Pointer<Int64>` out, one `Pointer<Pointer<Char>>` out, one `Pointer<Size>` count, decoded into
`List<UiEnumEntry>` by index, then freed with the single
`quiver_database_free_ui_vocabulary(codes, labels, count)` call — never the two generic frees, which
is the leak D-37 exists to prevent. `listUiVocabularies` frees with the existing
`quiver_database_free_string_array`. Keep the per-method boilerplate expanded; collapsing it into
closure-parameterized helpers is explicitly not this codebase's style.

Export `UiMetadata` and `UiEnumEntry` from `bindings/dart/lib/quiverdb.dart`, appended to the
existing `export 'src/metadata.dart' show ScalarMetadata, GroupMetadata;` line.

Create `bindings/dart/test/ui_metadata_test.dart` covering the eight behaviors above against
`tests/schemas/ui/enum_basic`, referenced from `tests/schemas/` and never copied (CORPUS-03),
following the fixture-opening idiom in `database_ui_options_test.dart`. Both error tests assert the
exact message text.
  </action>
  <verify>
    <automated>bindings/dart/test/test.bat</automated>
  </verify>
  <acceptance_criteria>
    - `bindings/dart/test/test.bat` passes in full.
    - `ui_metadata_test.dart` contains at least 8 tests covering every behavior listed above, including `max_generation` → `unit == 'MW'` and `internal_code` → `hidden == true` — four of the nine offsets are otherwise never read at a distinguishing value.
    - The two error tests assert the exact strings `Scalar attribute not found: 'no_such_column' in collection 'Storage'` and `Vocabulary not found: 'no_such_vocabulary'`.
    - All six text fields of Dart's `UiMetadata` class are declared with the non-nullable `final String` type.
    - `getUiVocabulary`'s body contains exactly one free call, and it is `quiver_database_free_ui_vocabulary` — one call releases both arrays (D-37).
    - `bindings/dart/lib/quiverdb.dart` exports both `UiMetadata` and `UiEnumEntry`.
    - `dart analyze` and `dart format --output=none --set-exit-if-changed .` both clean.
  </acceptance_criteria>
  <done>Dart reads all three getters with the core's error text intact and the dedicated free.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan delivers the rows marked **[03-04]**.

**Public C++** — `include/quiver/ui_metadata.h`, `quiver::UIMetadata`, `quiver::UIEnumEntry`, `Database::get_attribute_ui_metadata`, `Database::list_ui_vocabularies`, `Database::get_ui_vocabulary` [03-01]

**C API** — `quiver_ui_metadata_t`, `quiver_ui_metadata_sizeof()`, `quiver_database_get_attribute_ui_metadata()`, `quiver_database_free_ui_metadata()` [03-01]; `quiver_database_list_ui_vocabularies()`, `quiver_database_get_ui_vocabulary()`, `quiver_database_free_ui_vocabulary()` [03-02]

**Bindings**
- Python `UiMetadata`, `UiEnumEntry`, the three getters [03-01/03-02]
- Julia `UIMetadata`, `UIEnumEntry`, the three getters [03-03]
- Dart `UiMetadata`, `UiEnumEntry`, `getAttributeUiMetadata`, `listUiVocabularies`, `getUiVocabulary` — **[03-04]**
- JS `UiMetadata`, `UiEnumEntry`, `getAttributeUiMetadata`, `listUiVocabularies`, `getUiVocabulary`, `UI_METADATA_SIZE` [03-05]
- Lua `db:get_attribute_ui_metadata`, `db:list_ui_vocabularies`, `db:get_ui_vocabulary` [03-06]

**Tests** — `tests/test_database_ui_metadata.cpp` + `bindings/python/tests/test_database_ui_metadata.py` [03-01/03-02]; `tests/test_c_api_database_ui_metadata.cpp` [03-02]; `bindings/julia/test/test_database_ui_metadata.jl` [03-03]; `bindings/dart/test/ui_metadata_test.dart` — **[03-04]**; `bindings/js/test/database-ui-metadata.test.ts` [03-05]; `tests/test_lua_runner_ui_metadata.cpp` [03-06]. Struct-size gate tests four → five: Python [03-01], Julia [03-03], Dart — **[03-04]**, JS [03-05].
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dart ↔ `libquiver_c` | A hand-written `Struct` declaration whose field order alone determines every offset, plus two manually freed heap arrays. No generator and no compile-time cross-check with the C header. |
| Dart test runner ↔ `.dart_tool` build cache | A stale `hooks_runner`/`lib` cache runs the previous native layout against the new Dart constants, which is a passing suite over a mismatched ABI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Tampering | `quiver_ui_metadata_t` in `bindings.dart` | high | mitigate | Field order transcribed from the frozen header with the offsets recorded in a comment; `sizeOf<quiver_ui_metadata_t>() == 64` asserted in the suite; fifth entry in the load-time gate; `test.bat` clears both `.dart_tool` caches before running. |
| T-03-02b | Tampering | A gate that is written but unwired | high | mitigate | The wiring-evidence test is ordered first in its file so no sibling can repopulate the record, and Task 2 requires the mutation to be **executed** — call site commented out, test observed failing, call site restored. |
| T-03-03 | Denial of Service | `getUiVocabulary`'s two heap arrays | medium | mitigate | One `quiver_database_free_ui_vocabulary` call owns both; every allocation sits in an `Arena` released in a `finally`; the acceptance criterion forbids substituting the two generic frees. |
| T-03-01 | Information Disclosure | Dart's exception text | low | mitigate | Dart re-throws the core's message verbatim and crafts none of its own; both exact strings are asserted in Task 3. |

No package-manager install occurs in this plan, so no `T-03-SC` row applies.
</threat_model>

<verification>
- `bindings/dart/test/test.bat` green in full, with both `.dart_tool` caches cleared first.
- `dart analyze` and `dart format` clean.
- The executed mutation result is recorded in the plan summary.
- `git diff --stat bindings/julia bindings/js bindings/python src include tests` empty — this plan
  touches Dart only.
</verification>

<success_criteria>
Dart reads the UI record and both vocabulary surfaces with the core's error text intact; its
hand-edited FFI layer carries the frozen layout; its five-struct gate is proven wired by an executed
mutation.
</success_criteria>

<output>
Create `.planning/phases/03-structured-attribute-metadata/03-04-SUMMARY.md` when done
</output>
