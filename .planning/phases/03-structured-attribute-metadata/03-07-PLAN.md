---
phase: 03-structured-attribute-metadata
plan: 07
type: execute
wave: 4
depends_on: ["03-03", "03-04", "03-05", "03-06"]
files_modified:
  - CLAUDE.md
  - src/CLAUDE.md
  - src/c/CLAUDE.md
  - tests/CLAUDE.md
  - bindings/julia/CLAUDE.md
  - bindings/dart/CLAUDE.md
  - bindings/python/CLAUDE.md
  - bindings/js/CLAUDE.md
  - CHANGELOG.md
autonomous: false
requirements: [META-01, META-02, META-03, META-04, META-05, META-06]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The full suite is green across all six test suites plus the CLI smoke test."
    - "describe / describe_collection / summarize_collection output is byte-identical to before this phase — it added getters, never rendering."
    - "git diff over the whole phase shows include/quiver/attribute_metadata.h untouched and quiver_scalar_metadata_t / quiver_group_metadata_t unchanged in either direction (ROADMAP criterion 4)."
    - "All four struct-size gates assert five structs, and each binding's wiring evidence proves its gate ran."
    - "The documented per-binding divergence list carries D-38's Lua nil semantics as its sixth entry."
  artifacts:
    - CHANGELOG.md
  key_links:
    - "CHANGELOG.md heads a complete unreleased 0.11.0 section carrying Phase 1 and Phase 2; Phase 3 appends to that same section and opens no second heading."
    - "A gate test still asserting four structs is the single highest-signal indicator that the fifth struct reached a loader but not its gate."
---

<objective>
Close the phase: record what changed in every CLAUDE.md that owns a piece of it, append to the
unreleased CHANGELOG section, and run the cross-cutting proofs that no single binding plan could
run — the full suite, the byte-identical describe regression, and the criterion-4 diff.

Purpose: the phase's two structural guarantees — that the new struct is independent of the old ones,
and that rendering is untouched — are whole-repo properties. They can only be proven after every
plan has landed.

Output: updated CLAUDE.md files, a CHANGELOG entry under the existing 0.11.0 heading, and a
recorded release-dispatch decision.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-structured-attribute-metadata/03-CONTEXT.md
@.planning/phases/03-structured-attribute-metadata/03-01-SUMMARY.md
@.planning/phases/03-structured-attribute-metadata/03-02-SUMMARY.md
@.planning/phases/03-structured-attribute-metadata/03-03-SUMMARY.md
@.planning/phases/03-structured-attribute-metadata/03-04-SUMMARY.md
@.planning/phases/03-structured-attribute-metadata/03-05-SUMMARY.md
@.planning/phases/03-structured-attribute-metadata/03-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the phase in every CLAUDE.md that owns a piece of it</name>
  <files>CLAUDE.md, src/CLAUDE.md, src/c/CLAUDE.md, tests/CLAUDE.md, bindings/julia/CLAUDE.md, bindings/dart/CLAUDE.md, bindings/python/CLAUDE.md, bindings/js/CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (the `## Design Decisions` list, the `## Core API` method list, the two cross-layer naming tables, and the per-binding divergence entries inside the boolean-wrappers and Lua-whole-group-readers decisions)
    - src/CLAUDE.md (the LuaRunner section and the UI-config notes)
    - src/c/CLAUDE.md lines 95-115 (the promoted "every hand-allocated struct joins the size gate by default" rule, and the doc-drift 03-CONTEXT flagged: the claim that native `static_assert`s pin 56 and 32, which was false before this phase)
    - tests/CLAUDE.md (the suite breakdown and the shared-fixture rule)
    - each binding's CLAUDE.md (the struct-size gate notes and the per-binding decoder conventions)
    - all six plan summaries from this phase
  </read_first>
  <action>
Update each CLAUDE.md nearest to what changed. Keep every edit factual and short — these are
reference documents, not a changelog.

**Root `CLAUDE.md`.** Add a `## Design Decisions` entry for the public UI-metadata surface: that
`UIMetadata` / `UIEnumEntry` moved from the private `src/ui_config.h` to the public
`include/quiver/ui_metadata.h` as a header move rather than a redesign (D-30); that `UIConfigSet`
stays private; that one record answers for collection, attribute and group so Phase 4 adds getters
and zero structs (D-09, D-31); that `format` carries one collapsed string and the three non-winning
keys of a 4-key TOML table are unreachable through any Quiver surface (D-32); that the getter
validates against the live SQL schema and throws Pattern 2 on an unknown column while returning a
default-constructed record for a real-but-unconfigured one (D-36); and that the vocabulary crosses
as two parallel arrays with one dedicated combined free rather than two generic ones (D-11, D-37).
Add the three methods to the `## Core API` `Database` method list, and add rows to both cross-layer
tables for `get_attribute_ui_metadata`, `list_ui_vocabularies` and `get_ui_vocabulary` with their
C API, Julia, Dart and Lua spellings.

Record **D-38 as the sixth entry on the documented per-binding divergence list**, alongside
JS-datetime, the binary/expression subsystems, the Lua whole-group readers, the boolean wrappers and
the Lua-only CSV reader: in Lua an empty text field on a UI record arrives as `nil`, not `""`,
because `""` is truthy in Lua and the idiomatic `if md.unit then` guard would otherwise fire for
every attribute in the database. State that `configured` is always present as the discriminator, and
that this is the one layer where D-13's empty-string-means-absent does not hold.

**`src/c/CLAUDE.md`.** Name `quiver_ui_metadata_t` as the fifth struct in the load-time gate, with
its 64-byte size, and record that this one **is** pinned by native `static_assert`s — one on `sizeof`
and one `offsetof` per field, in `src/c/database_metadata.cpp`. While there, fix the pre-existing
drift 03-CONTEXT flagged: the file currently claims native `static_assert`s pin
`quiver_scalar_metadata_t` at 56 and `quiver_group_metadata_t` at 32, and there are none — those two
sizes are enforced solely by the four runtime binding gates, and that is precisely why criterion 4's
"untouched" guarantee holds for them.

**`src/CLAUDE.md`.** Note the three new `db:` methods in the LuaRunner section, the D-38 nil
mapping, and the D-39 record-array shape with its JSON-encoder rationale.

**`tests/CLAUDE.md`.** Add the three new test files to the suite breakdown, and record that the
declared-but-empty vocabulary edge is covered by a scratch sidecar rather than a tracked fixture, so
a later reader does not go looking for a corpus directory that does not exist.

**Each binding's `CLAUDE.md`.** One line naming the three new methods and the fifth gate entry.
`bindings/dart/CLAUDE.md` additionally records that the wiring-evidence test must stay first in its
file; `bindings/js/CLAUDE.md` records that `UI_METADATA_SIZE` lives in `ffi-helpers.ts` for the same
cycle-avoidance reason as the other four size constants.
  </action>
  <verify>
    <automated>git diff --stat CLAUDE.md src/CLAUDE.md src/c/CLAUDE.md tests/CLAUDE.md bindings/julia/CLAUDE.md bindings/dart/CLAUDE.md bindings/python/CLAUDE.md bindings/js/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - All eight CLAUDE.md files show a non-empty diff.
    - Root `CLAUDE.md` lists all three new methods in the `## Core API` `Database` section and in both cross-layer naming tables.
    - Root `CLAUDE.md` records the Lua nil-for-empty divergence as an entry on the per-binding divergence list, naming `configured` as the discriminator.
    - `src/c/CLAUDE.md` names `quiver_ui_metadata_t` at 64 bytes as the fifth gate struct and no longer claims native `static_assert`s pin the 56 and 32 sizes.
    - `tests/CLAUDE.md` names all three new test files.
  </acceptance_criteria>
  <done>Every CLAUDE.md that owns part of this phase records it, and one pre-existing false claim in `src/c/CLAUDE.md` is corrected.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate — full suite, byte-identical describe, and the criterion-4 diff</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (the unreleased `## [0.11.0]` section and its compare-link chain; 02-13 reconciled the duplicate headings, so there is exactly one)
    - .planning/STATE.md `## Pending Todos` (the deferred release dispatch and the "no second heading" rule)
    - .planning/ROADMAP.md Phase 3 success criteria 1-5 — note that criteria 1 and 3 and the phase goal line were amended during planning (D-33, plus 03-CONTEXT P-01 and P-03); criterion 3 now reads "the winning key's string round-trips verbatim", so do not look for a four-key round-trip test and do not treat its absence as a gap
    - .planning/phases/03-structured-attribute-metadata/03-VALIDATION.md (the sampling contract and the four gate tests that must assert five)
    - tests/test_database_ui_golden.cpp (the golden describe baseline Phase 1 captured, pinned to `eol=lf`)
    - scripts/test-all.bat
  </read_first>
  <action>
Run the cross-cutting proofs no single binding plan could run, then record the result.

**Full suite.** `scripts/build-all.bat` then `scripts/test-all.bat` — all six suites plus the
`quiver_cli` smoke test. Record the pass counts per suite in the plan summary.

**Byte-identical describe (criterion from Phase 1 that this phase must not break).** Run
`./build/bin/quiver_tests.exe --gtest_filter='*Describe*'` and the golden-baseline suite. This phase
added getters and touched no renderer, so the output must be unchanged; confirm with
`git diff --stat src/database_describe.cpp`, which must be empty.

**Criterion 4 diff.** Produce, and paste into the plan summary, the output of
`git diff --stat master...HEAD -- include/quiver/attribute_metadata.h src/CMakeLists.txt` (must be
empty) and `git diff master...HEAD -- include/quiver/c/database.h` filtered to the
`quiver_scalar_metadata_t` and `quiver_group_metadata_t` blocks (must show no field change in either
direction, D-16). Also confirm `bindings/js/src/ffi-helpers.ts` gained exactly one constant and that
the existing 56 and 32 values are unchanged.

**The four gate tests assert five.** Grep each of `bindings/js/test/struct-sizes.test.ts`,
`bindings/julia/test/test_struct_sizes.jl`, `bindings/python/tests/test_struct_sizes.py` and
`bindings/dart/test/struct_sizes_test.dart` and confirm every one asserts a five-element checked-
struct list. 03-VALIDATION names a gate test still reading four as the single highest-signal
indicator that the fifth struct reached a loader but not its gate.

**CHANGELOG.** Append the Phase 3 entries to the **existing** unreleased `## [0.11.0]` section — do
not open a second heading and do not add a compare link; 02-13 already reconciled that chain, and
STATE.md's pending-todo records the one-section rule. Under `Added`, name the public header
`include/quiver/ui_metadata.h`, the public `UIMetadata` / `UIEnumEntry` types, the three `Database`
getters, `quiver_ui_metadata_t` with its 64-byte size, `quiver_ui_metadata_sizeof`, the three
`quiver_database_*` functions and the two frees, the equivalent methods in Julia, Dart, Python, JS
and Lua, and the three new `db:` Lua names. Note that the fifth struct joined every binding's
load-time size gate. There is no `BREAKING` entry: this phase adds symbols and changes no existing
one.
  </action>
  <verify>
    <automated>scripts/test-all.bat</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/test-all.bat` passes: all six suites plus the CLI smoke test, with per-suite counts recorded in the summary.
    - `./build/bin/quiver_tests.exe --gtest_filter='*Describe*'` green and `git diff --stat src/database_describe.cpp` empty.
    - `git diff --stat master...HEAD -- include/quiver/attribute_metadata.h src/CMakeLists.txt` is empty, pasted into the summary.
    - The diff of `include/quiver/c/database.h` shows no change to any field of `quiver_scalar_metadata_t` or `quiver_group_metadata_t`.
    - All four struct-size test files assert a five-element checked-struct list; the grep output is pasted into the summary.
    - `CHANGELOG.md` has exactly one `## [0.11.0]` heading and the Phase 3 entries are inside it.
    - `scripts/assert_version.py` reports all five manifests in agreement (no bump performed in this task).
    - The summary confirms the three ROADMAP amendments landed during planning are present in `.planning/ROADMAP.md`: criterion 3's "winning key" wording (D-33), criterion 1's "the two getters together" wording (the D-10 drift), and the corrected goal line (P-01 / P-03). Criterion 3 is the one that would otherwise be scored against a test that cannot pass.
    - The summary confirms no plural collection-wide getter was added in any layer (D-34 defers it).
  </acceptance_criteria>
  <done>Every cross-cutting phase guarantee is proven with pasted evidence, and the CHANGELOG records the phase under the single unreleased heading.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Release dispatch — bump now, or hold again for Phase 4/5</name>
  <decision>
Whether to dispatch the **Bump Version** workflow now that Phase 3 has landed, or hold the
unreleased `## [0.11.0]` section until the milestone's remaining native phases land.
  </decision>
  <context>
STATE.md's pending todo records the Phase-2 resolution: the dispatch was held for Phase 3 because
the ROADMAP's Phase 2 note says Phases 2 and 3 "should ship in the same release", and it was to be
dispatched exactly once with `part=minor` — never `part=patch` first, which would tag a spurious
version shipping only Phase 1.

What has changed since that was recorded: Phase 4 (collection and group metadata) and Phase 5
(`validate_ui_config()`) are also native changes, and ROADMAP Phase 5 criterion 4 assigns the full
release ritual — `assert_version.py`, `publish-s3` → tag → Julia/Python/JS in parallel → Dart
published by hand — to Phase 5. Dispatching now means this milestone ships two or three native
releases rather than one, and every one of them needs the manual Dart leg, which has no CI job on
any OS.

All five manifests currently agree; `CHANGELOG.md` heads a single complete unreleased `## [0.11.0]`
carrying Phases 1, 2 and 3. Either choice leaves the repo consistent.
  </context>
  <options>
    <option id="hold">
      <name>Hold — keep 0.11.0 unreleased until Phase 5's release ritual (recommended)</name>
      <pros>One release for the milestone, one manual Dart publish, one tag; matches ROADMAP Phase 5
      criterion 4, which owns the ritual. Phase 4 appends to the same unreleased section exactly as
      Phase 3 just did.</pros>
      <cons>Contradicts STATE.md's recorded "dispatched after Phase 3" plan, which must be amended
      rather than silently ignored. claw cannot consume any of this from a published artifact until
      the milestone ships.</cons>
    </option>
    <option id="dispatch">
      <name>Dispatch Bump Version with part=minor now</name>
      <pros>Honours the plan recorded in STATE.md; gets the structured getters into published Julia,
      Python, JS and S3 natives while Phases 4 and 5 are still being built, which is the only way
      claw can start integrating before the milestone ends.</pros>
      <cons>Commits the milestone to two or three native releases, each needing the hand-published,
      un-automated Dart leg. Phase 4 then opens a fresh unreleased section.</cons>
    </option>
  </options>
  <read_first>
    - .planning/STATE.md `## Pending Todos` (the recorded deferral and its exact wording)
    - .planning/ROADMAP.md Phase 5 criterion 4 and its Coverage Note
    - CHANGELOG.md (the unreleased 0.11.0 section as left by Task 2)
    - .github/CLAUDE.md (the release flow and the Bump Version workflow)
  </read_first>
  <acceptance_criteria>
    - A selection of `hold` or `dispatch` is recorded in the plan summary with its rationale.
    - STATE.md's pending-todo wording is updated to match the selection — if `hold`, the todo now
      names Phase 5 rather than Phase 3; if `dispatch`, the todo is closed and the dispatched
      version recorded.
    - No manifest is hand-edited in either case; `scripts/assert_version.py` still reports agreement.
  </acceptance_criteria>
  <resume-signal>Select: hold or dispatch</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
The complete list, as delivered. This plan delivers documentation and the CHANGELOG entry; every
symbol below already exists by the time it runs.

**Public C++ header and types** — `include/quiver/ui_metadata.h`, `quiver::UIMetadata` (9 fields,
`QUIVER_API`), `quiver::UIEnumEntry` (`code`, `label`, `QUIVER_API`) [03-01]

**Public C++ getters** — `Database::get_attribute_ui_metadata(collection, attribute)`,
`Database::list_ui_vocabularies()`, `Database::get_ui_vocabulary(name)` [03-01]

**C API** — `quiver_ui_metadata_t` (64 bytes, hole-free), `quiver_ui_metadata_sizeof()`,
`quiver_database_get_attribute_ui_metadata()`, `quiver_database_free_ui_metadata()` [03-01];
`quiver_database_list_ui_vocabularies()`, `quiver_database_get_ui_vocabulary()`,
`quiver_database_free_ui_vocabulary(int64_t* codes, char** labels, size_t count)` [03-02]

**Bindings**
- Python `UiMetadata`, `UiEnumEntry`, `get_attribute_ui_metadata`, `list_ui_vocabularies`, `get_ui_vocabulary` [03-01/03-02]
- Julia `UIMetadata`, `UIEnumEntry`, `get_attribute_ui_metadata`, `list_ui_vocabularies`, `get_ui_vocabulary` [03-03]
- Dart `UiMetadata`, `UiEnumEntry`, `getAttributeUiMetadata`, `listUiVocabularies`, `getUiVocabulary` [03-04]
- JS `UiMetadata`, `UiEnumEntry`, `getAttributeUiMetadata`, `listUiVocabularies`, `getUiVocabulary`, `UI_METADATA_SIZE` [03-05]
- Lua `db:get_attribute_ui_metadata`, `db:list_ui_vocabularies`, `db:get_ui_vocabulary` [03-06]

**Tests** — `tests/test_database_ui_metadata.cpp` [03-01], `tests/test_c_api_database_ui_metadata.cpp`
[03-02], `bindings/python/tests/test_database_ui_metadata.py` [03-01/03-02],
`bindings/julia/test/test_database_ui_metadata.jl` [03-03],
`bindings/dart/test/ui_metadata_test.dart` [03-04],
`bindings/js/test/database-ui-metadata.test.ts` [03-05],
`tests/test_lua_runner_ui_metadata.cpp` [03-06]; the four struct-size test files updated from four
structs to five [03-01, 03-03, 03-04, 03-05].

**Documentation** — root `CLAUDE.md`, `src/CLAUDE.md`, `src/c/CLAUDE.md`, `tests/CLAUDE.md`, the
four binding `CLAUDE.md` files, `CHANGELOG.md`, and the `bindings/js/src/lua-api.ts` reference block
[03-06] — **[03-07]** for all but the last.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Published native artifact ↔ installed binding | A version bump publishes natives that every binding's load-time gate then checks; a bump dispatched against a repo whose manifests disagree ships a skewed set. |
| Documentation ↔ future maintainer | A CLAUDE.md claim that is false — such as the `static_assert` claim Task 1 corrects — is a wrong safety assumption acted on later. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Tampering | A gate that received the fifth struct in its loader but not in its test | high | mitigate | Task 2 greps all four struct-size test files and requires each to assert a five-element list; 03-VALIDATION names this as the highest-signal indicator of a half-wired gate. |
| T-03-04 | Tampering | Release skew — a bump dispatched with manifests disagreeing | medium | mitigate | `scripts/assert_version.py` is run before any dispatch and refuses to bump from a disagreeing state; no manifest is hand-edited in either branch of Task 3. |
| T-03-05 | Repudiation (documentation integrity) | `src/c/CLAUDE.md`'s false `static_assert` claim | low | mitigate | Task 1 corrects it and states what actually enforces the 56 and 32 sizes, so the next reader does not assume a compile-time guard that is not there. |

No package-manager install occurs in this plan, so no `T-03-SC` row applies.
</threat_model>

<verification>
- `scripts/test-all.bat` green, counts recorded.
- `./build/bin/quiver_tests.exe --gtest_filter='*Describe*'` green with `src/database_describe.cpp` unchanged.
- The criterion-4 diffs pasted into the summary and empty where required.
- All four struct-size test files assert five.
- `CHANGELOG.md` has exactly one `## [0.11.0]` heading.
- `scripts/assert_version.py` reports agreement.
</verification>

<success_criteria>
The phase's cross-cutting guarantees are proven with pasted evidence, every CLAUDE.md that owns a
piece of the phase records it, the CHANGELOG carries Phase 3 under the single unreleased heading,
and the release-dispatch question is answered and reflected in STATE.md.
</success_criteria>

<output>
Create `.planning/phases/03-structured-attribute-metadata/03-07-SUMMARY.md` when done
</output>
