---
phase: 05-ragged-rows-and-forgotten-closes
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/lua_runner.cpp
  - tests/test_lua_runner_write_csv.cpp
autonomous: true
requirements: [WRITE-06, TEST-11]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # --- goal-backward (ROADMAP Phase 5 criterion 4) ---
    - "A script that returns without calling `w:close()` leaves a complete, re-readable file: a SECOND `lua.run(...)` on the same, still-alive, never-destroyed `LuaRunner` reads the same path through `db:read_csv` and finds the header and every row the first script wrote (ROADMAP criterion 4)."
    - "The fix is exactly one `impl_->lua.collect_garbage()` call, made from the destructor of a single function-local RAII guard declared before `safe_script` so it covers the throw path and both return paths of `LuaRunner::run` (D-46, D-48) -- not three calls at three exits, and not a loop that collects until a counter stabilizes (RESEARCH.md Pitfall 1)."
    - "No warning, log line or other diagnostic is produced for the unclosed writer -- the observable difference is the flushed file alone (WRITE-06 is the flush, not the diagnostic)."
    # --- probe edge WRITE-06 / concurrency (resolved: explicit) ---
    - "EDGE WRITE-06/concurrency: a Lua script is single-threaded and `LuaRunner` borrows a raw `Database&`, so the guarantee is per-`run()`-return and nothing more -- no parallel-`run()` and no cross-thread contract is claimed. If a script throws mid-write, the guard still fires during stack unwinding (D-47), so the rows completed before the error are on disk and readable through `db:read_csv`; the file ends at the last completed record and is never a partially-written one."
    # --- probe edge TEST-11 / concurrency (resolved: explicit) ---
    - "EDGE TEST-11/concurrency: TEST-11's two `lua.run(...)` calls run sequentially on one live runner; the assertion is that the file was complete when the FIRST `run()` returned, observed by the SECOND `run()` reading it back. The runner is never destroyed, moved from, or reset between the two calls -- destroying it would pass on `Writer`'s own destructor and prove nothing (ROADMAP criterion 4's explicit trap)."
  artifacts:
    - "src/lua_runner.cpp -- `GcGuard`, a function-local RAII struct inside `LuaRunner::run` holding a `sol::state&`, whose destructor calls `collect_garbage()` once"
    - "tests/test_lua_runner_write_csv.cpp -- `LuaRunner_WriteCsv.UnclosedWriterIsFlushedWhenRunReturns` and `LuaRunner_WriteCsv.ScriptErrorMidWriteStillLeavesEarlierRowsReadable`"
    - ".planning/phases/05-ragged-rows-and-forgotten-closes/05-02-SUMMARY.md -- records the byte count ACTUALLY observed on the unflushed fixture file during the RED run (D-49)"
  key_links:
    - "`GcGuard` is declared BEFORE `auto result = impl_->lua.safe_script(...)`. Stack locals are destroyed in reverse declaration order, so the guard (first) runs AFTER `result` (second) is destroyed -- the `sol::protected_function_result`'s Lua stack reference is released before `collect_garbage()` runs. Reversing the two declarations would collect while a live stack reference still anchors the writer userdata."
    - "`CsvWriter`'s ownership is `std::unique_ptr` + `sol::no_constructor` + sol2's default `__gc` (LUA-11, Phase 4). The collection runs `CsvWriter`'s destructor, which destroys `csv_write::Writer`, which closes the `std::ofstream`, which flushes. Break any link in that chain and TEST-11 goes red again."
    - "TEST-11's fixture must stay small (a 1-2 name header, one short row). A payload big enough to spill `std::filebuf`'s internal buffer would put bytes on disk without the fix and make the RED accidental (RESEARCH.md Pitfall 2)."
  prohibitions:
    - "No warning, log line, counter or `weak_ptr` registry is added to detect the unclosed writer, and no `Database::log_warning` is introduced. That whole mechanism was evaluated and declined for the milestone; silently growing it back as an implementation detail of the flush is exactly the scope creep REQUIREMENTS.md § Out of Scope forbids."
    - "`collect_garbage()` is called exactly once per `run()`. No loop that collects until an object count stabilizes, and no duplicated call at each of `run()`'s three exit points -- a 'usually works' collection loop would make TEST-11 flaky in CI, which is worse than the bug."
    - "No test in this plan destroys, moves from, or resets the `LuaRunner` before asserting the file is complete. An assertion after destruction would be satisfied by `Writer`'s own destructor and would prove nothing about the flush."
    - "No correctness assertion reads the emitted file as raw text. The byte count D-49 requires is recorded as diagnostic evidence attached to the RED run's failure message only; the pass/fail decision is the `db:read_csv` round trip."
---

<objective>
Flush a writer the script never closed: one RAII guard in `LuaRunner::run` whose destructor calls
`sol::state::collect_garbage()` once, covering both returns and the throw path.

Purpose: `sol::state` is a member of `LuaRunner::Impl`, so a writer left open at `run()`'s return is
unreachable but uncollected — its `std::ofstream` buffer never flushes and the file the script
"wrote" is not there. WRITE-06 is the flush alone; the warning was declined.
Output: `GcGuard` in `src/lua_runner.cpp`, the two TEST-11 cases, and the recorded RED byte count.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ragged-rows-and-forgotten-closes/05-CONTEXT.md
@.planning/phases/05-ragged-rows-and-forgotten-closes/05-PATTERNS.md
@.planning/phases/05-ragged-rows-and-forgotten-closes/05-RESEARCH.md
@.planning/phases/05-ragged-rows-and-forgotten-closes/05-01-SUMMARY.md
@CLAUDE.md
@src/CLAUDE.md
@tests/CLAUDE.md
</context>

## Artifacts this phase produces

See the table in `05-01-PLAN.md` § "Artifacts this phase produces" — the canonical list for the
whole phase. This plan contributes `GcGuard` and the two TEST-11 cases. **No new files.**

<tasks>

<task type="auto">
  <name>Task 1: TEST-11 RED — write both cases, observe the failure, record the actual byte count</name>
  <files>tests/test_lua_runner_write_csv.cpp</files>
  <precondition>`05-01` is complete and `./build/bin/quiver_tests.exe` is green — so a failure in this task is attributable to the missing flush and not to an unfinished FMT-07 change.</precondition>
  <read_first>
    - `tests/test_lua_runner_write_csv.cpp` lines 1-105 (the `lp()` helper, `expect_prefixed_error`, the `LuaRunner_WriteCsv` fixture) and the first round-trip test after it, for the fixture conventions to reuse
    - `tests/test_lua_runner.h` lines 1-45 (the `LuaSandboxTest` fixture: `sandbox`, `db_path()`, `VALID_SCHEMA`)
    - `src/lua_runner.cpp` — `LuaRunner::run`'s current body (the `safe_script` call, the invalid-result throw, the zero-return early return, the JSON-encode return), unchanged at this point
    - `.planning/phases/05-ragged-rows-and-forgotten-closes/05-PATTERNS.md` § `tests/test_lua_runner_write_csv.cpp` — TEST-11, including its explicit **no analog exists** call-out for the two-`run()`-call shape
    - `.planning/phases/05-ragged-rows-and-forgotten-closes/05-RESEARCH.md` Q4 (the shape, the legality of a second `run()`, and the genuine-RED caveat) and Pitfall 2
    - `.planning/phases/05-ragged-rows-and-forgotten-closes/05-CONTEXT.md` D-47 and D-49
  </read_first>
  <action>
Write the two TEST-11 cases against UNMODIFIED `LuaRunner::run`, run them, and confirm they fail
for the right reason before any fix exists. This sequencing is a hard requirement (D-49): the
ROADMAP's "the file is zero bytes" claim is unverified, and a fixture large enough to spill
`std::filebuf`'s internal buffer would pass without the fix and hide a broken one.

There is no precedent in this suite for two separate `lua.run(...)` calls on one live `LuaRunner` —
every existing test does write, `close()` and read-back inside a single script. Write the sequencing
fresh from the neighbouring tests' fixture conventions (`LuaSandboxTest`,
`VALID_SCHEMA("basic.sql")`, `quiver::Database::from_schema(db_path(), schema)`,
`quiver::LuaRunner lua(db)`, `lp((sandbox / "...").string())`); do not pretend an analog exists.

Case one, the normal-return path: first `lua.run(...)` opens `db:write_csv` with a one- or two-name
header, calls `w:write_row` once with one short row, and returns with no `w:close()` call. Keep the
payload deliberately tiny. Then, WITHOUT destroying, moving from or reassigning `lua`, a second
`lua.run(...)` on the same object reads the same path through `db:read_csv` and asserts the header
and the row are both present with the exact cell values written.

Case two, the error path (D-47): one `lua.run(...)` opens the writer, writes one good row, then
raises a Lua error before any `w:close()`. Assert that the call throws. Then a second
`lua.run(...)` on the same still-alive runner reads the path back through `db:read_csv` and asserts
the row written before the error is present and readable.

For the RED evidence D-49 demands, attach the on-disk size to the failure message only — stream
`std::filesystem::file_size(path)` into the `<<` annotation of the assertion that the rows came
back, so a failing run prints the byte count actually observed and a passing run prints nothing.
This is diagnostic, not the assertion: the pass/fail decision stays the `db:read_csv` round trip.

Build, run only these two tests, and capture the observed byte count from the failure output. Record
that number verbatim in `05-02-SUMMARY.md` — the number observed, not the ROADMAP's claim. Do NOT
implement the fix in this task; leave the suite red.
  </action>
  <verify>
    <automated>cmake --build build --config Debug &amp;&amp; ./build/bin/quiver_tests.exe --gtest_filter=LuaRunner_WriteCsv.UnclosedWriterIsFlushedWhenRunReturns:LuaRunner_WriteCsv.ScriptErrorMidWriteStillLeavesEarlierRowsReadable; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `cmake --build build --config Debug` succeeds.
    - `./build/bin/quiver_tests.exe --gtest_filter=LuaRunner_WriteCsv.UnclosedWriterIsFlushedWhenRunReturns:LuaRunner_WriteCsv.ScriptErrorMidWriteStillLeavesEarlierRowsReadable` exits NON-ZERO with 2 tests run and 2 failed — the RED observation.
    - The failure output contains the on-disk byte count of the fixture file (the `file_size` annotation), and that literal number is written into `05-02-SUMMARY.md`.
    - `grep -c 'lua.run' ` over each new test body returns 2 — two separate calls, not one combined script.
    - No `GcGuard` and no `collect_garbage` token exists in `src/lua_runner.cpp` at the end of this task: `git diff --name-only` lists only `tests/test_lua_runner_write_csv.cpp`.
    - The rest of the suite is unaffected: `./build/bin/quiver_tests.exe --gtest_filter=-LuaRunner_WriteCsv.UnclosedWriterIsFlushedWhenRunReturns:-LuaRunner_WriteCsv.ScriptErrorMidWriteStillLeavesEarlierRowsReadable` exits 0.
  </acceptance_criteria>
  <done>Both TEST-11 cases exist, run, and fail against unmodified `LuaRunner::run`, and the byte count actually observed on the unflushed file is recorded in the plan summary rather than the ROADMAP's unverified claim repeated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The GcGuard — one collect_garbage() covering both returns and the throw path</name>
  <files>src/lua_runner.cpp</files>
  <behavior>
    - The two failing tests from task 1 both pass, unmodified — no assertion is relaxed, no fixture is enlarged, no `w:close()` is added to make them pass.
    - `LuaRunner_WriteCsv.UnclosedWriterIsFlushedWhenRunReturns`: after the first `run()` returns normally, the second `run()` on the same live runner reads the header and the row back through `db:read_csv`.
    - `LuaRunner_WriteCsv.ScriptErrorMidWriteStillLeavesEarlierRowsReadable`: after the first `run()` throws, the second `run()` on the same live runner reads the pre-error row back.
    - Every existing `LuaRunner*` test still passes — a `collect_garbage()` call at the end of `run()` must not disturb a subsequent script's state (RESEARCH.md Q1, probe case H).
  </behavior>
  <read_first>
    - `src/lua_runner.cpp` — `LuaRunner::run`'s body in full, and the `Impl` declaration showing `sol::state lua` as a member
    - `.planning/phases/05-ragged-rows-and-forgotten-closes/05-PATTERNS.md` § `src/lua_runner.cpp` — `LuaRunner::run` (WRITE-06), including its statement that no prior code in this repo drives Lua GC from a scope guard
    - `.planning/phases/05-ragged-rows-and-forgotten-closes/05-RESEARCH.md` Q1 (the EXECUTED probe: one call suffices for every reachability shape tested, including the error path and a second `run()` on the same state), Q5, and Pitfall 1
    - `.planning/phases/05-ragged-rows-and-forgotten-closes/05-CONTEXT.md` D-46, D-47, D-48
    - `src/CLAUDE.md` § LuaRunner — the implementation-conventions bullet list, for the RAII and commenting voice used in this file
  </read_first>
  <action>
Declare one function-local RAII struct at the very top of `LuaRunner::run`, before the
`auto result = impl_->lua.safe_script(...)` line, holding a reference to `impl_->lua`, with a
one-line destructor that calls `collect_garbage()` on it. Construct it immediately as a named local.
Leave every other line of `run` exactly as it is.

The declaration order is load-bearing and must be stated in a comment at the declaration: stack
locals are destroyed in reverse declaration order, so the guard (declared first) runs AFTER `result`
(declared second) is destroyed — the `sol::protected_function_result`'s Lua stack reference is
released before the collection runs. Declaring the guard after `result` would collect while a live
stack reference still anchors the `CsvWriter` userdata.

One guard, not three calls. Do not add a second `collect_garbage()`, and do not loop until an object
count stabilizes: RESEARCH.md Q1 compiled and executed a probe against this repo's own vendored
sol2/Lua build, with the project's real `SOL_SAFE_NUMERICS`/`SOL_SAFE_FUNCTION` flags and the exact
`unique_ptr`-returning-factory + `sol::no_constructor` ownership shape `CsvWriter` uses, and found
one call sufficient and deterministic for every reachability shape tested — bare local, nested in a
table, unbound, the error path, and a second `run()` on the same live state. Cite that in the
comment as executed evidence so a later reader does not "harden" it into a loop.

Add nothing else: no diagnostic, no logger call, no registry of live writers, no row counter, no
`is_open()`. WRITE-06 is the flush alone — the declined mechanism is enumerated by name in this
plan's first `must_haves.prohibitions` entry.

Re-run the two tests from task 1 and confirm they now pass unmodified, then run the whole suite.
  </action>
  <reversibility rating="reversible">One function-local struct and one named local in one function; reverting is a single-hunk revert with no interface change and no on-disk format consequence.</reversibility>
  <verify>
    <automated>cmake --build build --config Debug &amp;&amp; ./build/bin/quiver_tests.exe &amp;&amp; ./build/bin/quiver_c_tests.exe</automated>
  </verify>
  <acceptance_criteria>
    - `./build/bin/quiver_tests.exe --gtest_filter=LuaRunner_WriteCsv.UnclosedWriterIsFlushedWhenRunReturns:LuaRunner_WriteCsv.ScriptErrorMidWriteStillLeavesEarlierRowsReadable` exits 0 with 2 tests run — and `git diff tests/test_lua_runner_write_csv.cpp` is empty for this task, proving the tests passed without being altered.
    - `grep -c 'collect_garbage' src/lua_runner.cpp` returns exactly 1.
    - The guard precedes the script call: `awk '/GcGuard/{g=NR} /safe_script/{if(g && NR>g) print "ok"; exit}' src/lua_runner.cpp` prints `ok`.
    - `./build/bin/quiver_tests.exe` exits 0 for the whole suite (every `LuaRunner*` suite included), and `./build/bin/quiver_c_tests.exe` exits 0.
    - `git diff --name-only` for this task lists exactly `src/lua_runner.cpp`.
    - `grep -c 'log_warning\|weak_ptr' src/lua_runner.cpp` returns 0 — the declined diagnostic machinery was not reintroduced.
  </acceptance_criteria>
  <done>A writer left open at `run()`'s return is flushed on both the normal and the throwing exit, proven by two tests that were red before this task and pass unmodified after it, with exactly one `collect_garbage()` call in the file and the whole suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lua script -> C++ (`resolve_sandboxed_path`) | Unchanged by this plan — no new path is accepted and the sandbox choke point is not touched. |
| sol2 runtime -> host C++ (`LuaRunner::run`'s scope) | Where the collection is triggered. No untrusted data crosses here; the guard reads no script-controlled value. |

No new trust boundary, no new parsing surface, no network surface, and no new dependency
(`sol::state::collect_garbage()` is already-linked sol2 3.5.0 public API, already used elsewhere in
this file).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Denial of Service | `CsvWriter` lifetime across `LuaRunner::run` (`src/lua_runner.cpp`) | low | mitigate | A script that returns without `close()` leaves an unreachable-but-uncollected writer holding an OS file descriptor and an unflushed buffer; a host calling `run()` repeatedly accumulates them until the runner is destroyed, and every such file is incomplete on disk. Mitigated by this plan's single `collect_garbage()` at `run()`'s scope exit — no new resource-tracking machinery, since ownership is already `unique_ptr` + sol2 default `__gc` (LUA-11). |
| T-05-05 | Tampering | partially-written CSV after a script error | low | mitigate | Before this plan, a script erroring mid-write left a zero-length or truncated file that a downstream consumer could read as "no data" rather than "failed run". D-47's guard fires during unwinding, so the file ends at the last COMPLETED record and is re-readable through `db:read_csv`. Asserted by `ScriptErrorMidWriteStillLeavesEarlierRowsReadable`. Note the residual, accepted property: a partial file is still a partial file — CSV carries no completion marker, and adding one (or an atomic write-then-rename) is declined for the milestone. |
| T-05-02 | Tampering | `db:write_csv` target path (Phase 4, unchanged) | medium | **accept** | Carried acceptance from Phase 4's T-04-02 — a script can truncate any file inside the sandbox. This plan adds no write path; the flush writes only to the path the script already named and already truncated at open. |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | n/a | **No supply-chain threat exists for this plan**: no package-manager install task, no dependency added; RESEARCH.md § Package Legitimacy Audit records "Not applicable". Recorded rather than manufactured. |
</threat_model>

<verification>
- Task 1 leaves the suite RED for exactly the two new tests, and the observed byte count is in the
  summary.
- Task 2 turns them GREEN with the test file unmodified (`git diff` on the test file is empty for
  that task).
- `grep -c 'collect_garbage' src/lua_runner.cpp` is 1.
- `./build/bin/quiver_tests.exe` and `./build/bin/quiver_c_tests.exe` both exit 0.
- Neither test destroys, moves from, or reassigns the `LuaRunner` before asserting.
</verification>

<success_criteria>
ROADMAP Phase 5 criterion 4 is true and asserted by an automated test: a script that returns
without calling `w:close()` leaves a complete, re-readable file, asserted after `LuaRunner::run`
returns with the `LuaRunner` still alive and undestroyed — and the same holds when the script exits
by throwing.
</success_criteria>

<output>
Create `.planning/phases/05-ragged-rows-and-forgotten-closes/05-02-SUMMARY.md` when done. It MUST
record the byte count actually observed on the unflushed fixture file during task 1's RED run
(D-49), not the ROADMAP's unverified "zero bytes" claim.
</output>
