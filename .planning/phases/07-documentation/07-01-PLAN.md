---
phase: 07-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bindings/js/README.md
  - bindings/js/MIGRATION.md
autonomous: true
requirements: [DOCS-01, DOCS-02]

must_haves:
  truths:
    - "README tells users how to run Deno binding with correct permissions"
    - "README explains the Deno permission model (--allow-ffi for native library access)"
    - "README no longer references koffi, Node.js, npm, or Bun"
    - "MIGRATION.md explains what changed (koffi to Deno.dlopen)"
    - "MIGRATION.md explains why (path-scoped permissions, native Deno support)"
    - "MIGRATION.md documents developer workflow changes (not public API changes)"
  artifacts:
    - path: "bindings/js/README.md"
      provides: "Updated Deno usage documentation"
      contains: "--allow-ffi"
    - path: "bindings/js/MIGRATION.md"
      provides: "Migration guide from koffi to Deno.dlopen"
      contains: "Deno.dlopen"
  key_links:
    - from: "bindings/js/README.md"
      to: "bindings/js/deno.json"
      via: "task commands reference deno.json tasks"
      pattern: "deno task test"
    - from: "bindings/js/MIGRATION.md"
      to: "bindings/js/src/loader.ts"
      via: "documents the Deno.dlopen loading mechanism"
      pattern: "Deno\\.dlopen"
---

<objective>
Rewrite README.md for Deno usage and create MIGRATION.md documenting the koffi-to-Deno.dlopen transition.

Purpose: Users opening the JS binding directory need to know (1) how to run and test it with Deno, including which permission flags are required and why, and (2) what changed from the previous koffi-based version and how their developer workflow is affected.

Output: Updated bindings/js/README.md and new bindings/js/MIGRATION.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@bindings/js/deno.json
@bindings/js/src/loader.ts
@bindings/js/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md for Deno (DOCS-01)</name>
  <files>bindings/js/README.md</files>
  <read_first>
    - bindings/js/README.md (current outdated content to fully replace)
    - bindings/js/deno.json (task names, exports config, version)
    - bindings/js/src/index.ts (public exports to document)
    - bindings/js/src/loader.ts (library search tiers to document)
    - bindings/js/test/test.bat (test runner invocation)
  </read_first>
  <action>
Rewrite bindings/js/README.md from scratch. The entire current content is outdated (references koffi, Node.js, npm, Bun). Replace with Deno-specific documentation.

**Header and description:**
- Title: `# quiverdb`
- Description: `SQLite wrapper binding for Quiver via Deno FFI (Deno.dlopen).`
- No mention of koffi, Node.js, or Bun anywhere.

**Requirements section:**
- Deno >= 2.0
- `libquiver.dll` and `libquiver_c.dll` (or `.so`/`.dylib`) accessible (see Native Library Setup)

**Installation section:**
- Since this is a Deno module (not on npm), document importing from local path:
  ```typescript
  import { Database } from "./src/index.ts";
  ```
- Note: The native Quiver libraries must be compiled separately. See the Quiver project build instructions.

**Deno Permissions section (key for DOCS-01):**
This binding uses Deno FFI to load native C libraries at runtime. Deno requires explicit permission flags:

| Flag | Why |
|------|-----|
| `--allow-ffi` | Required to call `Deno.dlopen()` and load the native Quiver C library |
| `--allow-read` | Required to locate the native library on disk and read schema/data files |
| `--allow-write` | Required when creating or modifying databases |
| `--allow-env` | Optional, used by test infrastructure |

Explain that `--allow-ffi` is Deno's security model: unlike Node.js where any package can load native code silently, Deno requires explicit user consent. This is a security feature, not a limitation.

Example run command:
```bash
deno run --allow-ffi --allow-read --allow-write my_script.ts
```

**Native Library Setup section:**
Document the 3-tier search order from loader.ts:
1. Bundled: `libs/{os}-{arch}/` directory next to the binding source
2. Dev mode: Walk up directories looking for `build/bin/` (auto-discovered during development)
3. System PATH: Falls back to loading by library name from system PATH

For development, the simplest approach is to have `build/bin/` in the repo tree -- the loader finds it automatically.

**Quick Start section:**
```typescript
import { Database } from "./src/index.ts";

const db = Database.fromSchema("my.db", "schema.sql");
db.createElement("Items", { label: "Item 1", value: 42 });

const values = db.readScalarIntegers("Items", "value");
console.log(values); // [42]

db.close();
```

Run with: `deno run --allow-ffi --allow-read --allow-write example.ts`

**API Methods section:**
Keep the same API method listing as the current README (Lifecycle, Create/Delete, Read bulk, Read by ID, Read IDs, Query, Transaction) -- the public API did not change. Copy these sections verbatim from the current README. Add additional sections for methods documented in index.ts exports:

- Metadata: `getScalarMetadata`, `getVectorMetadata`, `getSetMetadata`, `getTimeSeriesMetadata`, `listScalarAttributes`, `listVectorGroups`, `listSetGroups`, `listTimeSeriesGroups`
- Time Series: `readTimeSeriesGroup`, `updateTimeSeriesGroup`
- CSV: `exportCSV`, `importCSV`
- Composites: `readScalarsById`, `readVectorsById`, `readSetsById`
- Introspection: `describe`, `isHealthy`, `path`, `currentVersion`
- Lua: `LuaRunner` class with `run(script)` and `close()`

**Types section:**
Keep the current exported types list. Add `ScalarMetadata`, `GroupMetadata`, `TimeSeriesData`, `CsvOptions` (all exported from index.ts).

**Development section:**
```bash
deno task test       # Run all tests (includes --allow-ffi --allow-read --allow-write --allow-env)
deno task lint       # Biome linting
deno task format     # Biome formatting (auto-fix)
```

Or run tests directly:
```bash
deno test --allow-ffi --allow-read --allow-write --allow-env test/
```

**License section:**
MIT
  </action>
  <verify>
    <automated>cd bindings/js && grep -c "allow-ffi" README.md && grep -c "Deno.dlopen\|Deno FFI" README.md && grep -c "koffi\|Node.js\|npm\|Bun" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "allow-ffi" bindings/js/README.md` returns at least 3 matches (permissions table, run command, test command)
    - `grep "Deno.dlopen\|Deno FFI" bindings/js/README.md` returns at least 1 match
    - `grep -c "koffi" bindings/js/README.md` returns 0
    - `grep -c "npm install" bindings/js/README.md` returns 0
    - `grep -c "bun test" bindings/js/README.md` returns 0
    - `grep "deno task test" bindings/js/README.md` returns a match
    - `grep "deno run" bindings/js/README.md` returns a match
    - `grep "Deno >= 2.0\|Deno 2" bindings/js/README.md` returns a match
    - `grep "permission" bindings/js/README.md` returns at least 1 match explaining the permission model
  </acceptance_criteria>
  <done>README.md is rewritten for Deno with --allow-ffi documented, permission model explained, koffi/Node.js/npm/Bun references removed, and development commands use deno task</done>
</task>

<task type="auto">
  <name>Task 2: Create MIGRATION.md (DOCS-02)</name>
  <files>bindings/js/MIGRATION.md</files>
  <read_first>
    - bindings/js/src/loader.ts (Deno.dlopen loading mechanism to document)
    - bindings/js/deno.json (new config to reference)
    - bindings/js/src/ffi-helpers.ts (marshaling changes to summarize)
    - .planning/phases/05-configuration-packaging/05-01-SUMMARY.md (config changes reference)
    - .planning/phases/06-test-migration-validation/06-01-SUMMARY.md (test migration reference)
  </read_first>
  <action>
Create bindings/js/MIGRATION.md documenting the koffi-to-Deno.dlopen migration.

**Title and intro:**
```
# Migration Guide: koffi to Deno FFI

This document describes the migration of the quiverdb JS binding from koffi (Node.js FFI) to Deno's native FFI (`Deno.dlopen`).
```

**What Changed section:**

1. **FFI Layer**: `koffi.load()` replaced with `Deno.dlopen()`. All 85 C API symbols now use Deno FFI type descriptors (`"pointer"`, `"i32"`, `"i64"`, `"f64"`, `"buffer"`, `"usize"`, `"void"`) instead of koffi type strings.

2. **Memory Marshaling**: 
   - Pointer out-parameters use `BigInt` (`Deno.UnsafePointer`) instead of koffi `Buffer`
   - String encoding uses `TextEncoder`/`TextDecoder` instead of koffi string coercion
   - Array decoding uses `Deno.UnsafePointerView` instead of `koffi.decode`
   - Struct field reading uses `Deno.UnsafePointerView` at byte offsets instead of koffi struct definitions
   - Native memory allocation uses `Uint8Array`/`DataView` instead of `koffi.alloc`/`koffi.encode`

3. **Package Configuration**:
   - `package.json` replaced by `deno.json`
   - `tsconfig.json` removed (Deno handles TypeScript natively)
   - `vitest.config.ts` removed
   - No `node_modules` directory needed

4. **Test Runner**:
   - `vitest` replaced by `Deno.test` with `@std/assert`
   - Test command: `deno test --allow-ffi --allow-read --allow-write --allow-env test/`

5. **Imports**:
   - All `node:fs`, `node:path`, `node:url`, `node:os` imports removed
   - Uses `jsr:@std/path` for path operations
   - Uses `import.meta.dirname` instead of `dirname(fileURLToPath(import.meta.url))`
   - All internal imports use `.ts` extension (Deno requirement)

**Why section:**

1. **Path-scoped FFI permissions**: Deno's `--allow-ffi` flag makes native library loading explicit. Users must opt in to FFI access, unlike Node.js where any package can silently load native code via koffi or N-API. This is a security improvement.

2. **Native Deno support**: `Deno.dlopen` is a built-in API -- no third-party FFI library dependency. This eliminates the koffi npm package (and its native compilation step) from the dependency chain.

3. **Single runtime target**: The binding previously aimed for Node.js/Deno/Bun compatibility via koffi. Targeting Deno exclusively simplifies the codebase and removes compatibility shims.

**Breaking Changes (Developer Workflow) section:**

Make clear: The public API (Database class, method signatures, types) did NOT change. These are developer workflow changes only.

| Before (koffi) | After (Deno FFI) |
|-----------------|-------------------|
| `npm install` | No install step (Deno imports from source) |
| `bun test` or `npx vitest` | `deno task test` |
| `node script.js` | `deno run --allow-ffi --allow-read --allow-write script.ts` |
| `package.json` scripts | `deno.json` tasks |
| `tsconfig.json` required | Not needed (Deno built-in TypeScript) |
| koffi npm dependency | No FFI dependency (Deno built-in) |
| Works with Node.js, Deno, Bun | Deno only |

**Public API Unchanged section:**

State explicitly: All public methods, types, and error handling remain identical. Code using `Database.fromSchema`, `createElement`, `readScalarIntegers`, etc. requires no changes beyond updating the import path to use `.ts` extension.
  </action>
  <verify>
    <automated>cd bindings/js && test -f MIGRATION.md && grep -c "Deno.dlopen" MIGRATION.md && grep -c "koffi" MIGRATION.md && grep -c "allow-ffi" MIGRATION.md && grep -c "permission" MIGRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f bindings/js/MIGRATION.md` succeeds (file exists)
    - `grep "Deno.dlopen" bindings/js/MIGRATION.md` returns at least 2 matches (what changed + why sections)
    - `grep "koffi" bindings/js/MIGRATION.md` returns at least 3 matches (documenting what was replaced)
    - `grep "allow-ffi" bindings/js/MIGRATION.md` returns at least 1 match
    - `grep "permission" bindings/js/MIGRATION.md` returns at least 1 match explaining the permission model
    - `grep "Public API" bindings/js/MIGRATION.md` returns a match confirming API did not change
    - `grep "Breaking Changes\|Developer Workflow" bindings/js/MIGRATION.md` returns a match
    - `grep "deno task test\|deno test" bindings/js/MIGRATION.md` returns a match
    - `grep "package.json\|deno.json" bindings/js/MIGRATION.md` returns a match documenting config change
  </acceptance_criteria>
  <done>MIGRATION.md exists documenting the koffi-to-Deno.dlopen transition: what changed (FFI layer, marshaling, config, tests, imports), why (path-scoped permissions, native Deno support, single runtime), and breaking developer workflow changes (not public API)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation files | Static markdown, no executable content, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | README.md / MIGRATION.md | accept | Documentation files contain no secrets, credentials, or internal paths. All content describes public API usage. |

No security-relevant trust boundaries in this phase. All work is static documentation with no executable code, no user input handling, and no data flow.
</threat_model>

<verification>
1. `grep -c "koffi" bindings/js/README.md` returns 0
2. `grep -c "allow-ffi" bindings/js/README.md` returns >= 3
3. `grep "deno task test" bindings/js/README.md` returns a match
4. `test -f bindings/js/MIGRATION.md` succeeds
5. `grep "Deno.dlopen" bindings/js/MIGRATION.md` returns >= 2
6. `grep "Public API" bindings/js/MIGRATION.md` returns a match
</verification>

<success_criteria>
- README.md documents Deno as the sole runtime with --allow-ffi flag and permission model explanation
- README.md contains no references to koffi, Node.js, npm, or Bun
- MIGRATION.md explains what changed (koffi to Deno.dlopen), why (permissions + native support), and developer workflow impact
- MIGRATION.md explicitly states the public API is unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/07-documentation/07-01-SUMMARY.md`
</output>
