---
phase: 02-config-path-locale-and-struct-size-safety
plan: 07
type: execute
wave: 4
depends_on: ["02-02", "02-03", "02-04", "02-05", "02-06"]
files_modified:
  - CHANGELOG.md
autonomous: false
requirements: [OPT-03, OPT-05, OPT-06, SAFE-01, SAFE-02, SAFE-03]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`CHANGELOG.md`'s unreleased heading reads `## [0.11.0] — unreleased` and its compare link reads `v0.10.6...v0.11.0`. This is a RENAME of Phase 1's existing unreleased `## [0.10.7]` heading, not a new section — no `v0.10.7` tag exists and none should (D-18, RESEARCH Pitfall 2)."
    - "The 0.11.0 section carries a **BREAKING**-prefixed entry naming the `quiver_database_options_t` 8 -> 24 byte growth AND telling a caller what to do about it."
    - "`scripts/assert_version.py` reports no skew: all five manifests still agree on 0.10.6 until the Bump Version workflow rewrites them. This plan hand-edits NO manifest."
    - "The Bump Version workflow is dispatched at most ONCE, with `part=minor`, which takes 0.10.6 straight to 0.11.0. A `part=patch` dispatch first would cut a real, tagged `v0.10.7` release shipping only Phase 1 with none of this phase's content (RESEARCH Pitfall 2)."
    - "EDGE/ordering — the unreleased section's entries are ordered BREAKING first, then Added, then Changed; the compare-link block stays newest-first, matching every existing section."
    - "EDGE/empty — if Phase 3 is folded into the same 0.11.0 section later, no second heading is created; entries append to the existing unreleased block."
    - "EDGE/concurrency — N/A: `bump-version.yml` declares a `bump-version` concurrency group with `cancel-in-progress: false`, so a second dispatch would queue rather than race. The single-dispatch rule is about not creating a spurious tag, not about races."
    - statement: "The BREAKING entry is actually actionable — a reader learns what breaks and what to do, not merely that something changed."
      verification: backstop
  artifacts:
    - "CHANGELOG.md — the `## [0.11.0] — unreleased` heading, its compare link, and the BREAKING entry"
  key_links:
    - "`CMakeLists.txt:4` still reads `VERSION 0.10.6` — Phase 1 wrote a changelog heading but never dispatched a bump, which is why `0.10.7 -> 0.11.0` is one rename and one dispatch, not two releases."
    - "`scripts/assert_version.py`'s bump table computes `minor` as `{major}.{minor+1}.0` from the CURRENT manifest version, so a single `part=minor` dispatch goes 0.10.6 -> 0.11.0 and skips 0.10.7 entirely."
    - "ROADMAP Phase 3's note says Phase 2 and Phase 3 'should ship in the same release'. That is the open question Task 2 puts to the developer."
  prohibitions:
    - statement: "MUST NOT hand-edit `CMakeLists.txt`, `bindings/python/pyproject.toml`, `bindings/js/package.json`, `bindings/dart/pubspec.yaml` or `bindings/julia/Project.toml`. D-18: dispatch the Bump Version workflow, which rewrites all five atomically and refuses to run from a skewed state."
      status: active
      verification: "`git diff --stat` for this plan names only `CHANGELOG.md`; `uv run python scripts/assert_version.py` exits 0."
    - statement: "MUST NOT dispatch Bump Version more than once, and MUST NOT dispatch it with `part=patch`. A patch dispatch produces a tagged `v0.10.7` shipping only Phase 1's enum-label feature and none of this phase's ABI change."
      status: active
      verification: "`gh run list --workflow=bump-version.yml --limit 5` shows at most one run created by this plan, and its `part` input is `minor`."
    - statement: "MUST NOT create a git tag by hand. Tags track the library version and are created by the release pipeline (user memory: GSD milestones do not get tags)."
      status: active
      verification: "`git tag --points-at HEAD` is empty."
---

<objective>
Reconcile the changelog to 0.11.0 with a BREAKING entry that tells a caller what to do, then
decide — with the developer — whether to cut the release now or hold it for Phase 3.

Purpose: two facts make this more than a formality. `CHANGELOG.md` already heads an
**unreleased** `## [0.10.7]` from Phase 1 that was never tagged, while `CMakeLists.txt` is
still 0.10.6 — so `0.10.7 -> 0.11.0` is a rename plus one dispatch, not two sequential
releases. And the ROADMAP says Phase 2 and Phase 3 should ship together, which makes
"dispatch now" a genuine open question rather than a settled step.
Output: a correct 0.11.0 unreleased section, and either a single `part=minor` dispatch or a
recorded decision to hold.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-config-path-locale-and-struct-size-safety/02-CONTEXT.md
@.planning/phases/02-config-path-locale-and-struct-size-safety/02-RESEARCH.md
@.github/CLAUDE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename the unreleased heading to 0.11.0 and write the BREAKING entry</name>
  <read_first>
    - CHANGELOG.md lines 1-30 (the header rules, the `## [0.10.7] — unreleased` heading, its Added entry, and its compare link at `[0.10.7]: .../compare/v0.10.6...v0.10.7`)
    - CHANGELOG.md tail (the compare-link block, newest-first)
    - CMakeLists.txt lines 1-8 (`VERSION 0.10.6` — proof no bump ever ran for Phase 1)
    - scripts/assert_version.py lines 1-50 and 84-120 (the five TARGETS and the bump table)
    - .planning/phases/02-config-path-locale-and-struct-size-safety/02-RESEARCH.md, Pitfall 2
    - The five SUMMARY files from 02-02..02-06 (what actually shipped, so the entry describes reality)
  </read_first>
  <files>CHANGELOG.md</files>
  <action>
    Rename the existing `## [0.10.7] — unreleased` heading to `## [0.11.0] — unreleased` and
    rewrite its compare link to `[0.11.0]: https://github.com/psrenergy/quiver/compare/v0.10.6...v0.11.0`.
    Do not add a second section and do not remove Phase 1's Added entry — it ships in this
    release too, because no `v0.10.7` tag was ever cut.

    Add a **BREAKING**-prefixed entry at the top of the 0.11.0 section, above the existing
    Added block, that states what changed and what a caller must do. It must name:
    `quiver_database_options_t` grew from 8 to 24 bytes by appending `const char* ui_config_dir`
    and `const char* ui_locale`; any consumer linking the C API directly must recompile against
    the new header, and any hand-written FFI layer must be updated — the five first-party
    bindings already are; the C API now exposes `quiver_database_options_sizeof`,
    `quiver_scalar_metadata_sizeof` and `quiver_group_metadata_sizeof` so a consumer can check
    its own layout at load time, and every first-party binding now does, failing loudly on
    skew rather than corrupting memory.

    Add the non-breaking additions as Added entries in the same section: an explicit UI config
    directory and locale as optional parameters on `open` / `from_schema` / `from_migrations`
    in all five bindings and as `--ui-config-dir` / `--ui-locale` on `quiver_cli`; and
    `has_ui_config()` reaching every layer.

    Write in the file's established voice — a bolded lead sentence stating the change, then
    plain prose. Do not restate the implementation; say what a reader must do.

    Touch no manifest. `scripts/assert_version.py` must still report all five in agreement at
    0.10.6.
  </action>
  <verify>
    <automated>uv run python scripts/assert_version.py &amp;&amp; grep -c '^## \[0.11.0\] — unreleased$' CHANGELOG.md &amp;&amp; grep -c 'compare/v0.10.6\.\.\.v0.11.0' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## \[0.11.0\] — unreleased$' CHANGELOG.md` is **1**.
    - `grep -c '0.10.7' CHANGELOG.md` is **0** — the heading and its compare link were renamed,
      not duplicated.
    - `grep -c 'compare/v0.10.6\.\.\.v0.11.0' CHANGELOG.md` is **1**.
    - The 0.11.0 section contains a line beginning with `- **BREAKING**` that names both `8` and
      `24` and names `quiver_database_options_t`.
    - That BREAKING entry contains an explicit instruction to the reader (recompile / update a
      hand-written FFI layer), not only a description of the change.
    - `uv run python scripts/assert_version.py` exits 0 and reports 0.10.6.
    - `git diff --stat` names only `CHANGELOG.md`.
  </acceptance_criteria>
  <done>The changelog heads a correct, actionable 0.11.0 unreleased section and no manifest has moved.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Dispatch the Bump Version workflow now (cutting 0.11.0 with Phase 2 only), or hold until Phase 3 lands and ship both in one release?</decision>
  <context>
    This is the phase's one genuinely undecided one-way door. D-18 says Phase 2 "ships as a
    minor bump — 0.11.0" and to dispatch the Bump Version workflow. The ROADMAP's Phase 3 note
    says Phase 2 and Phase 3 are "both native changes and should ship in the same release".
    Both cannot be true at once, and the source artifacts do not resolve it.

    What is already settled and needs no confirmation: the version NUMBER (0.11.0), the bump
    PART (`minor`, once — never `patch` first), and the ABI break itself. Only the TIMING is
    open.

    Undo cost if this is wrong: dispatching now opens a Bump Version PR; merging it and tagging
    publishes 0.11.0 to npm, PyPI, pub.dev, the Julia artifact bucket and S3. Those are not
    retractable — a mistake costs a 0.11.1. Holding costs nothing but a delay.

    State of the tree at this checkpoint: plans 02-01 through 02-06 are complete and the full
    six-suite build is green. Phase 3 (Structured Attribute Metadata) has not started.
  </context>
  <options>
    <option id="hold-for-phase-3">
      <name>Hold — leave 0.11.0 unreleased, dispatch after Phase 3</name>
      <pros>One ABI-breaking release instead of two; honours the ROADMAP's explicit "same release" note; Phase 3's entries append to the same unreleased section with no second heading; zero risk of publishing a half-milestone.</pros>
      <cons>Phase 2's value (claw's unblocking) sits unshipped until Phase 3 lands; the manifests stay at 0.10.6 longer, so any consumer testing against a local build has no version marker for the ABI change.</cons>
    </option>
    <option id="dispatch-now">
      <name>Dispatch now — `part=minor`, once</name>
      <pros>Unblocks claw immediately; the ABI break gets its own clearly-scoped release, which is easier to describe and easier to bisect; matches D-18's literal wording.</pros>
      <cons>Phase 3 then needs its own release weeks later, giving consumers two upgrades; contradicts the ROADMAP note; publishing is not retractable.</cons>
    </option>
  </options>
  <resume-signal>Select: hold-for-phase-3, or dispatch-now</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Execute the decision and record it</name>
  <precondition>Task 2's checkpoint has been answered. Do not proceed on a guess — this is the one step in the phase that publishes.</precondition>
  <read_first>
    - .github/CLAUDE.md (the release ritual: publish-s3 -> tag -> Julia/Python/JS in parallel, Dart by hand)
    - .github/workflows/bump-version.yml (the `part` choice input and the `bump-version` concurrency group)
    - scripts/ci/dispatch_workflow.sh (usage and its `-f key=value` input forwarding)
  </read_first>
  <files>CHANGELOG.md</files>
  <action>
    If the developer chose **hold-for-phase-3**: dispatch nothing. Record in the SUMMARY that
    the 0.11.0 section is complete and unreleased, that the single `part=minor` dispatch is
    deferred to the end of Phase 3, and that a `part=patch` dispatch must never be run first
    (it would tag a `v0.10.7` shipping only Phase 1). Add the deferral to STATE.md's Pending
    Todos so it cannot be forgotten between phases.

    If the developer chose **dispatch-now**: dispatch the Bump Version workflow exactly once
    with `part=minor`, using `gh workflow run bump-version.yml -f part=minor` (or
    `scripts/ci/dispatch_workflow.sh bump-version.yml master part=minor`). Then verify the PR
    it opens takes all five manifests from 0.10.6 to 0.11.0 and touches nothing else. Do not
    merge it, do not create a tag, and do not run any publish workflow — those are the
    developer's, per `.github/CLAUDE.md`'s release ritual.

    Either way, record the decision and its rationale in the SUMMARY so the next reader does
    not re-derive it.
  </action>
  <verify>
    <automated>uv run python scripts/assert_version.py &amp;&amp; git tag --points-at HEAD | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git tag --points-at HEAD` is empty — no hand-created tag.
    - If **hold-for-phase-3**: `gh run list --workflow=bump-version.yml --limit 5` shows no run
      created by this plan, and STATE.md's Pending Todos names the deferred `part=minor` dispatch.
    - If **dispatch-now**: exactly one Bump Version run exists with input `part=minor`, and the
      PR it opens changes all five manifests from `0.10.6` to `0.11.0` and nothing else.
    - In both cases `uv run python scripts/assert_version.py` exits 0 against the working tree.
    - The SUMMARY names the chosen option and its rationale.
  </acceptance_criteria>
  <done>The release decision is executed and written down, with no tag created by hand and no manifest hand-edited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo -> public registries | A tag triggers publication to npm, PyPI, pub.dev, the Julia artifact bucket and S3. Not retractable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-17 | Tampering | A `part=patch` dispatch cutting a spurious `v0.10.7` | high | mitigate | The single-dispatch `part=minor` rule is a prohibition with a `gh run list` verification, and Task 2's checkpoint states the settled part explicitly so it is not re-derived under time pressure. |
| T-02-18 | Repudiation | Five manifests drifting out of agreement via a hand edit | medium | mitigate | No manifest is hand-edited; `scripts/assert_version.py` is the verify command on two of the three tasks and refuses to bump from a skewed state. |
| T-02-19 | Elevation of Privilege | Publishing a half-milestone before Phase 3 | medium | mitigate | Task 2's blocking `checkpoint:decision` puts the timing to the developer rather than letting an autonomous run publish. |
| T-02-SC | Tampering | Registry publication of a compromised artifact | high | transfer | Publication runs through the existing `.github/` release pipeline with trusted publishing (OIDC, top-level workflow filename validation). This plan adds no new publish path and changes no workflow. |
</threat_model>

<verification>
- `uv run python scripts/assert_version.py` exits 0.
- `grep -c '0.10.7' CHANGELOG.md` is 0.
- `git tag --points-at HEAD` is empty.
</verification>

<success_criteria>
- A `## [0.11.0] — unreleased` section with a correct compare link and an actionable BREAKING entry.
- No manifest hand-edited, no tag hand-created.
- The release-timing decision made by the developer and recorded.
</success_criteria>

<output>
Create `.planning/phases/02-config-path-locale-and-struct-size-safety/02-07-SUMMARY.md` when done
</output>
</content>
