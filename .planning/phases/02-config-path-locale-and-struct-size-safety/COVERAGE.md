No external API integration: the api-coverage detector fired on "The C API surface Wave 2 consumes" — Quiver's *own* in-repo C API (`include/quiver/c/`), not a third-party API, SDK, or network service. This phase adds `*_sizeof` accessors and UI-config options to that first-party surface and threads them through the five in-repo bindings; there is no external capability surface to enumerate or opt out of.
