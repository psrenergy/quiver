---
phase: 02-config-path-locale-and-struct-size-safety
plan: 06
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03", "02-04", "02-05"]
files_modified:
  - src/lua_runner.cpp
  - src/cli/main.cpp
  - bindings/js/src/lua-api.ts
  - tests/test_lua_runner_ui_options.cpp
  - tests/CMakeLists.txt
  - src/CLAUDE.md
autonomous: true
requirements: [OPT-01, OPT-02, OPT-03, OPT-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`db:has_ui_config()` is bound on the Lua `Database` usertype and returns a Lua boolean (OPT-04, D-13)."
    - "`bindings/js/test/lua-api-sync.test.ts` stays green — the `src/lua_runner.cpp` binding and the `bindings/js/src/lua-api.ts` reference line land as ONE edit, never two."
    - "`quiver_cli` accepts `--ui-config-dir` and `--ui-locale`, consumed into `DatabaseOptions` before `Database` construction, alongside `--read-only` and `--log-level` (OPT-01..03, D-14)."
    - "`LuaRunner` itself is unchanged: it takes an already-configured `Database&` and has no options channel of its own (D-14)."
    - "Running `quiver_cli` against `foresight_like` with `--ui-config-dir` pointing off-tree and `--ui-locale es` prints a report containing `Ingenuo Estacional` — proven by an AUTOMATED test that shells out to the built binary, not by a manual probe."
    - "EDGE/empty — a flag that is present but empty behaves as absent, because the CLI forwards the string straight into `DatabaseOptions` and 02-01's empty-is-unset rule governs from there. The CLI adds no rule of its own."
    - "EDGE/adjacency — `--ui-config-dir` naming exactly `<db_dir>/ui` takes the override branch and produces the same config as omitting the flag, per 02-01's branch rule."
    - "EDGE/ordering — Lua's own describe output ordering is Phase 1's and is untouched; only labels change with the locale."
    - "EDGE/concurrency — N/A: `quiver_cli` runs one script against one database in one thread, and sqlite3 is built with `SQLITE_THREADSAFE=0`."
    - "The CLI performs NO validation of the two new paths — an absent or malformed directory degrades via D-05's warn-and-continue, consistent with every other `DatabaseOptions` value the CLI passes through unvalidated (`--read-only`, `--log-level`). This answers 02-RESEARCH.md Open Question 3."
    - statement: "ASSUMPTION (probe edge `unclassified`, unresolved) — OPT-01/OPT-02 carry no CLI- or Lua-specific ordering or adjacency semantics beyond the rules above."
      verification: backstop
  artifacts:
    - "src/lua_runner.cpp — `has_ui_config` on the `Database` usertype"
    - "bindings/js/src/lua-api.ts — the `db:has_ui_config()` reference line"
    - "src/cli/main.cpp — `--ui-config-dir` and `--ui-locale` arguments"
    - "tests/test_lua_runner_ui_options.cpp — NEW file, including the automated CLI probe"
  key_links:
    - "`bindings/js/test/lua-api-sync.test.ts` parses `new_usertype<Database>` blocks by the clang-format line shape (the bound name alone on its own line as a bare lowercase string) and cross-checks against `LUA_DB_API_REFERENCE`. Adding one without the other fails that test."
    - "`src/cli/main.cpp` already builds a `quiver::DatabaseOptions options{}` and sets `read_only` / `console_level` before the three-mode `Database` construction lambda — the two new flags go in exactly that block."
    - "`gtest_discover_tests(quiver_tests WORKING_DIRECTORY $<TARGET_FILE_DIR:quiver_tests>)` puts the test's cwd next to `quiver_cli`, which is what makes shelling out to the CLI from a gtest case practical."
  prohibitions:
    - statement: "MUST NOT relocate `LUA_DB_API_REFERENCE` out of `bindings/js/src/lua-api.ts`, or move it into an imported `.md`. That move was implemented, verified and reverted; it fixes editing ergonomics only, at the cost of a `files`-allowlist dependency and a Bun text-loader floor (root 'Do Not Fix')."
      status: active
      verification: "`grep -c 'LUA_DB_API_REFERENCE' bindings/js/src/lua-api.ts` is at least 1 and the constant is still a TypeScript template literal in that file."
    - statement: "MUST NOT give `LuaRunner` an options channel, a `ui_config_dir` parameter, or any config-path logic. D-14 puts the surface on `quiver_cli`; `LuaRunner` takes an already-configured `Database&`."
      status: active
      verification: "`git diff include/quiver/lua_runner.h` is empty and `git diff src/lua_runner.cpp` adds only the `has_ui_config` binding."
    - statement: "MUST NOT add Lua file-sandbox handling for `ui_config_dir`. The path is supplied by the CLI host (a C++ caller), not by a script; `resolve_sandboxed_path` governs script-supplied paths and does not apply."
      status: active
      verification: "`grep -n 'resolve_sandboxed_path' src/lua_runner.cpp` shows no new occurrence, and `has_ui_config`'s binding takes no path argument."
    - statement: "MUST NOT add validation of the two new CLI flags. D-05 already covers an absent or malformed explicit directory at the C++ layer; validating here would diverge from every other pass-through flag."
      status: active
      verification: "`src/cli/main.cpp` contains no `std::filesystem::exists` check for either new flag."
---

<objective>
Give the Lua host the same surface every other binding just gained: `db:has_ui_config()` in
the runner, and `--ui-config-dir` / `--ui-locale` on `quiver_cli`.

Purpose: Lua's host is `quiver_cli` (D-14). `LuaRunner` takes an already-configured
`Database&` and has no options channel, so the config-path and locale surface is two argparse
flags and nothing else. The one non-mechanical part is that a new `db:` method is a
TWO-file edit by contract — `src/lua_runner.cpp` and `bindings/js/src/lua-api.ts` — enforced
by `lua-api-sync.test.ts`.
Output: one new Lua method, two new CLI flags, and an automated test that actually runs the
built CLI rather than leaving the phase's only Lua-layer proof to a manual probe.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-config-path-locale-and-struct-size-safety/02-CONTEXT.md
@.planning/phases/02-config-path-locale-and-struct-size-safety/02-RESEARCH.md
@.planning/phases/02-config-path-locale-and-struct-size-safety/02-01-SUMMARY.md
@src/CLAUDE.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: CLI flags to a Spanish label, proven by running the built binary</name>
  <precondition>`build/bin/quiver_cli.exe` and `build/bin/quiver_tests.exe` are built from 02-01's sources — assert with `./build/bin/quiver_tests.exe --gtest_filter='DatabaseUiOptions.LocaleAffectsRenderedLabel'`, which proves `DatabaseOptions::ui_locale` reaches `require_ui_config`.</precondition>
  <read_first>
    - src/cli/main.cpp (the WHOLE file — `parse_log_level`, the argparse block, the mutual-exclusivity checks, the `quiver::DatabaseOptions options{}` block and the three-mode construction lambda)
    - src/lua_runner.cpp lines 255-300 (the `new_usertype<Database>` block, specifically `is_healthy` at :271 — the exact clang-format line shape the sync test parses)
    - bindings/js/src/lua-api.ts lines 140-200 (where `db:is_healthy()` and the describe methods are documented)
    - bindings/js/test/lua-api-sync.test.ts (the parser: bound name alone on its own line as a bare lowercase string)
    - tests/CMakeLists.txt lines 1-60 (the `quiver_tests` source list)
    - tests/test_lua_runner_describe.cpp (the suite's open/fixture idiom — note plan 01-05 used plain `TEST(...)` with `open_ui_fixture` rather than `TEST_F(LuaRunnerTest, ...)`, because that fixture's temp dir has no `ui/` sibling)
    - tests/schemas/ui/README.md rows L9 and L19
  </read_first>
  <files>src/cli/main.cpp, src/lua_runner.cpp, bindings/js/src/lua-api.ts, tests/test_lua_runner_ui_options.cpp, tests/CMakeLists.txt</files>
  <behavior>
    - `db:has_ui_config()` returns `true` for a fixture whose `ui/` loaded and `false` otherwise.
    - `quiver_cli --schema <foresight schema> --ui-config-dir <foresight ui> --ui-locale es <db> <script>`
      where the script returns `db:describe_collection("EconomicDriver")` prints a report
      containing `Ingenuo Estacional` and not `Seasonal Na\xC3\xAFve`.
    - The same invocation without `--ui-locale` prints `Seasonal Na\xC3\xAFve`.
    - An invocation whose `--ui-config-dir` does not exist still exits 0 and prints a report.
  </behavior>
  <action>
    Bind `has_ui_config` on the Lua `Database` usertype in `src/lua_runner.cpp`, directly
    beside `is_healthy` (~:271) and in the same shape — the bound name alone on its own line,
    then a lambda returning `self.has_ui_config()`. `bindings/js/test/lua-api-sync.test.ts`
    parses that exact clang-format line shape, so matching it is not cosmetic.

    In the SAME edit, add `db:has_ui_config()                 -- boolean: did a ui/ sidecar
    load?` to `LUA_DB_API_REFERENCE` in `bindings/js/src/lua-api.ts`, beside `db:is_healthy()`.
    One edit, two files — the sync test exists because this drifted before. Do not relocate
    the constant or move it into a `.md`; that was tried and reverted (root 'Do Not Fix').

    Add two arguments to `src/cli/main.cpp`'s argparse block, beside `--read-only` and
    `--log-level`: `--ui-config-dir` ("load the UI config from this directory instead of
    `<db_dir>/ui`") and `--ui-locale` ("locale for UI labels; defaults to en"). Consume both
    into the existing `quiver::DatabaseOptions options{}` block, before the three-mode
    construction lambda, using `program.present<std::string>(...)` so an unsupplied flag leaves
    the field at its default. Do NOT validate either path: D-05 already degrades an absent or
    malformed explicit directory with a warn log and no throw, and every other option the CLI
    forwards (`--read-only`, `--log-level`) is passed through unvalidated. This answers
    RESEARCH Open Question 3; record the answer in the SUMMARY.

    Create `tests/test_lua_runner_ui_options.cpp` and add it to the `quiver_tests` source list
    in `tests/CMakeLists.txt`. Also add `add_dependencies(quiver_tests quiver_cli)` so the CLI
    binary exists before the suite runs.

    Cover the Lua half with plain `TEST(...)` cases using `open_ui_fixture` from
    `tests/test_ui_fixture.h`, following plan 01-05's precedent rather than `TEST_F(LuaRunnerTest, ...)`.

    Cover the CLI half with an AUTOMATED case, not a manual probe. `gtest_discover_tests` sets
    the working directory to `$<TARGET_FILE_DIR:quiver_tests>`, which is where `quiver_cli`
    also lands, so the case can write a one-line Lua script and a command line to a temp
    directory, invoke the CLI through `std::system` with stdout redirected to a temp file, and
    assert on the captured bytes. Assert the exit status as well as the content. Build the
    database file inside the temp directory — a directory with no `ui/` sibling — with
    `--schema` pointing at `tests/schemas/ui/foresight_like/schema.sql` and `--ui-config-dir`
    at `tests/schemas/ui/foresight_like/ui`.

    Write the accented expected literal as the UTF-8 escape `Seasonal Na\xC3\xAFve` in the C++
    source — the MSVC build does not pass `/utf-8`.

    Cases: `LuaHasUiConfigTrueWhenLoaded`, `LuaHasUiConfigFalseWhenAbsent`,
    `CliUiLocaleFlagRendersSpanishLabel`, `CliDefaultLocaleRendersEnglishLabel`,
    `CliMissingUiConfigDirStillSucceeds`.
  </action>
  <verify>
    <automated>cmake --build build --config Debug &amp;&amp; ./build/bin/quiver_tests.exe --gtest_filter='LuaRunnerUiOptions.*'</automated>
  </verify>
  <acceptance_criteria>
    - `CliUiLocaleFlagRendersSpanishLabel` runs the real `quiver_cli` binary, asserts exit
      status 0, asserts the captured stdout contains `Ingenuo Estacional`, and asserts it does
      NOT contain `Seasonal Na\xC3\xAFve`.
    - `CliDefaultLocaleRendersEnglishLabel` asserts the inverse on the same fixture.
    - `CliMissingUiConfigDirStillSucceeds` asserts exit status 0 for a `--ui-config-dir` that
      does not exist.
    - `LuaHasUiConfigTrueWhenLoaded` and `LuaHasUiConfigFalseWhenAbsent` both pass.
    - `cd bindings/js && bun test test/lua-api-sync.test.ts` is green.
    - `git diff include/quiver/lua_runner.h` is empty.
    - `quiver_cli --help` lists both `--ui-config-dir` and `--ui-locale`.
  </acceptance_criteria>
  <done>A Lua script can ask whether a UI config loaded, and `quiver_cli` can point at a config directory anywhere on disk in any locale — both proven by automated tests, with no manual step left over.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite green and the Lua surface recorded</name>
  <read_first>
    - src/CLAUDE.md, the LuaRunner and CLI sections
    - scripts/test-all.bat (the six suites plus the `quiver_cli` smoke test it already runs)
    - example/example1.lua and example/example1.bat (the CLI's worked example — read to confirm the new flags do not change it)
  </read_first>
  <files>src/CLAUDE.md</files>
  <action>
    Run `scripts/build-all.bat` and confirm all six suites plus the existing `quiver_cli`
    smoke test are green. Run `scripts/format.bat` and accept only the formatting of files this
    plan touched.

    Update `src/CLAUDE.md`: `db:has_ui_config()` is bound on the Lua `Database` usertype and is
    mirrored in `bindings/js/src/lua-api.ts` as one edit; `quiver_cli` gained
    `--ui-config-dir` and `--ui-locale`, forwarded into `DatabaseOptions` unvalidated because
    D-05 already degrades a bad path; and `LuaRunner` is deliberately unchanged because it
    takes an already-configured `Database&`.

    Note in the SUMMARY that the phase's previously manual-only verification — the
    `quiver_cli` probe listed in `02-VALIDATION.md` — is now covered by
    `LuaRunnerUiOptions.Cli*`, so no manual step remains for OPT-01/OPT-02 at the Lua layer.
  </action>
  <verify>
    <automated>scripts/build-all.bat</automated>
  </verify>
  <acceptance_criteria>
    - All six suites green, plus the `quiver_cli` smoke test.
    - `src/CLAUDE.md` names the new Lua method, the two CLI flags, the no-validation decision
      and the LuaRunner-unchanged rule.
    - The SUMMARY records that `02-VALIDATION.md`'s manual CLI probe row is now automated.
  </acceptance_criteria>
  <done>Every layer including Lua carries the new surface, and the phase has no manual-only verification left for OPT-01/OPT-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv -> `DatabaseOptions` | A user-supplied filesystem path forwarded into the core unvalidated, by design. |
| `src/lua_runner.cpp` -> `bindings/js/src/lua-api.ts` | A documentation contract with no compiler behind it, enforced only by `lua-api-sync.test.ts`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | Denial of Service | `--ui-config-dir` naming an absent or malformed directory | medium | mitigate | D-05's degrade-and-warn at the C++ layer; the CLI adds no validation and cannot throw. Pinned by `CliMissingUiConfigDirStillSucceeds`. |
| T-02-16 | Repudiation | The Lua reference drifting from the actual bound surface | medium | mitigate | `lua-api-sync.test.ts` is an existing gate; the two-file edit is mandated by this plan and re-asserted in its acceptance criteria. |
| T-02-03 | Information Disclosure | A CLI user pointing `--ui-config-dir` at an arbitrary directory | low | accept | The CLI user already has the host process's filesystem access; this is the same acceptance as T-02-03 in 02-01. Explicitly not sandboxed — Lua's sandbox governs script-supplied paths, and this path is host-supplied. |
| T-02-SC | Tampering | package-manager installs | high | accept | None occur in this plan. |
</threat_model>

<verification>
- `scripts/build-all.bat` green.
- `cd bindings/js && bun test test/lua-api-sync.test.ts` green.
- `quiver_cli --help` lists both new flags.
</verification>

<success_criteria>
- `db:has_ui_config()` bound and documented in one edit.
- `--ui-config-dir` and `--ui-locale` on `quiver_cli`, unvalidated by design.
- An automated test runs the real CLI binary and reads a Spanish label out of its stdout.
</success_criteria>

<output>
Create `.planning/phases/02-config-path-locale-and-struct-size-safety/02-06-SUMMARY.md` when done
</output>
</content>
