---
milestone: v1.0
checked: 2026-05-06
status: wired
phases_in_scope: [01-c-core]
plans_in_scope: [01-01, 01-02, 01-03]
findings:
  connected: 14
  orphaned: 0
  missing: 0
  broken_flows: 0
  unprotected_routes: 0
e2e_flows_complete: 1
e2e_flows_broken: 0
---

# Milestone v1.0 — Integration Check

> Single-phase milestone (Phase 1: c-core), 3 plans (binary extensions → expr core → expression tests).
> No external services, no APIs, no routes — integration boundary is the public C++ API surface and
> the in-process call chain from operator overloads through the AST to BinaryFile I/O.

## Wiring Summary

- **Connected:** 14 declared cross-plan exports, all imported and called at the correct point
- **Orphaned:** 0
- **Missing:** 0
- **API coverage:** N/A (pure C++ in-process; no HTTP/RPC routes)
- **Auth protection:** N/A
- **E2E flows:** 1 complete, 0 broken

## Plan 01 → Plan 02 Wiring (binary iterators / fast overloads → save engine + FileNode)

| Producer (Plan 01) | Consumer (Plan 02) | Call site | Status |
|--------------------|--------------------|-----------|--------|
| `quiver::binary::first_dimensions` | `Expression::save` row-loop init | `src/expr/expression.cpp:77` | WIRED |
| `quiver::binary::next_dimensions` | `Expression::save` row-loop step | `src/expr/expression.cpp:82` | WIRED |
| `BinaryFile::write(vector<double>, vector<int64_t>)` (D-14) | `Expression::save` row-loop write | `src/expr/expression.cpp:81` resolves to vector-overload at `src/binary/binary_file.cpp:202` | WIRED |
| `BinaryFile::read_into(vector<int64_t>, vector<double>&, bool)` (D-13) | `FileNode::compute_row` | `src/expr/nodes.cpp:37` | WIRED |
| `BinaryFile::open_file('w', meta)` | `Expression::save` writer | `src/expr/expression.cpp:74` | WIRED |
| `BinaryFile::open_file('r')` | `FileNode::compute_row` lazy open (D-10) | `src/expr/nodes.cpp:30` | WIRED |
| `BinaryFile::next_dimensions` (protected delegate) | `csv_converter.cpp:85, 130` regression-safe path | delegates to free function; null→`first_dimensions` translation preserves loop semantics | WIRED |

## Plan 02 → Plan 03 Wiring (public API → test fixture)

| Public API element | Test sites | Coverage |
|--------------------|-----------|----------|
| `Expression(const BinaryFile&)` implicit conversion | 5 cases (`Expression e = a;`) | CORE-01 |
| `operator+/-/*//` (Expression, Expression) | 7 cases | CORE-02 |
| Scalar overloads (left + right) | 8 `ScalarBroadcast{Op}{Side}` cases | CORE-03 |
| Chained `((a+b)-c)/2.0` | 3 cases | CORE-04 |
| `Expression::save(path)` | 25 distinct call sites | CORE-05 |
| `quiver::binary::first_dimensions` / `next_dimensions` | `write_qvr` + `read_all_cells` test helpers | dogfoods Plan 01 free functions |

29 / 29 declared `ExpressionFixture` cases present in source — exact match to VERIFICATION.md inventory.

## End-to-End Flow Trace: `Expression e = a + b; e.save("out.qvr")`

| Step | File:Line | Status |
|------|-----------|--------|
| 1. Implicit `BinaryFile → Expression` (CORE-01) | `expression.h:23` non-explicit ctor; body at `expression.cpp:46` constructs `FileNode` | WIRED |
| 2. `operator+(Expression, Expression)` builds `BinaryOpNode` (CORE-02) | `expression.cpp:111-113` → `make_binop` (line 95) | WIRED |
| 3. `BinaryOpNode` ctor 6-step validation (D-03..D-09) | `nodes.cpp:102-269` | WIRED |
| 4. Translation tables + sizes pre-computed (D-05) | `nodes.cpp:246-259` | WIRED |
| 5. Mutable buffers pre-sized (CORE-06) | `nodes.cpp:264-268` | WIRED |
| 6. `Expression::save(path)` D-11 self-save check BEFORE writer | `expression.cpp:63-70` (canonicalize, throw before `open_file('w',…)` at line 74) | WIRED — verified by `SelfSaveCollisionThrows` byte-snapshot test |
| 7. Writer opened with broadcast metadata | `expression.cpp:74` | WIRED |
| 8. Row loop init: `dims = first_dimensions(meta)`, `row` declared once (CORE-06) | `expression.cpp:77-78` | WIRED |
| 9. `node_->compute_row(dims, row)` virtual dispatch | `expression.cpp:80` → recurses through `BinaryOpNode` / `FileNode` / `ScalarNode` | WIRED |
| 10. `FileNode::compute_row` calls D-13 `read_into` (trusted-caller fast path) | `nodes.cpp:37` | WIRED |
| 11. `BinaryOpNode::compute_row` translates dims, recurses, applies op (D-08 label-axis broadcast) | `nodes.cpp:283-320` | WIRED |
| 12. `writer.write(row, dims)` D-14 fast write | `expression.cpp:81` resolves to `binary_file.cpp:202` | WIRED |
| 13. `next_dimensions(meta, dims)` advances; `nullopt` ⇒ break | `expression.cpp:82-86` | WIRED |
| 14. `.qvr` + `.toml` written to disk | TOML at `binary_file.cpp:90`; `.qvr` flushed in `~Impl` | WIRED |

Every link is wired with no gaps.

## CMake Wiring

| Required entry | Found at | Status |
|----------------|----------|--------|
| `binary/iteration.cpp` in QUIVER_SOURCES | `src/CMakeLists.txt:24` | WIRED |
| `expr/expression.cpp` in QUIVER_SOURCES | `src/CMakeLists.txt:29` | WIRED |
| `expr/nodes.cpp` in QUIVER_SOURCES | `src/CMakeLists.txt:30` | WIRED |
| `test_iteration.cpp` in `quiver_tests` | `tests/CMakeLists.txt:7` | WIRED |
| `test_expression.cpp` in `quiver_tests` | `tests/CMakeLists.txt:9` | WIRED |

## Public API Surface (QUIVER_API decoration)

| Symbol | Location | QUIVER_API |
|--------|----------|-----------|
| `class Expression` | `expression.h:17` | YES |
| `class Node` | `node.h:21` | YES |
| `class FileNode final` | `node.h:42` | YES |
| `class ScalarNode final` | `node.h:66` | YES |
| `class BinaryOpNode final` | `node.h:91` | YES |
| 12 free-function operator overloads | `expression.h:52-66` | YES (each individually) |
| `enum class BinaryOp` | `node.h:79` | public via header (no decoration needed for enum) |
| `quiver::binary::first_dimensions` | `iteration.h:16` | YES |
| `quiver::binary::next_dimensions` | `iteration.h:24` | YES |
| `BinaryFile::read(vector<int64_t>)` | `binary_file.h:42` | YES (class-level) |
| `BinaryFile::read_into` | `binary_file.h:48` | YES (class-level) |
| `BinaryFile::write(vector<double>, vector<int64_t>)` | `binary_file.h:50` | YES (class-level) |

## Requirements Integration Map

| Requirement | Cross-plan wiring path | Status |
|-------------|------------------------|--------|
| CORE-01 | `BinaryFile` → `FileNode(file)` (`nodes.cpp:19`) → `Expression(node_)` (`expression.cpp:46`) → tests via `Expression e = a;` | WIRED |
| CORE-02 | `Expression × Expression` → `make_binop` (`expression.cpp:95`) → `BinaryOpNode` ctor → 4 op-variant tests | WIRED |
| CORE-03 | `Expression × double` / `double × Expression` → `scalar_left/right` (`expression.cpp:99,104`) → `ScalarNode(value, sibling.metadata())` + `BinaryOpNode` → 8 ScalarBroadcast tests | WIRED |
| CORE-04 | Recursive `BinaryOpNode` chain via `compute_row` recursion (`nodes.cpp:312-313`) → `Chained`, `LargeGridCompletes` tests | WIRED |
| CORE-05 | `Expression::save(path)` → D-11 check → `BinaryFile::open_file('w',…)` → row loop → `.qvr`+`.toml` → `SaveProducesReadableFile`, `SelfSaveCollisionThrows*` tests | WIRED |
| CORE-06 | `expression.cpp:77-78` declares `dims`/`row` ONCE outside loop; `BinaryOpNode`/`FileNode` reuse mutable buffers (`node.h:60, 122-126`) → `LargeGridCompletes` runtime backstop (228-310 ms vs 5000 ms budget) | WIRED |
| TEST-01 | `quiver_tests` exec → 29 `ExpressionFixture` + 5 `IterationTest` + 9 `BinaryTempFileFixture` D-13/D-14 cases → 781/781 pass | WIRED |

## Verdict

**WIRED — no integration gaps.** All declared cross-plan exports are imported and used at the correct points. The end-to-end flow `Expression e = a + b; e.save(path)` traces cleanly through the AST. Build wiring complete. Public API fully `QUIVER_API`-decorated.

Pre-existing CR-01 / CR-02 / CR-03 / WR-* advisory items in `01-REVIEW.md` are correctness/quality concerns within individual files (not cross-plan integration breaks). Human verification gate on CR-01 was resolved as "accept-and-defer" per VERIFICATION.md.

---

*Checked: 2026-05-06*
*Checker: Claude (gsd-integration-checker)*
