---
milestone: v1.0
checked: 2026-05-06
re_checked: 2026-05-06
status: wired
phases_in_scope: [01-c-core, 02-tech-debt]
plans_in_scope: [01-01, 01-02, 01-03, 02-01]
findings:
  connected: 17
  orphaned: 0
  missing: 0
  broken_flows: 0
  unprotected_routes: 0
e2e_flows_complete: 1
e2e_flows_broken: 0
test_count: 787 / 787
c_test_count: 403 / 403
---

# Milestone v1.0 — Integration Check

> Two-phase milestone (Phase 1: c-core, 3 plans; Phase 2: tech-debt, 1 plan). No external services, no APIs, no routes — integration boundary is the public C++ API surface and the in-process call chain from operator overloads through the AST to BinaryFile I/O. Phase 2 is a refactor that closed 12 deferred audit items without changing the public API surface.

## Wiring Summary

- **Connected:** 17 declared cross-plan / cross-phase exports, all imported and called at the correct point (14 baseline from Phase 1; +3 internal connections re-traced post-Phase 2 refactor)
- **Orphaned:** 0 (Phase 2 deleted 3 dead-code paths — `validate_file_is_open`, member `dimension_sizes_at_values`, protected `next_dimensions` delegate — and verified zero in-tree callers remain)
- **Missing:** 0
- **API coverage:** N/A (pure C++ in-process; no HTTP/RPC routes)
- **Auth protection:** N/A
- **E2E flows:** 1 complete, 0 broken

## Plan 01 → Plan 02 Wiring (binary iterators / fast overloads → save engine + FileNode)

| Producer (Plan 01-01) | Consumer (Plan 01-02) | Call site | Status |
|--------------------|--------------------|-----------|--------|
| `quiver::binary::first_dimensions` | `Expression::save` row-loop init | `src/expr/expression.cpp:77` | WIRED |
| `quiver::binary::next_dimensions` | `Expression::save` row-loop step | `src/expr/expression.cpp:82` | WIRED |
| `BinaryFile::write(vector<double>, vector<int64_t>)` (D-14) | `Expression::save` row-loop write | `src/expr/expression.cpp:81` resolves to vector-overload at `src/binary/binary_file.cpp:209` | WIRED |
| `BinaryFile::read_into(vector<int64_t>, vector<double>&, bool)` (D-13) | `FileNode::compute_row` | `src/expr/nodes.cpp:37` | WIRED |
| `BinaryFile::open_file('w', meta)` | `Expression::save` writer | `src/expr/expression.cpp:74` | WIRED |
| `BinaryFile::open_file('r')` | `FileNode::compute_row` lazy open (D-10) | `src/expr/nodes.cpp:30` | WIRED |
| `quiver::binary::next_dimensions` (free function) | `csv_converter.cpp:86, 131` (post-Phase-2 re-point) | direct call with `std::optional` unwrap; replaces the deleted `BinaryFile::next_dimensions` protected delegate | WIRED |

## Plan 01-02 → Plan 01-03 Wiring (public API → test fixture)

| Public API element | Test sites | Coverage |
|--------------------|-----------|----------|
| `Expression(const BinaryFile&)` implicit conversion | 5 cases (`Expression e = a;`) + 1 new D-25 regression test | CORE-01 |
| `operator+/-/*//` (Expression, Expression) | 7 cases | CORE-02 |
| Scalar overloads (left + right) | 8 `ScalarBroadcast{Op}{Side}` cases | CORE-03 |
| Chained `((a+b)-c)/2.0` | 3 cases | CORE-04 |
| `Expression::save(path)` | 25 distinct call sites | CORE-05 |
| `quiver::binary::first_dimensions` / `next_dimensions` | `write_qvr` + `read_all_cells` test helpers | dogfoods Plan 01-01 free functions |

29 baseline + 5 new (Phase 2) = 34 / 34 declared `ExpressionFixture` cases present in source.

## Phase 1 → Phase 2 Cross-Phase Wiring (re-traced post-refactor)

| Phase 1 Producer | Phase 2 Consumer / Touchpoint | Site | Status |
|---|---|---|---|
| `BinaryOpNode` (private members) | New `lhs_to_out_` / `rhs_to_out_` reverse tables (WR-06) | `include/quiver/expr/node.h:122-123`, populated `src/expr/nodes.cpp:283-290`, consumed at `:313, :321` | WIRED |
| `BinaryOpNode::compute_row` hot loop | O(1) reverse-table lookups replace O(n) `find_dim_index` inner search | `src/expr/nodes.cpp:312-326` | WIRED |
| `BinaryFile::open_file('w', meta)` writer entry | Late-insert `write_registry` (CR-02) | `src/binary/binary_file.cpp:81-106` — registry insert moved to line 104 (post-`fill_file_with_nulls`) | WIRED — `Expression::save`'s writer-open flow unchanged in semantics |
| `quiver::binary::next_dimensions` free function | `csv_converter.cpp` direct call (WR-04 collateral) | `src/binary/csv_converter.cpp:86, :131` — both call sites use free function with `std::optional` unwrap | WIRED — 37/37 CSVConverterFixture tests pass |
| `validate_dimension_values_indexed` | Map-keyed `validate_dimension_values` (WR-05) | map overload converts to declaration-order vector, delegates to `_indexed`; calendar-aware logic now in exactly one place | WIRED |
| `BinaryOp` enum | `apply_op` exhaustive-switch canary (WR-07) | `src/expr/nodes.cpp:97` — throws `"Cannot apply: unhandled BinaryOp variant"` instead of `return 0.0` | WIRED |
| `next_dimensions` end-of-iteration logic | `bool incremented` flag (WR-09) | `src/binary/iteration.cpp:115-129` — no per-call `first_dimensions(meta)` rebuild | WIRED |

## End-to-End Flow Trace: `Expression e = a + b; e.save("out.qvr")` (post-Phase-2)

| Step | File:Line | Status |
|------|-----------|--------|
| 1. Implicit `BinaryFile → Expression` (CORE-01) | `expression.h:23` non-explicit ctor; body at `expression.cpp:46` constructs `FileNode` | WIRED |
| 2. `operator+(Expression, Expression)` builds `BinaryOpNode` (CORE-02) | `expression.cpp:111-113` → `make_binop` (line 95) | WIRED |
| 3. `BinaryOpNode` ctor 6-step validation (D-03..D-09 + CR-01/WR-01 symmetric mirror + parent-by-name) | `nodes.cpp:106-176` | WIRED |
| 4. Forward translation tables + sizes pre-computed (D-05) | `nodes.cpp:264-277` | WIRED |
| 5. Reverse translation tables `lhs_to_out_`/`rhs_to_out_` pre-computed (WR-06) | `nodes.cpp:283-290` | WIRED |
| 6. Mutable buffers pre-sized (CORE-06) | `nodes.cpp:296-299` | WIRED |
| 7. `Expression::save(path)` D-11 self-save check BEFORE writer | `expression.cpp:63-70` (canonicalize, throw before `open_file('w', ...)` at line 74) | WIRED — verified by `SelfSaveCollisionThrows` byte-snapshot test |
| 8. Writer opened with broadcast metadata; CR-02 late-insert preserves invariant | `expression.cpp:74` → `binary_file.cpp:81-106` (registry insert at `:104` after all throwable ops succeed) | WIRED |
| 9. Row loop init: `dims = first_dimensions(meta)`, `row` declared once (CORE-06) | `expression.cpp:77-78` | WIRED |
| 10. `node_->compute_row(dims, row)` virtual dispatch | `expression.cpp:80` → recurses through `BinaryOpNode` / `FileNode` / `ScalarNode` | WIRED |
| 11. `BinaryOpNode::compute_row` translates dims via O(1) reverse tables, recurses, applies op (D-08 label-axis broadcast) | `nodes.cpp:312-336` | WIRED |
| 12. `FileNode::compute_row` calls D-13 `read_into` (trusted-caller fast path) | `nodes.cpp:37` | WIRED |
| 13. `apply_op` switch with WR-07 canary | `nodes.cpp:82-98` (throws on unhandled variant instead of `return 0.0`) | WIRED |
| 14. `writer.write(row, dims)` D-14 fast write | `expression.cpp:81` resolves to `binary_file.cpp:209` | WIRED |
| 15. `next_dimensions(meta, dims)` advances; `nullopt` ⇒ break (WR-09 flag) | `expression.cpp:82-86` → `iteration.cpp:104-147` | WIRED |
| 16. `.qvr` + `.toml` written to disk | TOML at `binary_file.cpp:92`; `.qvr` flushed in `~Impl()` (line 41); registry erased at `:39` | WIRED |

Every link is wired with no gaps. Phase 2's internal refactor preserved every cross-plan call site verified in the original Phase 1 integration check.

## CMake Wiring

| Required entry | Found at | Status |
|----------------|----------|--------|
| `binary/iteration.cpp` in QUIVER_SOURCES | `src/CMakeLists.txt:24` | WIRED |
| `binary/csv_converter.cpp` in QUIVER_SOURCES | `src/CMakeLists.txt:25` | WIRED |
| `expr/expression.cpp` in QUIVER_SOURCES | `src/CMakeLists.txt:29` | WIRED |
| `expr/nodes.cpp` in QUIVER_SOURCES | `src/CMakeLists.txt:30` | WIRED |
| `test_iteration.cpp` in `quiver_tests` | `tests/CMakeLists.txt:7` | WIRED |
| `test_expression.cpp` in `quiver_tests` | `tests/CMakeLists.txt:9` | WIRED |
| `test_binary_file.cpp` in `quiver_tests` | `tests/CMakeLists.txt:4` | WIRED |
| `test_csv_converter.cpp` in `quiver_tests` | `tests/CMakeLists.txt:5` | WIRED |

## Public API Surface (QUIVER_API decoration — unchanged by Phase 2)

| Symbol | Location | QUIVER_API |
|--------|----------|-----------|
| `class Expression` | `expression.h:17` | YES |
| `class Node` | `node.h:21` | YES |
| `class FileNode final` | `node.h:42` | YES |
| `class ScalarNode final` | `node.h:66` | YES |
| `class BinaryOpNode final` | `node.h:91` | YES |
| 12 free-function operator overloads | `expression.h:52-66` | YES (each individually) |
| `enum class BinaryOp` | `node.h:79` | public via header (no decoration needed for enum) |
| `quiver::binary::first_dimensions` | `iteration.h:16` | YES |
| `quiver::binary::next_dimensions` | `iteration.h:24` | YES |
| `BinaryFile::read(vector<int64_t>)` | `binary_file.h:42` | YES (class-level) |
| `BinaryFile::read_into` | `binary_file.h:48` | YES (class-level) |
| `BinaryFile::write(vector<double>, vector<int64_t>)` | `binary_file.h:50` | YES (class-level) |

## Requirements Integration Map (post-Phase-2)

| Requirement | Cross-plan / cross-phase wiring path | Status |
|-------------|------------------------|--------|
| CORE-01 | `BinaryFile` → `FileNode(file)` (`nodes.cpp:19`) → `Expression(node_)` (`expression.cpp:46`) → tests `Expression e = a;` and the new D-25 `ImplicitConversionInOperatorArgument` regression at `test_expression.cpp:231` | WIRED |
| CORE-02 | `Expression × Expression` → `make_binop` (`expression.cpp:95`) → `BinaryOpNode` ctor → 4 op-variant tests | WIRED |
| CORE-03 | `Expression × double` / `double × Expression` → `scalar_left/right` (`expression.cpp:99,104`) → `ScalarNode(value, sibling.metadata())` + `BinaryOpNode` → 8 ScalarBroadcast tests | WIRED |
| CORE-04 | Recursive `BinaryOpNode` chain via `compute_row` recursion (`nodes.cpp:328-329`) → `Chained`, `LargeGridCompletes` tests | WIRED |
| CORE-05 | `Expression::save(path)` → D-11 check → CR-02 late-insert `BinaryFile::open_file('w',…)` → row loop → `.qvr`+`.toml` → `SaveProducesReadableFile`, `SelfSaveCollisionThrows*`, new D-24 `OpenFileWriteFailureDoesNotLeakRegistry` regression | WIRED |
| CORE-06 | `expression.cpp:77-78` declares `dims`/`row` ONCE outside loop; `BinaryOpNode`/`FileNode` reuse mutable buffers (`node.h:60, 122-126, 130-133`); WR-06 reverse tables eliminate per-row O(n) inner search → `LargeGridCompletes` runtime backstop unchanged (228-310 ms vs 5000 ms budget) | WIRED |
| TEST-01 | `quiver_tests` exec → 34 `ExpressionFixture` (29 baseline + 5 new) + 5 `IterationTest` + 9 `BinaryTempFileFixture` D-13/D-14 + 1 D-24 cases → 787/787 pass | WIRED |

## Verdict

**WIRED — no integration gaps.** All declared cross-plan exports are imported and used at the correct points. The end-to-end flow `Expression e = a + b; e.save(path)` traces cleanly through the AST. Build wiring complete. Public API fully `QUIVER_API`-decorated. Phase 2's internal refactor preserved every cross-plan call site verified in the original Phase 1 integration check; the `csv_converter.cpp` re-point from the deleted `BinaryFile::next_dimensions` protected delegate to the `quiver::binary::next_dimensions` free function is correctly wired at both call sites and 37/37 CSVConverterFixture tests pass.

**787/787 quiver_tests + 403/403 quiver_c_tests green.**

---

*Initial check: 2026-05-06 (Phase 1 only — `connected: 14`, `phases_in_scope: [01-c-core]`)*
*Re-check: 2026-05-06 (post-Phase-2 — `connected: 17`, `phases_in_scope: [01-c-core, 02-tech-debt]`)*
*Checker: Claude (gsd-integration-checker)*
