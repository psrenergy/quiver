---
phase: 05-ragged-rows-and-forgotten-closes
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - src/CLAUDE.md
  - CLAUDE.md
  - CHANGELOG.md
  - bindings/js/src/lua-api.ts
autonomous: true
requirements: [DOC-06]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # --- goal-backward (ROADMAP Phase 5 criterion 5) ---
    - "`src/CLAUDE.md`, the root `CLAUDE.md` and `CHANGELOG.md` each record the writer and its design decisions, and no file in the repository still claims that writing is not exposed or that `db:write_csv` does not exist (ROADMAP criterion 5)."
    - "Root `CLAUDE.md` § Design Decisions carries the writer inside the EXISTING `db:read_csv` bullet (D-53, not a new bullet), with the stale 'writing (`db:write_csv`) is not exposed' clause corrected, and the cross-layer table has a `CSV file write` row beside its `CSV file read` neighbour with `N/A` in every non-Lua column (D-52)."
    - "`CHANGELOG.md`'s existing `## [0.10.7] — unreleased` section carries the correction and the writer's own entry; all five version manifests and every version number in the file are untouched (D-51 — the 0.10.7-vs-0.10.6 mismatch is Phase 4's carried D-33)."
    - "`bindings/js/src/lua-api.ts`'s `LUA_DB_API_REFERENCE` states FMT-07's pad-short/throw-long rule and WRITE-06's flush-without-`close()` guarantee as clauses in the existing CSV file writing section -- no second worked example, and the existing fenced `lua` block is unchanged so the test that extracts and executes it still passes (D-50)."
    - "The whole suite passes in a RELEASE configuration, where `SOL_SAFE_GETTER` is off -- the configuration that has hidden Lua marshalling bugs in this project before, and which Phase 4 made its own task for the same reason."
    - "`bindings/js/test/lua-api-sync.test.ts` passes. Phase 5 binds no new `db:` name, so the gate is not what forces the `lua-api.ts` edit (D-50) -- but it parses `src/lua_runner.cpp` and is a hard build gate, so a green run is the proof that the FMT-07 and WRITE-06 code changes did not disturb it."
  flagged_assumptions:
    # --- probe edge DOC-06 / unclassified: stays UNRESOLVED, surfaced here, never auto-resolved ---
    - "UNRESOLVED EDGE (DOC-06/unclassified): the edge probe could not classify DOC-06 and its generic prompt ('review manually') yields no predicate. Surfaced rather than dropped or backstopped. The planner's reading, recorded as an assumption for the verifier and the developer: DOC-06 is a pure record-keeping requirement with no runtime behaviour, so its only checkable properties are the four file edits enumerated in `must_haves.truths` and RESEARCH.md Q6's edit list -- documentation CURRENCY (no file still describing a pre-Phase-4 contract) rather than a behavioural predicate. If the developer wants a stronger DOC-06 gate than 'these four files were edited as specified', that gate does not exist in any source artifact and must be stated before it can be planned."
  artifacts:
    - "src/CLAUDE.md -- added sentences in the existing `csv_write.h`/`csv_write.cpp` paragraph (where FMT-07's enforcement lives) plus one bullet in the `## LuaRunner` implementation-conventions list (the `collect_garbage()`-at-`run()`-exit guarantee)"
    - "CLAUDE.md (root) -- the corrected + extended `db:read_csv` Design Decisions bullet, and a new `CSV file write` cross-layer table row"
    - "CHANGELOG.md -- the corrected `### Added` clause plus the writer's own entry under the existing `## [0.10.7] — unreleased` section"
    - "bindings/js/src/lua-api.ts -- two added clauses in the `## CSV file writing` section of `LUA_DB_API_REFERENCE`"
  key_links:
    - "Four files describe the same feature. The stale clause appears in BOTH root `CLAUDE.md` and `CHANGELOG.md` in near-identical wording ('writing ... is not exposed' / 'reading is the only direction, `db:write_csv` is not exposed') -- fixing one and missing the other is the failure mode, so grep the whole repo for the claim rather than editing the two known line numbers."
    - "`bindings/js/src/lua-api.ts` is consumed two ways: `bindings/js/test/lua-api-sync.test.ts` parses it against `src/lua_runner.cpp`'s bound names, and `tests/test_lua_runner_write_csv.cpp`'s `extract_lua_example` pulls its fenced `lua` block out and EXECUTES it. Both must stay green, which is why the edit is clauses only and never touches the fence."
  prohibitions:
    - "No version number anywhere is changed -- not in `CHANGELOG.md`, and not in any of the five manifests (`CMakeLists.txt`, `bindings/python/pyproject.toml`, `bindings/js/package.json`, `bindings/dart/pubspec.yaml`, `bindings/julia/Project.toml`). The 0.10.7-vs-0.10.6 mismatch is a carried, already-accepted decision; bumping is the Bump Version workflow's job at release. 'Tidying it while I'm in the file' is the exact silent scope expansion D-51 forbids."
    - "No second worked example is added to `LUA_DB_API_REFERENCE`, and no new option is documented. Every byte of that constant is interpolated into every `claw` session, so documentation weight is a permanent per-session cost -- the addition is clauses in the existing section's voice."
    - "No documentation sentence describes behaviour that was not actually built and tested in 05-01/05-02. Documentation written ahead of the code is how the reference came to describe a subtly wrong contract in the first place; every clause here must be checkable against a passing test named in 05-01 or 05-02."
    - "The declined items are not documented as forthcoming, planned, or 'not yet' anywhere in these four files. A destructive-overwrite guard, an unclosed-writer warning, a whole-file `write_csv`, atomic write-then-rename, and any further option were declined for the milestone -- writing them up as future work turns a closed decision back into an open one."
---

<objective>
Record the writer and its design decisions in the four files that carry the project's memory, and
prove both Phase 5 code changes hold in a Release configuration.

Purpose: root `CLAUDE.md` and `CHANGELOG.md` both still state that `db:write_csv` is not exposed —
false since Phase 4 shipped — and neither FMT-07's width rule nor WRITE-06's flush is written down
anywhere a script author or a future agent would look. `SOL_SAFE_GETTER` is off in Release and has
hidden Lua marshalling bugs in this project before, so Debug-green is not shipped-green.
Output: four documentation edits, a green Release suite, and a green lua-api sync gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ragged-rows-and-forgotten-closes/05-CONTEXT.md
@.planning/phases/05-ragged-rows-and-forgotten-closes/05-PATTERNS.md
@.planning/phases/05-ragged-rows-and-forgotten-closes/05-RESEARCH.md
@.planning/phases/05-ragged-rows-and-forgotten-closes/05-01-SUMMARY.md
@.planning/phases/05-ragged-rows-and-forgotten-closes/05-02-SUMMARY.md
@CLAUDE.md
@src/CLAUDE.md
</context>

## Artifacts this phase produces

See the table in `05-01-PLAN.md` § "Artifacts this phase produces" — the canonical list for the
whole phase. This plan produces no new symbol: it edits four existing documentation files and runs
two build configurations. **No new files.**

<tasks>

<task type="auto">
  <name>Task 1: The documentation record — four files, clauses only, no version numbers</name>
  <files>src/CLAUDE.md, CLAUDE.md, CHANGELOG.md, bindings/js/src/lua-api.ts</files>
  <precondition>`05-01` and `05-02` are complete and their SUMMARYs exist — every clause written here must describe behaviour a passing test already asserts, including the pinned FMT-07 message text from 05-01 task 2.</precondition>
  <read_first>
    - `src/CLAUDE.md` lines 95-115 (the existing `csv_write.h`/`csv_write.cpp` paragraph carrying D-34/D-37/D-40) and lines 338-372 (the `## LuaRunner` implementation-conventions bullet list, for the voice and the place the new `run` bullet goes)
    - `CLAUDE.md` (root) — the `db:read_csv` Design Decisions bullet ending "...writing (`db:write_csv`) is not exposed; a script's parsed rows go through the existing group writers", and the cross-layer table's `CSV file read` row with its exact column shape
    - `CHANGELOG.md` lines 1-40 (the `## [0.10.7] — unreleased` section, its `### Changed` and `### Added` bullets, and the bullet style already in use)
    - `bindings/js/src/lua-api.ts` lines 698-735 (the whole `## CSV file writing` section: the fenced `lua` example, the two-options sentence, the truncate-at-open paragraph, and the `write_row`-after-`close` clause whose declarative voice this edit matches)
    - `tests/test_lua_runner_write_csv.cpp` lines 20-70 (`extract_lua_example`) — read to understand why the fenced block and its escaping must not be disturbed
    - `.planning/phases/05-ragged-rows-and-forgotten-closes/05-RESEARCH.md` Q6 (the concrete file/line edit list and the version-mismatch scope note)
    - `.planning/phases/05-ragged-rows-and-forgotten-closes/05-CONTEXT.md` D-50, D-51, D-52, D-53
    - `.planning/REQUIREMENTS.md` § Out of Scope (the five declined items, which must not be written up as future work)
    - The 05-01 and 05-02 SUMMARYs, for the pinned FMT-07 message text and the RED byte count
  </read_first>
  <action>
Four edits, each in its target file's own existing voice. Start by grepping the whole repository for
the stale claim (the phrase about writing not being exposed, and `db:write_csv` described as absent)
so no copy is missed — it appears in at least two files in near-identical wording.

`CLAUDE.md` (root), per D-52 and D-53: extend the EXISTING `db:read_csv` Design Decisions bullet
rather than adding a new one — that bullet already frames the pair as one decision and is where the
stale clause lives. Replace the stale clause with what the writer now is: streaming-only
(`db:write_csv` returns a handle, `w:write_row` appends, `w:close` finishes), Lua-only with no C++
header / C API / FFI counterpart, sandboxed through the same `resolve_sandboxed_path` gate,
truncate-at-open with no overwrite guard, two options and no more, numbers via `std::to_chars`
shortest round-trip, a `nil` cell and an empty-string cell indistinguishable after a round trip.
Then add FMT-07 and WRITE-06: with a `header`, its length is the row width — a short row pads with
empty cells and a long one raises the Pattern 1 message pinned in 05-01, naming the row ordinal and
both counts, with no header meaning no check; and a writer still open when `LuaRunner::run` returns
is flushed by one `collect_garbage()` at `run()`'s scope exit, covering the throw path too, with no
warning emitted. Separately, add a `CSV file write` row to the cross-layer table immediately beside
the existing `CSV file read` row, listing `db:write_csv()` / `w:write_row()` / `w:close()` in the
Lua column and `N/A` in every other column, matching its neighbour's shape exactly.

`CHANGELOG.md`, per D-51: work only inside the existing `## [0.10.7] — unreleased` section. Correct
the stale trailing clause in the `db:read_csv` `### Added` bullet — the one that denies the writer
exists. Add the writer's own `### Added` entry covering both phases — streaming-only with no whole-file form, the two options,
truncate-at-open, hand-rolled RFC-4180 over `std::ofstream` with no new dependency, `to_chars`
number formatting, `nil`/`""` indistinguishability, the header as row-width authority (pad short /
throw long), and the unclosed-writer flush. Follow the file's existing bullet style (short
present-tense clauses, identifiers in backticks). Change no version number and touch none of the
five manifests.

`src/CLAUDE.md`: add one or two sentences to the existing `csv_write.h`/`csv_write.cpp` paragraph
stating that FMT-07's width enforcement lives in the Lua-layer `CsvWriter` in `src/lua_runner.cpp`
and NOT in `Writer` — the ordinal and the decoded header already live there, `Writer` gained no
header state and no signature change, and padding happens before the vector reaches `append_record`
so its blank-line predicate sees the final cell count. Add one bullet to the `## LuaRunner`
implementation-conventions list, near the existing `run` bullet, describing the guard: one
function-local RAII guard declared before `safe_script` calls `collect_garbage()` once at `run()`'s
scope exit, covering both returns and the throw path, which is what flushes a writer the script
never closed; note that it is declared before `result` so reverse-order destruction releases the
Lua stack reference first, and that one call was proven sufficient by an executed probe against this
repo's own vendored sol2/Lua build, so it must not be "hardened" into a loop.

`bindings/js/src/lua-api.ts`, per D-50: add two clauses to the existing `## CSV file writing`
section, in the same declarative script-author-facing voice as the truncate-at-open paragraph and
the `write_row`-after-`close` clause — the pad-short/throw-long rule (with a `header`, its length is
the row width; a shorter `write_row` pads with empty cells, a longer one throws), and the
flush-without-`close()` guarantee (a writer never explicitly closed is still flushed when the
script's run returns, so the file is complete and re-readable, and no warning is emitted). Clauses
only: no second worked example, and do not touch the fenced `lua` block or its backslash-escaped
fence markers — a test extracts that block and executes it verbatim.

Across all four files: describe only behaviour 05-01 and 05-02 actually built and tested, and do not
mention any of the five declined items as future or planned work.
  </action>
  <verify>
    <automated>cd bindings/js &amp;&amp; bun test test/lua-api-sync.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'is not exposed' --include=*.md --include=*.ts . | grep -v '^\./\.planning/'` returns nothing — every copy of the stale claim, in both files that carry it, is corrected.
    - `grep -c 'CSV file write' CLAUDE.md` returns 1, and that line contains `db:write_csv` and at least three `N/A` cells.
    - `grep -n 'header' CLAUDE.md | grep -c 'row width\|width authority'` returns at least 1 — FMT-07 is recorded in the root file.
    - `grep -c 'collect_garbage' src/CLAUDE.md` returns at least 1 — WRITE-06 is recorded in the `src/` file.
    - `grep -c 'header_width\|width enforcement' src/CLAUDE.md` returns at least 1 — FMT-07's placement is recorded.
    - `git diff CHANGELOG.md | grep -E '^[-+].*0\.10\.[0-9]'` returns nothing — no version line changed.
    - `git diff --name-only -- CMakeLists.txt bindings/python/pyproject.toml bindings/js/package.json bindings/dart/pubspec.yaml bindings/julia/Project.toml` returns nothing — the five manifests are untouched.
    - `git diff --numstat bindings/js/src/lua-api.ts` shows deletions of 0 or 1 line (only the two-options sentence may be re-flowed) — the edit is additive clauses, not a rewrite.
    - `cd bindings/js && bun test test/lua-api-sync.test.ts` exits 0.
    - `./build/bin/quiver_tests.exe --gtest_filter=LuaRunner_WriteCsv.*` still exits 0 — this includes the test whose `extract_lua_example` pulls the reference's fenced block out and executes it, so a disturbed fence or a broken example fails here.
  </acceptance_criteria>
  <done>All four files record the writer and both Phase 5 behaviours in their own voice, no file still claims writing is unexposed, no version number or manifest moved, the declined items appear nowhere as future work, and both consumers of `lua-api.ts` are green.</done>
</task>

<task type="auto">
  <name>Task 2: Release build — the configuration where SOL_SAFE_GETTER is off</name>
  <files>(no source changes — verification only)</files>
  <precondition>`05-01` and `05-02` are complete and the Debug suite is green; a Release-only failure is otherwise unattributable.</precondition>
  <read_first>
    - `.planning/STATE.md` § Accumulated Context — the standing note that Release must be exercised separately because `SOL_SAFE_GETTER` is off there and has hidden Lua marshalling bugs before, and 04-03's recorded Release counts to compare against
    - `cmake/CompilerOptions.cmake` — the `SOL_SAFE_*` definitions and how they differ between configurations
    - `CMakePresets.json` — note that the `release` configure preset sets `QUIVER_BUILD_TESTS=OFF` and so cannot be used for this task
    - `scripts/build-all.bat` — the project's documented configure line, for the flags to mirror
  </read_first>
  <action>
Configure and build a separate Release tree and run both C++ suites in it. Use a dedicated
directory (`build-release/`, already in `.gitignore`) with the single-config Ninja generator, mirroring
`scripts/build-all.bat`'s flags but with `CMAKE_BUILD_TYPE=Release`, so the binary paths are
deterministic and the existing Debug tree at `build/` is left intact. Do not use the `release`
configure preset — it sets `QUIVER_BUILD_TESTS=OFF` and would build no tests at all, passing
vacuously.

Run `quiver_tests` and `quiver_c_tests` from the Release tree and record both pass counts in the
plan summary alongside 04-03's recorded Debug/Release counts, so a silently shrinking suite is
visible. If any `LuaRunner*` test fails only in Release, that is a genuine sol2 marshalling finding
in the new FMT-07 or `GcGuard` code — diagnose and fix it in `src/lua_runner.cpp` rather than
relaxing the test, and note it in the summary.

Then re-run the Debug suite and the lua-api sync gate one final time so the phase closes with every
gate green in both configurations. No source file is expected to change in this task; if one does,
name it and the Release-only failure that forced it in the summary.
  </action>
  <verify>
    <automated>cmake -S . -B build-release -G Ninja -DCMAKE_BUILD_TYPE=Release -DQUIVER_BUILD_TESTS=ON -DQUIVER_BUILD_C_API=ON &amp;&amp; cmake --build build-release &amp;&amp; ./build-release/bin/quiver_tests.exe &amp;&amp; ./build-release/bin/quiver_c_tests.exe</automated>
  </verify>
  <acceptance_criteria>
    - `cmake -S . -B build-release -G Ninja -DCMAKE_BUILD_TYPE=Release -DQUIVER_BUILD_TESTS=ON -DQUIVER_BUILD_C_API=ON` succeeds and `cmake --build build-release` succeeds.
    - `./build-release/bin/quiver_tests.exe` exits 0, and its reported test count is greater than or equal to 04-03's recorded Release count plus the tests added in 05-01 and 05-02.
    - `./build-release/bin/quiver_c_tests.exe` exits 0.
    - `./build-release/bin/quiver_tests.exe --gtest_filter=LuaRunner_WriteCsv.*` exits 0 — the FMT-07 and WRITE-06 paths specifically, in the configuration where `SOL_SAFE_GETTER` is off.
    - `./build/bin/quiver_tests.exe` and `./build/bin/quiver_c_tests.exe` both still exit 0 (Debug re-run).
    - `cd bindings/js && bun test test/lua-api-sync.test.ts` exits 0.
    - Both Release pass counts are written into `05-03-SUMMARY.md`.
  </acceptance_criteria>
  <done>Both C++ suites pass in a Release build with `SOL_SAFE_GETTER` off, the pass counts are recorded next to 04-03's, and Debug plus the lua-api sync gate are green at phase close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `LUA_DB_API_REFERENCE` -> every `claw` session's system prompt | `bindings/js/src/lua-api.ts` is interpolated into an agent's prompt. A wrong clause here is not a cosmetic defect: it teaches every future session a contract the code does not honour. This is the only boundary this plan touches. |

No code boundary is crossed: this plan changes no runtime behaviour, accepts no input, and adds no
dependency. The sandbox, the error channel and the emitter are all untouched.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Repudiation | `LUA_DB_API_REFERENCE` (`bindings/js/src/lua-api.ts`) | low | mitigate | The reference currently documents no width rule at all, so an agent writing a script cannot tell whether a short row is padded, rejected, or silently misaligned — and the file it produces is wrong in a way neither it nor the caller can account for. Mitigated by this plan's two added clauses (D-50), and by the build gate plus the extract-and-execute test both staying green so the clauses cannot drift from the binding. |
| T-05-07 | Tampering | version manifests / `CHANGELOG.md` version lines | low | mitigate | Editing `CHANGELOG.md` for content puts the executor one line away from "tidying" the accepted 0.10.7-vs-0.10.6 mismatch, which would desynchronise the five manifests `scripts/assert_version.py` asserts agree and mis-stamp a release. Mitigated by the explicit prohibition plus the two `git diff` acceptance gates on the version lines and the five manifests. |
| T-05-02 | Tampering | `db:write_csv` target path (Phase 4, unchanged) | medium | **accept** | Carried acceptance from Phase 4's T-04-02. This plan's only relation to it is documentary: WRITE-08's truncate-at-open note already stands in the reference and root `CLAUDE.md`, and the declined overwrite guard must not be written up as future work. |
| T-05-SC | Tampering | npm/pip/cargo installs | n/a | n/a | **No supply-chain threat exists for this plan**: no package-manager install task, no dependency added. The `bun test` invocation runs an already-installed workspace. RESEARCH.md § Package Legitimacy Audit records "Not applicable". Recorded rather than manufactured. |
</threat_model>

<verification>
- The stale claim about writing not being exposed survives nowhere outside `.planning/`.
- All four target files are edited; no version number and none of the five manifests moved.
- `bun test test/lua-api-sync.test.ts` exits 0.
- `./build/bin/quiver_tests.exe`, `./build/bin/quiver_c_tests.exe`,
  `./build-release/bin/quiver_tests.exe` and `./build-release/bin/quiver_c_tests.exe` all exit 0.
- The Release pass counts are recorded in the summary next to 04-03's.
</verification>

<success_criteria>
ROADMAP Phase 5 criterion 5 is true: `src/CLAUDE.md`, the root `CLAUDE.md` and `CHANGELOG.md` record
the writer and its design decisions — with `bindings/js/src/lua-api.ts` covered as well per D-50 —
and both Phase 5 code changes hold in a Release build.
</success_criteria>

<output>
Create `.planning/phases/05-ragged-rows-and-forgotten-closes/05-03-SUMMARY.md` when done. It MUST
record both Release pass counts and carry forward the unresolved DOC-06 edge flagged in this plan's
`must_haves.flagged_assumptions`.
</output>
